From bd8c0c4bf763ab1591dd22fb7b9a9213308d8078 Mon Sep 17 00:00:00 2001
From: ruddygreat <ruddygreat1@gmail.com>
Date: Tue, 4 Nov 2025 13:58:47 +0000
Subject: [PATCH 01/10] basic account creation on player spawn. ATMs next.

---
 .../PersonalEconomy/ClientBankingSystem.cs    |   8 ++
 .../Station/Systems/StationSpawningSystem.cs  |  35 ++++-
 .../PersonalEconomy/ServerBankingSystem.cs    | 117 ++++++++++++++++
 Content.Shared/PDA/PdaComponent.cs            |   9 ++
 Content.Shared/PDA/SharedPdaSystem.cs         |   6 +
 .../Components/BankCardComponent.cs           |  35 +++++
 .../Components/RemoteBankServerComponent.cs   |  47 +++++++
 .../Systems/SharedBankingSystem.cs            | 127 ++++++++++++++++++
 .../_Impstation/personal-economy/banking.ftl  |   6 +
 .../Entities/Objects/Devices/pda.yml          |  10 ++
 .../Entities/Objects/Misc/banking.yml         |  27 ++++
 11 files changed, 426 insertions(+), 1 deletion(-)
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/ClientBankingSystem.cs
 create mode 100644 Content.Server/_Impstation/PersonalEconomy/ServerBankingSystem.cs
 create mode 100644 Content.Shared/_Impstation/PersonalEconomy/Components/BankCardComponent.cs
 create mode 100644 Content.Shared/_Impstation/PersonalEconomy/Components/RemoteBankServerComponent.cs
 create mode 100644 Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
 create mode 100644 Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl
 create mode 100644 Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml

diff --git a/Content.Client/_Impstation/PersonalEconomy/ClientBankingSystem.cs b/Content.Client/_Impstation/PersonalEconomy/ClientBankingSystem.cs
new file mode 100644
index 0000000000000..7849df4c2840b
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/ClientBankingSystem.cs
@@ -0,0 +1,8 @@
+﻿using Content.Shared._Impstation.PersonalEconomy.Systems;
+
+namespace Content.Client._Impstation.PersonalEconomy;
+
+public sealed class ClientBankingSystem : SharedBankingSystem
+{
+
+}
diff --git a/Content.Server/Station/Systems/StationSpawningSystem.cs b/Content.Server/Station/Systems/StationSpawningSystem.cs
index 92c8493574841..fb19471cdf400 100644
--- a/Content.Server/Station/Systems/StationSpawningSystem.cs
+++ b/Content.Server/Station/Systems/StationSpawningSystem.cs
@@ -1,8 +1,11 @@
+using Content.Server._Impstation.PersonalEconomy;
 using Content.Server.Access.Systems;
 using Content.Server.Humanoid;
 using Content.Server.Mind;
 using Content.Server.PDA;
 using Content.Server.Station.Components;
+using Content.Shared._Impstation.PersonalEconomy.Components;
+using Content.Shared._Impstation.PersonalEconomy.Systems;
 using Content.Shared.Access.Components;
 using Content.Shared.Access.Systems;
 using Content.Shared.CCVar;
@@ -44,6 +47,7 @@ public sealed class StationSpawningSystem : SharedStationSpawningSystem
     [Dependency] private readonly IPrototypeManager _prototypeManager = default!;
     [Dependency] private readonly MindSystem _mindSystem = default!;
     [Dependency] private readonly GrammarSystem _grammar = default!; // imp
+    [Dependency] private readonly SharedBankingSystem _banking = null!; //imp
 
     /// <summary>
     /// Attempts to spawn a player character onto the given station.
@@ -188,7 +192,10 @@ private void DoJobSpecials(ProtoId<JobPrototype>? job, EntityUid entity)
     /// <param name="characterName">Character name to use for the ID.</param>
     /// <param name="jobPrototype">Job prototype to use for the PDA and ID.</param>
     /// <param name="station">The station this player is being spawned on.</param>
-    public void SetPdaAndIdCardData(EntityUid entity, string characterName, JobPrototype jobPrototype, EntityUid? station)
+    public void SetPdaAndIdCardData(EntityUid entity,
+        string characterName,
+        JobPrototype jobPrototype,
+        EntityUid? station)
     {
         if (!InventorySystem.TryGetSlotEntity(entity, "id", out var idUid))
             return;
@@ -206,6 +213,32 @@ public void SetPdaAndIdCardData(EntityUid entity, string characterName, JobProto
         if (_prototypeManager.Resolve(jobPrototype.Icon, out var jobIcon))
             _cardSystem.TryChangeJobIcon(cardId, jobIcon, card);
 
+        //imp edit start - set up bank cards for players that spawn with PDAs
+        //I only want to give bank cards set up here an account automatically, other cards should need to be activated by the HoP
+        //why is this so evil
+        //<star wars opening text crawl music>
+        //todo split this out into a single method call
+        //todo this kinda really sucks, I don't like that it just doesn't work for ppl not spawned by spawning in at the station aaeugh
+        //todo move all of this logic out into comp init / map init?
+        //put starting balance & salary into there
+        //basically, I need to get the job proto / starting gear that an ent was spawned with after it's been spawned
+        //or am I fine with economy stuff only applying to people spawned on the station?
+        //ok - thought 2 - make the bank cards get spawned like Id cards using the wierd yaml datafield, then PDAs that don't need cards won't spawn with them
+        //use card prototypes to determine starting money and all that, mess with things with traits here
+        //card prototypes let me do cute dept-branded cards, which is fun
+        //still not entirely sure how things will get the right name for visitors etc etc, but maybe it'll just work out? hopefully? probably?
+        //something something 1-2 TC disposable agent card for nukies, etc etc
+        if (idUid is { } real && //is the id slot item real?
+            TryComp<PdaComponent>(real, out var pdaComp) && //is it a PDA?
+            pdaComp.BankCardSlot.ContainerSlot!.ContainedEntity is { } containedCard && //does it have something in the bank card slot?
+            TryComp<BankCardComponent>(containedCard, out var bankCardComp)) //is it a bank card?
+        {
+            var realCardRealTrue = (containedCard, bankCardComp);
+            _banking.SetAccountName(bankCardComp.AccessNumber, characterName);
+            _banking.UpdateCardDetails(realCardRealTrue, bankCardComp.AccessNumber);
+        }
+        //imp edit end
+
         var extendedAccess = false;
         if (station != null)
         {
diff --git a/Content.Server/_Impstation/PersonalEconomy/ServerBankingSystem.cs b/Content.Server/_Impstation/PersonalEconomy/ServerBankingSystem.cs
new file mode 100644
index 0000000000000..c79251b55a080
--- /dev/null
+++ b/Content.Server/_Impstation/PersonalEconomy/ServerBankingSystem.cs
@@ -0,0 +1,117 @@
+﻿using Content.Server.GameTicking.Events;
+using Content.Shared._Impstation.PersonalEconomy.Components;
+using Content.Shared._Impstation.PersonalEconomy.Systems;
+using Content.Shared.GameTicking;
+using Robust.Server.GameStates;
+using Robust.Shared.Map;
+using Robust.Shared.Prototypes;
+using Robust.Shared.Random;
+
+namespace Content.Server._Impstation.PersonalEconomy;
+
+/// <summary>
+/// This handles...
+/// </summary>
+public sealed class ServerBankingSystem : SharedBankingSystem
+{
+
+    [Dependency] private SharedMapSystem _map = null!;
+    [Dependency] private MetaDataSystem _metaData = null!;
+    [Dependency] private SharedTransformSystem _xform = null!;
+    [Dependency] private PvsOverrideSystem _pvsOverride = null!;
+    [Dependency] private IRobustRandom _random = null!;
+
+    private EntityUid _cheeseWorld;
+    private readonly EntProtoId _remoteServerProto = "RemoteBankServer";
+
+    /// <inheritdoc/>
+    public override void Initialize()
+    {
+        base.Initialize();
+
+        SubscribeLocalEvent<RoundStartingEvent>(OnRoundStart);
+        SubscribeLocalEvent<RoundRestartCleanupEvent>(OnRoundRestart);
+    }
+
+    private void OnRoundStart(RoundStartingEvent ev)
+    {
+        _cheeseWorld = EnsurePausedMap();
+        var server = Spawn(_remoteServerProto);
+        _xform.SetParent(server, _cheeseWorld);
+        _pvsOverride.AddForceSend(server); //probably not *great*, but every client needs to know about the banking server
+    }
+
+    /// <summary>
+    /// cleans up cheese world
+    /// </summary>
+    /// <param name="ev"></param>
+    private void OnRoundRestart(RoundRestartCleanupEvent ev)
+    {
+        if (!Exists(_cheeseWorld))
+            return;
+
+        Del(_cheeseWorld);
+    }
+
+    /// <summary>
+    /// creates a map for the banking server to live in
+    /// </summary>
+    private EntityUid EnsurePausedMap()
+    {
+        if (Exists(_cheeseWorld))
+            return _cheeseWorld;
+
+        var mapUid = _map.CreateMap();
+        _metaData.SetEntityName(mapUid, "cheese world. (for banking)");
+        return mapUid;
+    }
+
+    protected override void SetupID(Entity<BankCardComponent> ent)
+    {
+        var details = CreateNewAccount("Unknown");
+        ent.Comp.AccessNumber = details.AccessNumber;
+        ent.Comp.TransferNumber = details.TransferNumber;
+        SetAccountSalary(details.AccessNumber, ent.Comp.Salary);
+        SetAccountBalance(details.AccessNumber, ent.Comp.StartingBalance);
+        Dirty(ent);
+    }
+
+    /// <inheritdoc/>
+    public override (int AccessNumber, int TransferNumber) CreateNewAccount(string name)
+    {
+        var serverQuery = EntityQueryEnumerator<RemoteBankServerComponent>();
+        if (serverQuery.MoveNext(out var uid, out var server))
+        {
+            //generate a unique ID
+            var accountNo = _random.Next(0, 1000000);
+            while (server.AccountDict.ContainsKey(accountNo))
+            {
+                accountNo = _random.Next(0, 1000000);
+            }
+
+            //generate a unique transfer number
+            var transferNo = _random.Next(0, 10000);
+            while (server.TransferNumberToAccountNumberDict.ContainsKey(accountNo))
+            {
+                transferNo = _random.Next(0, 10000);
+            }
+
+            //create a new account, put it in the accounts dict
+            server.AccountDict[accountNo] = new BankAccount
+            {
+                AccessNumber = accountNo,
+                TransferNumber = transferNo,
+                Name = name,
+            };
+
+            //and map the transfer number to the account
+            server.TransferNumberToAccountNumberDict[transferNo] = accountNo;
+
+            Dirty<RemoteBankServerComponent>((uid, server));
+            return (accountNo, transferNo);
+        }
+
+        //todo error handling for if the remote server somehow gets deleted
+        return (0, 0);
+    }
+}
diff --git a/Content.Shared/PDA/PdaComponent.cs b/Content.Shared/PDA/PdaComponent.cs
index cdfeffa2c1ccd..a200ec724a917 100644
--- a/Content.Shared/PDA/PdaComponent.cs
+++ b/Content.Shared/PDA/PdaComponent.cs
@@ -38,5 +38,14 @@ public sealed partial class PdaComponent : Component
         [ViewVariables] public string? StationName;
         [ViewVariables] public string? StationAlertLevel;
         [ViewVariables] public Color StationAlertColor = Color.White;
+
+        //imp edit below for bank cards
+        public const string PdaBankCardSlotId = "PDA-bank-card";
+        [DataField]
+        public ItemSlot BankCardSlot = new();
+
+        //see above for the ID card stuff
+        [DataField]
+        public EntProtoId? BankCard;
     }
 }
diff --git a/Content.Shared/PDA/SharedPdaSystem.cs b/Content.Shared/PDA/SharedPdaSystem.cs
index 38a3899f7fdba..564c181f1c8e7 100644
--- a/Content.Shared/PDA/SharedPdaSystem.cs
+++ b/Content.Shared/PDA/SharedPdaSystem.cs
@@ -30,6 +30,11 @@ protected virtual void OnComponentInit(EntityUid uid, PdaComponent pda, Componen
             ItemSlotsSystem.AddItemSlot(uid, PdaComponent.PdaPenSlotId, pda.PenSlot);
             ItemSlotsSystem.AddItemSlot(uid, PdaComponent.PdaPaiSlotId, pda.PaiSlot);
 
+            if (pda.BankCard != null)
+                pda.BankCardSlot.StartingItem = pda.BankCard;
+
+            ItemSlotsSystem.AddItemSlot(uid, PdaComponent.PdaBankCardSlotId, pda.BankCardSlot); //imp edit for banking
+
             UpdatePdaAppearance(uid, pda);
         }
 
@@ -38,6 +43,7 @@ private void OnComponentRemove(EntityUid uid, PdaComponent pda, ComponentRemove
             ItemSlotsSystem.RemoveItemSlot(uid, pda.IdSlot);
             ItemSlotsSystem.RemoveItemSlot(uid, pda.PenSlot);
             ItemSlotsSystem.RemoveItemSlot(uid, pda.PaiSlot);
+            ItemSlotsSystem.RemoveItemSlot(uid, pda.BankCardSlot); //imp edit for banking
         }
 
         protected virtual void OnItemInserted(EntityUid uid, PdaComponent pda, EntInsertedIntoContainerMessage args)
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Components/BankCardComponent.cs b/Content.Shared/_Impstation/PersonalEconomy/Components/BankCardComponent.cs
new file mode 100644
index 0000000000000..90ed0a00aedfa
--- /dev/null
+++ b/Content.Shared/_Impstation/PersonalEconomy/Components/BankCardComponent.cs
@@ -0,0 +1,35 @@
+﻿using Robust.Shared.GameStates;
+
+namespace Content.Shared._Impstation.PersonalEconomy.Components;
+
+/// <summary>
+/// The component that indicates an entity is a bank card. only keeps track of the account it is connected to.
+/// </summary>
+[RegisterComponent, NetworkedComponent, AutoGenerateComponentState]
+public sealed partial class BankCardComponent : Component
+{
+
+    [DataField, AutoNetworkedField]
+    public int AccessNumber = 0; //todo this can probably just be a number that gets formatted actually?
+
+    [DataField, AutoNetworkedField]
+    public int TransferNumber = 0;
+
+    [DataField, AutoNetworkedField]
+    public string Name = "Unknown";
+
+    [DataField]
+    public LocId NamedLocId = "bank-card-name";
+
+    /// <summary>
+    /// Here as it's the easiest way to set a starting value per-job, like with IDs
+    /// </summary>
+    [DataField]
+    public int StartingBalance = 10000;
+
+    /// <summary>
+    /// Here as it's the easiest way to set a starting value per-job, like with IDs
+    /// </summary>
+    [DataField]
+    public int Salary = 500;
+}
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Components/RemoteBankServerComponent.cs b/Content.Shared/_Impstation/PersonalEconomy/Components/RemoteBankServerComponent.cs
new file mode 100644
index 0000000000000..edc23a966c6cb
--- /dev/null
+++ b/Content.Shared/_Impstation/PersonalEconomy/Components/RemoteBankServerComponent.cs
@@ -0,0 +1,47 @@
+﻿using Robust.Shared.GameStates;
+using Robust.Shared.Serialization;
+
+namespace Content.Shared._Impstation.PersonalEconomy.Components;
+
+/// <summary>
+/// This is used to keep track of what bank accounts exist and how much money they have
+/// </summary>
+[RegisterComponent, NetworkedComponent, AutoGenerateComponentState]
+public sealed partial class RemoteBankServerComponent : Component
+{
+    //for now I'm being lazy and just sending all the data to every client, someone yell at me later if it's a problem, I know where & how I can do the client-server split
+    /// <summary>
+    /// the one that stays on the server, the more detailed list of every acct's actions
+    /// </summary>
+    [DataField, AutoNetworkedField]
+    public Dictionary<int, BankAccount> AccountDict = [];
+
+    /// <summary>
+    /// Mapping of transfer numbers to actual account numbers
+    /// </summary>
+    [DataField, AutoNetworkedField]
+    public Dictionary<int, int> TransferNumberToAccountNumberDict = [];
+}
+
+[DataDefinition, Serializable, NetSerializable]
+public sealed partial class BankAccount
+{
+    [DataField] public string Name = string.Empty;
+
+    [DataField] public int AccessNumber = 0;
+    [DataField] public int TransferNumber = 0;
+
+    [DataField] public int Balance = 0;
+    [DataField] public int Salary = 0;
+
+    [DataField] public List<BankTransaction> Transactions = []; //todo make this only be the last like 10-15 transactions?
+}
+
+[DataDefinition, Serializable, NetSerializable]
+public sealed partial class BankTransaction
+{
+    [DataField] public bool Outgoing = false;
+    [DataField] public string Other = string.Empty;
+    [DataField] public int Amount = 0;
+    [DataField] public string Reason = string.Empty; //todo limit this to like 64 chars or so
+}
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs b/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
new file mode 100644
index 0000000000000..caa37cc36f7db
--- /dev/null
+++ b/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
@@ -0,0 +1,127 @@
+﻿using Content.Shared._Impstation.PersonalEconomy.Components;
+using Content.Shared.Administration;
+using Content.Shared.Examine;
+using Content.Shared.Roles;
+
+namespace Content.Shared._Impstation.PersonalEconomy.Systems;
+
+/// <summary>
+/// The main banking system; handles all funds transfers and keeps track of bank accounts etc etc
+/// </summary>
+public abstract class SharedBankingSystem : EntitySystem
+{
+    [Dependency] private readonly MetaDataSystem _metaData = null!;
+
+    /// <inheritdoc/>
+    public override void Initialize()
+    {
+        SubscribeLocalEvent<BankCardComponent, ExaminedEvent>(OnExamined);
+        SubscribeLocalEvent<BankCardComponent, ComponentInit>(OnComponentInit);
+    }
+
+    private void OnComponentInit(Entity<BankCardComponent> ent, ref ComponentInit args)
+    {
+        SetupID(ent);
+    }
+
+    protected virtual void SetupID(Entity<BankCardComponent> ent)
+    {
+
+    }
+
+    private void OnExamined(Entity<BankCardComponent> ent, ref ExaminedEvent args)
+    {
+        if (!args.IsInDetailsRange)
+            return;
+
+        args.PushMarkup(Loc.GetString("bank-card-examine-access-number", ("number", $"{ent.Comp.AccessNumber:000000}")),4);
+        args.PushMarkup(Loc.GetString("bank-card-examine-transfer-number", ("number", $"{ent.Comp.TransferNumber:0000}")),4);
+
+        args.PushMarkup("The two below are for testing!", 3);
+        args.PushMarkup(Loc.GetString("bank-card-examine-balance", ("balance", GetAccount(ent.Comp.AccessNumber)!.Balance)), 2); //todo remove this
+        args.PushMarkup(Loc.GetString("bank-card-examine-salary", ("salary", GetAccount(ent.Comp.AccessNumber)!.Salary)), 1); //todo remove this
+    }
+
+    /// <summary>
+    /// Create a new account. does nothing if run on the client.
+    /// </summary>
+    /// <returns>the Access number &amp; transfer number for the account</returns>
+    public virtual (int AccessNumber, int TransferNumber) CreateNewAccount(string name)
+    {
+        return (0, 0);
+    }
+
+    private BankAccount? GetAccount(int accessNumber)
+    {
+        var serverQuery = EntityQueryEnumerator<RemoteBankServerComponent>();
+        return serverQuery.MoveNext(out var uid, out var server) ? server.AccountDict.GetValueOrDefault(accessNumber) : null;
+    }
+
+    /// <summary>
+    /// Set the name for an account
+    /// </summary>
+    /// <param name="accessNumber"></param>
+    /// <param name="name"></param>
+    public virtual void SetAccountName(int accessNumber, string name)
+    {
+        var account = GetAccount(accessNumber);
+
+        if (account != null)
+            account.Name = name;
+    }
+
+    public virtual void SetAccountSalary(int accessNumber, int salary)
+    {
+        var account = GetAccount(accessNumber);
+
+        if (account != null)
+            account.Salary = salary;
+    }
+
+    public virtual void SetAccountBalance(int accessNumber, int balance)
+    {
+        var account = GetAccount(accessNumber);
+
+        if (account != null)
+            account.Balance = balance;
+    }
+
+    /// <summary>
+    /// Update the details on a bank card to reflect the details of a given account.
+    /// </summary>
+    /// <param name="card"></param>
+    /// <param name="accessNumber"></param>
+    public virtual void UpdateCardDetails(Entity<BankCardComponent> card, int accessNumber)
+    {
+        var account = GetAccount(accessNumber);
+
+        if (account is null)
+            return;
+
+        SetCardName(card, account.Name);
+        SetCardNumber(card, account.AccessNumber);
+    }
+
+    /// <summary>
+    /// Set the name on a card
+    /// </summary>
+    /// <param name="card"></param>
+    /// <param name="name"></param>
+    public virtual void SetCardName(Entity<BankCardComponent> card, string name)
+    {
+        card.Comp.Name = name;
+        _metaData.SetEntityName(card, Loc.GetString(card.Comp.NamedLocId, ("name", name)));
+        Dirty(card);
+    }
+
+    /// <summary>
+    /// set the number on a card
+    /// </summary>
+    /// <param name="card"></param>
+    /// <param name="accessNumber"></param>
+    public virtual void SetCardNumber(Entity<BankCardComponent> card, int accessNumber)
+    {
+        card.Comp.AccessNumber = accessNumber;
+        Dirty(card);
+    }
+}
diff --git a/Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl b/Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl
new file mode 100644
index 0000000000000..e875fa69f4a7d
--- /dev/null
+++ b/Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl
@@ -0,0 +1,6 @@
+﻿bank-card-name = {$name}'s bank card
+bank-card-slot-name = Bank Card
+bank-card-examine-access-number = The card's access number is {$number}
+bank-card-examine-transfer-number = The card's transfer number is {$number}
+bank-card-examine-balance = The account's balance is {$balance} spesitos
+bank-card-examine-salary = The account's salary is {$salary} spesitos
diff --git a/Resources/Prototypes/Entities/Objects/Devices/pda.yml b/Resources/Prototypes/Entities/Objects/Devices/pda.yml
index a560643b52afb..953258469fd2d 100644
--- a/Resources/Prototypes/Entities/Objects/Devices/pda.yml
+++ b/Resources/Prototypes/Entities/Objects/Devices/pda.yml
@@ -45,6 +45,15 @@
       whitelist:
         components:
         - IdCard
+    bankCardSlot: #imp edit
+      name: bank-card-slot-name
+      priority: -3
+      ejectSound: /Audio/Machines/id_swipe.ogg
+      insertSound: /Audio/Machines/id_insert.ogg
+      whitelist:
+        components:
+        - BankCard
+    bankCard: BankCard
   - type: PdaVisuals
   - type: Item
     size: Small
@@ -55,6 +64,7 @@
       PDA-pai: !type:ContainerSlot {}
       Cartridge-Slot: !type:ContainerSlot {}
       program-container: !type:Container
+      PDA-bank-card: !type:ContainerSlot {} #imp edit
   - type: ItemSlots
   - type: Clothing
     quickEquip: false
diff --git a/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml b/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml
new file mode 100644
index 0000000000000..b653aeaed3361
--- /dev/null
+++ b/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml
@@ -0,0 +1,27 @@
+﻿- type: entity
+  parent: Clothing
+  id: BankCard
+  name: bank card
+  description: The little square of plastic that controls your life. No, the other one.
+  components:
+  - type: Sprite
+    sprite: _Impstation/Objects/Misc/id_cards.rsi #imp edit
+    layers:
+    - state: default
+    - state: idpassenger
+  - type: Clothing
+    slots:
+    - idcard
+    sprite: _Impstation/Objects/Misc/id_cards.rsi #imp edit
+  - type: Item
+    size: Small
+    heldPrefix: default
+    storedRotation: -90
+  - type: BankCard
+
+- type: entity
+  id: RemoteBankServer
+  name: remote bank server
+  description: if you're seeing this, something has gone horribly wrong
+  components:
+  - type: RemoteBankServer

From 1723c632599959f5148ad40b95b3c101e0d43f10 Mon Sep 17 00:00:00 2001
From: ruddygreat <ruddygreat1@gmail.com>
Date: Wed, 12 Nov 2025 16:36:00 +0000
Subject: [PATCH 02/10] first basic UI. transactions next.

---
 .../UI/ATMBoundUserInterface.cs               | 41 ++++++++++
 .../PersonalEconomy/UI/ATMMenu.xaml           | 11 +++
 .../PersonalEconomy/UI/ATMMenu.xaml.cs        | 79 +++++++++++++++++++
 .../PersonalEconomy/UI/AccountInfoBox.xaml    | 20 +++++
 .../PersonalEconomy/UI/AccountInfoBox.xaml.cs | 43 ++++++++++
 .../PersonalEconomy/UI/Keypad.xaml            | 21 +++++
 .../PersonalEconomy/UI/Keypad.xaml.cs         | 68 ++++++++++++++++
 .../PersonalEconomy/UI/NoAccountInfoBox.xaml  | 11 +++
 .../UI/NoAccountInfoBox.xaml.cs               | 16 ++++
 .../PersonalEconomy/BankingUIKeys.cs          | 10 +++
 .../Systems/SharedBankingSystem.cs            | 44 +++++++----
 .../_Impstation/personal-economy/banking.ftl  | 15 +++-
 .../Entities/Objects/Misc/banking.yml         | 36 +++++++++
 13 files changed, 397 insertions(+), 18 deletions(-)
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/ATMBoundUserInterface.cs
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml.cs
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/NoAccountInfoBox.xaml
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/NoAccountInfoBox.xaml.cs
 create mode 100644 Content.Shared/_Impstation/PersonalEconomy/BankingUIKeys.cs

diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/ATMBoundUserInterface.cs b/Content.Client/_Impstation/PersonalEconomy/UI/ATMBoundUserInterface.cs
new file mode 100644
index 0000000000000..db8f0f0950b7e
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/ATMBoundUserInterface.cs
@@ -0,0 +1,41 @@
+﻿using Content.Shared._Impstation.PersonalEconomy.Systems;
+using Robust.Client.UserInterface;
+
+namespace Content.Client._Impstation.PersonalEconomy.UI;
+
+public sealed class ATMBoundUserInterface : BoundUserInterface
+{
+
+    private readonly ClientBankingSystem _banking;
+
+    private ATMMenu? _menu;
+
+    public ATMBoundUserInterface(EntityUid owner, Enum uiKey) : base(owner, uiKey)
+    {
+        _banking = EntMan.System<ClientBankingSystem>();
+    }
+
+    protected override void Open()
+    {
+        base.Open();
+
+        _menu = this.CreateWindow<ATMMenu>();
+        _menu.CreateInvalidInfoBox();
+
+        _menu.OnNumberEntered += s =>
+        {
+            if (!int.TryParse(s, out var i) || !_banking.TryGetAccount(i, out var account))
+            {
+                _menu.CreateInvalidInfoBox();
+                return;
+            }
+
+            _menu.CreateInfoBoxForAccount(account);
+        };
+
+        _menu.OnClearButtonPressed += () =>
+        {
+            _menu.CreateInvalidInfoBox();
+        };
+    }
+}
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml
new file mode 100644
index 0000000000000..a96b280d93a27
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml
@@ -0,0 +1,11 @@
+﻿<controls:FancyWindow xmlns="https://spacestation14.io"
+            xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
+            xmlns:ui="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI"
+            Title="{Loc 'atm-machine-window-title'}"
+            MinSize="512 256"
+            SetSize="512 256">
+    <BoxContainer Orientation="Horizontal">
+        <ui:Keypad Name="UIKeypad" NumberLength="6"/>
+        <BoxContainer Name="AccountInfoContainer" Orientation="Vertical" HorizontalExpand="True" VerticalExpand="True"/>
+    </BoxContainer>
+</controls:FancyWindow>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs
new file mode 100644
index 0000000000000..32680d1bf1758
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs
@@ -0,0 +1,79 @@
+﻿using Content.Client.UserInterface.Controls;
+using Content.Shared._Impstation.PersonalEconomy.Components;
+using Content.Shared.Hands.Components;
+using Content.Shared.Hands.EntitySystems;
+using Robust.Client.AutoGenerated;
+using Robust.Client.Player;
+using Robust.Client.UserInterface.XAML;
+using Robust.Shared.Network;
+using Robust.Shared.Timing;
+
+namespace Content.Client._Impstation.PersonalEconomy.UI;
+
+[GenerateTypedNameReferences]
+public sealed partial class ATMMenu : FancyWindow
+{
+    [Dependency] private readonly IPlayerManager _playerMan = null!;
+    [Dependency] private readonly IEntityManager _entityManager = null!;
+
+    public Action<string>? OnNumberEntered;
+    public Action? OnClearButtonPressed;
+
+    private bool _accountOpen;
+
+    public ATMMenu()
+    {
+        RobustXamlLoader.Load(this);
+        IoCManager.InjectDependencies(this);
+
+        UIKeypad.OnEnterButtonPressed += s => OnNumberEntered?.Invoke(s);
+        UIKeypad.OnClearButtonPressed += () => OnClearButtonPressed?.Invoke();
+    }
+
+    protected override void FrameUpdate(FrameEventArgs args)
+    {
+        //if we've already got an open account, we are the server or the player entity does not have hands, return
+        if (_accountOpen || _playerMan.LocalEntity is not { } realEnt || !_entityManager.HasComponent<HandsComponent>(realEnt))
+        {
+            return;
+        }
+
+        //go over everything that's held by the player
+        var handSystem = _entityManager.System<SharedHandsSystem>();
+        var held = handSystem.EnumerateHeld(realEnt);
+        foreach (var ent in held)
+        {
+            //if it's not a bank card, continue
+            if (!_entityManager.TryGetComponent<BankCardComponent>(ent, out var comp))
+                continue;
+
+            //set the entered number & invoke the number entered action
+            UIKeypad.SetEnteredNumber($"{comp.AccessNumber:000000}");
+            OnNumberEntered?.Invoke($"{comp.AccessNumber:000000}");
+            break;
+        }
+    }
+
+    public void CreateInvalidInfoBox()
+    {
+        ClearInfoBox();
+
+        var infoBox = new NoAccountInfoBox();
+        AccountInfoContainer.AddChild(infoBox);
+    }
+
+    public void CreateInfoBoxForAccount(BankAccount account)
+    {
+        ClearInfoBox();
+
+        var infobox = new AccountInfoBox(account);
+        AccountInfoContainer.AddChild(infobox);
+        _accountOpen = true;
+    }
+
+    public void ClearInfoBox()
+    {
+        AccountInfoContainer.RemoveAllChildren();
+        _accountOpen = false;
+    }
+}
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml
new file mode 100644
index 0000000000000..7c51d57d9605f
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml
@@ -0,0 +1,20 @@
+﻿<BoxContainer
+    xmlns="https://spacestation14.io"
+    Orientation="Vertical"
+    VerticalExpand="true">
+    <!--Account info-->
+    <BoxContainer Name="AccountInfoContainer" Orientation="Vertical">
+        <Label Name="AccNameLabel" VerticalAlignment="Center" Access="Public"/>
+        <Label Name="AccAccessNumberLabel" VerticalAlignment="Center" Access="Public"/>
+        <Label Name="AccTransferNumberLabel" VerticalAlignment="Center" Access="Public"/>
+        <Label Name="AccBalanceLabel" VerticalAlignment="Center" Access="Public"/>
+    </BoxContainer>
+    <!--Transaction info todo border thing here-->
+    <BoxContainer Name="TransactionInfoContainer" Orientation="Vertical" VerticalExpand="True" HorizontalExpand="True">
+        <BoxContainer Orientation="Horizontal" HorizontalExpand="True">
+            <Label Text="{Loc transactions-container-title}"/>
+            <Control HorizontalExpand="True"/>
+            <Button Name="TransferFundsButton" Text="{Loc transfer-funds-button-title}" HorizontalAlignment="Right"/>
+        </BoxContainer>
+    </BoxContainer>
+</BoxContainer>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs
new file mode 100644
index 0000000000000..77f0dfc4ee051
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs
@@ -0,0 +1,43 @@
+﻿using Content.Shared._Impstation.PersonalEconomy.Components;
+using Robust.Client.AutoGenerated;
+using Robust.Client.UserInterface;
+using Robust.Client.UserInterface.Controls;
+using Robust.Client.UserInterface.XAML;
+
+namespace Content.Client._Impstation.PersonalEconomy.UI;
+
+[GenerateTypedNameReferences]
+public sealed partial class AccountInfoBox : BoxContainer
+{
+    public AccountInfoBox()
+    {
+        RobustXamlLoader.Load(this);
+    }
+
+    public AccountInfoBox(BankAccount account)
+    {
+        RobustXamlLoader.Load(this);
+
+        AccNameLabel.Text = Loc.GetString("atm-machine-account-name-title", ("name", account.Name));
+        AccAccessNumberLabel.Text = Loc.GetString("atm-machine-account-access-number-title", ("number", $"{account.AccessNumber:000000}"));
+        AccTransferNumberLabel.Text = Loc.GetString("atm-machine-account-transfer-number-title", ("number", $"{account.TransferNumber:0000}"));
+        AccBalanceLabel.Text = Loc.GetString("atm-machine-account-balance-title", ("balance", $"{account.Balance:n0}"));
+
+        if (account.Transactions.Count == 0)
+        {
+            var noTransactionsLabel = new Label();
+            noTransactionsLabel.Text = Loc.GetString("atm-machine-no-transactions");
+            noTransactionsLabel.VerticalExpand = true;
+            noTransactionsLabel.HorizontalExpand = true;
+            noTransactionsLabel.HorizontalAlignment = HAlignment.Center;
+            noTransactionsLabel.VerticalAlignment = VAlignment.Center;
+            noTransactionsLabel.Align = Label.AlignMode.Center;
+            TransactionInfoContainer.AddChild(noTransactionsLabel);
+        }
+        else
+        {
+            //actually fill out transaction data here
+        }
+    }
+}
+
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml
new file mode 100644
index 0000000000000..ab5d6ac4c9825
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml
@@ -0,0 +1,21 @@
+﻿<controls:Keypad
+    xmlns="https://spacestation14.io"
+    xmlns:controls="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI">
+    <BoxContainer Orientation="Vertical" HorizontalExpand="False" HorizontalAlignment="Left">
+        <Label Name="NumberDisplay" Align="Center" MinHeight="24"/> <!--todo make this a text entry field instead?-->
+        <GridContainer Name="ButtonGrid" Columns="3" VSeparationOverride="0" HSeparationOverride="0">
+            <Button Name="OneButton" Text="1" StyleClasses="OpenBoth" Margin="0"/>
+            <Button Name="TwoButton" Text="2" StyleClasses="OpenBoth" Margin="0"/>
+            <Button Name="ThreeButton" Text="3" StyleClasses="OpenLeft" Margin="0"/>
+            <Button Name="FourButton" Text="4" StyleClasses="OpenBoth" Margin="0"/>
+            <Button Name="FiveButton" Text="5" StyleClasses="OpenBoth" Margin="0"/>
+            <Button Name="SixButton" Text="6" StyleClasses="OpenBoth" Margin="0"/>
+            <Button Name="SevenButton" Text="7" StyleClasses="OpenBoth" Margin="0"/>
+            <Button Name="EightButton" Text="8" StyleClasses="OpenBoth" Margin="0"/>
+            <Button Name="NineButton" Text="9" StyleClasses="OpenBoth" Margin="0"/>
+            <Button Name="EnterButton" Text="ENT" StyleClasses="OpenRight" Margin="0"/>
+            <Button Name="ZeroButton" Text="0" StyleClasses="OpenBoth" Margin="0"/>
+            <Button Name="ClearButton" Text="CLR" StyleClasses="OpenBoth" Margin="0"/>
+        </GridContainer>
+    </BoxContainer>
+</controls:Keypad>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml.cs
new file mode 100644
index 0000000000000..1f5bc6b7ce91c
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml.cs
@@ -0,0 +1,68 @@
+﻿using Robust.Client.AutoGenerated;
+using Robust.Client.UserInterface;
+using Robust.Client.UserInterface.XAML;
+
+namespace Content.Client._Impstation.PersonalEconomy.UI;
+
+[GenerateTypedNameReferences]
+public sealed partial class Keypad : Control
+{
+    public int NumberLength { get; set; } = 6;
+
+    private string _enteredNumber = ""; //todo this can probably be an int can't it
+
+    public Action<string>? OnEnterButtonPressed;
+    public Action? OnClearButtonPressed;
+
+    public Keypad()
+    {
+        RobustXamlLoader.Load(this);
+
+        SetupGrid();
+    }
+
+    public void SetEnteredNumber(string number)
+    {
+        _enteredNumber = number;
+        NumberDisplay.Text = _enteredNumber;
+    }
+
+    private void NumberButtonPressed(int i)
+    {
+        if (_enteredNumber.Length >= NumberLength)
+            return; //clear the number manually if we're at the limit
+
+        _enteredNumber += i;
+        NumberDisplay.Text = _enteredNumber;
+    }
+
+    private void ClearButtonPressed()
+    {
+        _enteredNumber = "";
+        NumberDisplay.Text = _enteredNumber;
+        OnClearButtonPressed?.Invoke();
+    }
+
+    private void EnterButtonPressed()
+    {
+        OnEnterButtonPressed?.Invoke(_enteredNumber);
+    }
+
+    private void SetupGrid()
+    {
+        OneButton.OnPressed += _ => NumberButtonPressed(1);
+        TwoButton.OnPressed += _ => NumberButtonPressed(2);
+        ThreeButton.OnPressed += _ => NumberButtonPressed(3);
+        FourButton.OnPressed += _ => NumberButtonPressed(4);
+        FiveButton.OnPressed += _ => NumberButtonPressed(5);
+        SixButton.OnPressed += _ => NumberButtonPressed(6);
+        SevenButton.OnPressed += _ => NumberButtonPressed(7);
+        EightButton.OnPressed += _ => NumberButtonPressed(8);
+        NineButton.OnPressed += _ => NumberButtonPressed(9);
+        ZeroButton.OnPressed += _ => NumberButtonPressed(0);
+
+        ClearButton.OnPressed += _ => ClearButtonPressed();
+        EnterButton.OnPressed += _ => EnterButtonPressed();
+    }
+}
+
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/NoAccountInfoBox.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/NoAccountInfoBox.xaml
new file mode 100644
index 0000000000000..76cf6555a7ff0
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/NoAccountInfoBox.xaml
@@ -0,0 +1,11 @@
+﻿<BoxContainer
+    xmlns="https://spacestation14.io"
+    Orientation="Vertical"
+    VerticalExpand="true">
+    <RichTextLabel Name="InvalidAccNumberBox"
+                   Text="{Loc atm-invalid-account-number}"
+                   HorizontalAlignment="Center"
+                   VerticalAlignment="Center"
+                   HorizontalExpand="True"
+                   VerticalExpand="True"/>
+</BoxContainer>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/NoAccountInfoBox.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/NoAccountInfoBox.xaml.cs
new file mode 100644
index 0000000000000..10fb38d9033eb
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/NoAccountInfoBox.xaml.cs
@@ -0,0 +1,16 @@
+﻿using Robust.Client.AutoGenerated;
+using Robust.Client.UserInterface;
+using Robust.Client.UserInterface.Controls;
+using Robust.Client.UserInterface.XAML;
+
+namespace Content.Client._Impstation.PersonalEconomy.UI;
+
+[GenerateTypedNameReferences]
+public sealed partial class NoAccountInfoBox : BoxContainer
+{
+    public NoAccountInfoBox()
+    {
+        RobustXamlLoader.Load(this);
+    }
+}
+
diff --git a/Content.Shared/_Impstation/PersonalEconomy/BankingUIKeys.cs b/Content.Shared/_Impstation/PersonalEconomy/BankingUIKeys.cs
new file mode 100644
index 0000000000000..6c56c4c8dade5
--- /dev/null
+++ b/Content.Shared/_Impstation/PersonalEconomy/BankingUIKeys.cs
@@ -0,0 +1,10 @@
+﻿using Robust.Shared.Serialization;
+
+namespace Content.Shared._Impstation.PersonalEconomy;
+
+[Serializable, NetSerializable]
+public enum ATMUIKey : byte
+{
+    Key
+}
+
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs b/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
index caa37cc36f7db..cae30dec6aef9 100644
--- a/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
+++ b/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
@@ -1,4 +1,5 @@
-﻿using Content.Shared._Impstation.PersonalEconomy.Components;
+﻿using System.Diagnostics.CodeAnalysis;
+using Content.Shared._Impstation.PersonalEconomy.Components;
 using Content.Shared.Administration;
 using Content.Shared.Examine;
 using Content.Shared.Roles;
@@ -37,9 +38,12 @@ private void OnExamined(Entity<BankCardComponent> ent, ref ExaminedEvent args)
         args.PushMarkup(Loc.GetString("bank-card-examine-access-number", ("number", $"{ent.Comp.AccessNumber:000000}")),4);
         args.PushMarkup(Loc.GetString("bank-card-examine-transfer-number", ("number", $"{ent.Comp.TransferNumber:0000}")),4);
 
+        if (!TryGetAccount(ent.Comp.AccessNumber, out var account))
+            return;
+
         args.PushMarkup("The two below are for testing!", 3);
-        args.PushMarkup(Loc.GetString("bank-card-examine-balance", ("balance", GetAccount(ent.Comp.AccessNumber)!.Balance)), 2); //todo remove this
-        args.PushMarkup(Loc.GetString("bank-card-examine-salary", ("salary", GetAccount(ent.Comp.AccessNumber)!.Salary)), 1); //todo remove this
+        args.PushMarkup(Loc.GetString("bank-card-examine-balance", ("balance", account.Balance)), 2); //todo remove this
+        args.PushMarkup(Loc.GetString("bank-card-examine-salary", ("salary", account.Salary)), 1); //todo remove this
     }
 
     /// <summary>
@@ -51,10 +55,18 @@ public virtual (int AccessNumber, int TransferNumber) CreateNewAccount(string na
         return (0, 0);
     }
 
-    private BankAccount? GetAccount(int accessNumber)
+    public bool TryGetAccount(int accessNumber, [NotNullWhen(true)] out BankAccount? account)
     {
+        account = null;
+
         var serverQuery = EntityQueryEnumerator<RemoteBankServerComponent>();
-        return serverQuery.MoveNext(out var uid, out var server) ? server.AccountDict.GetValueOrDefault(accessNumber) : null;
+        if (!serverQuery.MoveNext(out var uid, out var server) ||
+            !server.AccountDict.TryGetValue(accessNumber, out var bankAcc))
+            return false;
+
+        account = bankAcc;
+        return true;
+
     }
 
     /// <summary>
@@ -64,26 +76,26 @@ public virtual (int AccessNumber, int TransferNumber) CreateNewAccount(string na
     /// <param name="name"></param>
     public virtual void SetAccountName(int accessNumber, string name)
     {
-        var account = GetAccount(accessNumber);
+        if (!TryGetAccount(accessNumber, out var account))
+            return;
 
-        if (account != null)
-            account.Name = name;
+        account.Name = name;
     }
 
     public virtual void SetAccountSalary(int accessNumber, int salary)
     {
-        var account = GetAccount(accessNumber);
+        if (!TryGetAccount(accessNumber, out var account))
+            return;
 
-        if (account != null)
-            account.Salary = salary;
+        account.Salary = salary;
     }
 
     public virtual void SetAccountBalance(int accessNumber, int balance)
     {
-        var account = GetAccount(accessNumber);
+        if (!TryGetAccount(accessNumber, out var account))
+            return;
 
-        if (account != null)
-            account.Balance = balance;
+        account.Balance = balance;
     }
 
     /// <summary>
@@ -93,9 +105,7 @@ public virtual void SetAccountBalance(int accessNumber, int balance)
     /// <param name="accessNumber"></param>
     public virtual void UpdateCardDetails(Entity<BankCardComponent> card, int accessNumber)
     {
-        var account = GetAccount(accessNumber);
-
-        if (account is null)
+        if (!TryGetAccount(accessNumber, out var account))
             return;
 
         SetCardName(card, account.Name);
diff --git a/Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl b/Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl
index e875fa69f4a7d..4debf5c6009cc 100644
--- a/Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl
+++ b/Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl
@@ -1,6 +1,19 @@
-﻿bank-card-name = {$name}'s bank card
+﻿#bank card stuff
+bank-card-name = {$name}'s bank card
 bank-card-slot-name = Bank Card
 bank-card-examine-access-number = The card's access number is {$number}
 bank-card-examine-transfer-number = The card's transfer number is {$number}
 bank-card-examine-balance = The account's balance is {$balance} spesitos
 bank-card-examine-salary = The account's salary is {$salary} spesitos
+
+#atm stuff
+atm-machine-window-title = Automated Teller Machine Machine
+atm-machine-account-name-title = Welcome, {$name}!
+atm-machine-account-access-number-title = Access Number: {$number}
+atm-machine-account-transfer-number-title = Transfer Number: {$number}
+atm-machine-account-balance-title = Balance: ${$balance}
+atm-machine-no-transactions = This Account Has Made No Transactions
+atm-invalid-account-number = Please enter a valid account number or present a valid bank card
+
+transactions-container-title = Transactions
+transfer-funds-button-title = Transfer Funds
diff --git a/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml b/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml
index b653aeaed3361..a25d0a2724f99 100644
--- a/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml
+++ b/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml
@@ -25,3 +25,39 @@
   description: if you're seeing this, something has gone horribly wrong
   components:
   - type: RemoteBankServer
+
+- type: entity
+  parent: BaseComputerAiAccess
+  id: ATMMachine
+  name: ATM Machine
+  description: It's called that on purpose now
+  components:
+  - type: Sprite
+    layers:
+    - map: ["computerLayerBody"]
+      state: computer
+      sprite: _Impstation/Structures/Machines/computers.rsi # imp
+    - map: ["computerLayerKeyboard"]
+      state: generic_keyboard
+      sprite: _Impstation/Structures/Machines/computers.rsi # imp
+    - map: ["computerLayerScreen"]
+      state: solar_screen
+    - map: ["computerLayerKeys"]
+      state: generic_keys
+    - map: [ "enum.WiresVisualLayers.MaintenancePanel" ]
+      state: generic_panel_open
+      sprite: _Impstation/Structures/Machines/computers.rsi # imp
+  - type: ActivatableUI
+    key: enum.ATMUIKey.Key
+  - type: UserInterface
+    interfaces:
+      enum.ATMUIKey.Key:
+        type: ATMBoundUserInterface
+      enum.WiresUiKey.Key:
+        type: WiresBoundUserInterface
+  - type: Computer
+    board: RadarConsoleCircuitboard
+  - type: PointLight
+    radius: 2.4 #imp
+    energy: 1.6
+    color: "#e6e227"

From 36af614e639a8a1f4b4ca309c4b7bb73cae8208b Mon Sep 17 00:00:00 2001
From: ruddygreat <ruddygreat1@gmail.com>
Date: Wed, 12 Nov 2025 20:47:48 +0000
Subject: [PATCH 03/10] I lied. center the keypad vertically

---
 Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml | 6 +++++-
 1 file changed, 5 insertions(+), 1 deletion(-)

diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml
index a96b280d93a27..32c6d33f63016 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml
@@ -5,7 +5,11 @@
             MinSize="512 256"
             SetSize="512 256">
     <BoxContainer Orientation="Horizontal">
-        <ui:Keypad Name="UIKeypad" NumberLength="6"/>
+        <BoxContainer Orientation="Vertical">
+            <Control VerticalExpand="True"/>
+            <ui:Keypad Name="UIKeypad" NumberLength="6"/>
+            <Control VerticalExpand="True"/>
+        </BoxContainer>
         <BoxContainer Name="AccountInfoContainer" Orientation="Vertical" HorizontalExpand="True" VerticalExpand="True"/>
     </BoxContainer>
 </controls:FancyWindow>

From b4e8420eb1919de6a9b4bee4399d772d806160e8 Mon Sep 17 00:00:00 2001
From: ruddygreat <ruddygreat1@gmail.com>
Date: Fri, 14 Nov 2025 20:16:45 +0000
Subject: [PATCH 04/10] transaction UI & a few small UI improvements. real
 transactions tomorrow.

---
 .../UI/ATMBoundUserInterface.cs               | 98 ++++++++++++++++++-
 .../PersonalEconomy/UI/ATMMenu.xaml           |  4 +-
 .../PersonalEconomy/UI/ATMMenu.xaml.cs        |  8 ++
 .../PersonalEconomy/UI/AccountInfoBox.xaml    |  2 +-
 .../PersonalEconomy/UI/AccountInfoBox.xaml.cs |  5 +
 .../PersonalEconomy/UI/Keypad.xaml            |  4 +-
 .../PersonalEconomy/UI/Keypad.xaml.cs         | 23 ++++-
 .../PersonalEconomy/UI/TransactionWindow.xaml | 30 ++++++
 .../UI/TransactionWindow.xaml.cs              | 63 ++++++++++++
 .../Systems/SharedBankingSystem.cs            | 15 ++-
 .../_Impstation/personal-economy/banking.ftl  | 12 +++
 11 files changed, 253 insertions(+), 11 deletions(-)
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/TransactionWindow.xaml
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/TransactionWindow.xaml.cs

diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/ATMBoundUserInterface.cs b/Content.Client/_Impstation/PersonalEconomy/UI/ATMBoundUserInterface.cs
index db8f0f0950b7e..5a72fb197fcde 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/ATMBoundUserInterface.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/ATMBoundUserInterface.cs
@@ -1,4 +1,5 @@
-﻿using Content.Shared._Impstation.PersonalEconomy.Systems;
+﻿using System.Numerics;
+using Content.Shared._Impstation.PersonalEconomy.Components;
 using Robust.Client.UserInterface;
 
 namespace Content.Client._Impstation.PersonalEconomy.UI;
@@ -9,6 +10,8 @@ public sealed class ATMBoundUserInterface : BoundUserInterface
     private readonly ClientBankingSystem _banking;
 
     private ATMMenu? _menu;
+    private TransactionWindow? _transactionWindow;
+    private BankAccount? _account;
 
     public ATMBoundUserInterface(EntityUid owner, Enum uiKey) : base(owner, uiKey)
     {
@@ -21,21 +24,108 @@ protected override void Open()
 
         _menu = this.CreateWindow<ATMMenu>();
         _menu.CreateInvalidInfoBox();
+        _menu.OnClose += ClearMenu;
 
         _menu.OnNumberEntered += s =>
         {
             if (!int.TryParse(s, out var i) || !_banking.TryGetAccount(i, out var account))
             {
-                _menu.CreateInvalidInfoBox();
+                ClearMenu();
                 return;
             }
 
             _menu.CreateInfoBoxForAccount(account);
+            _account = account;
         };
 
-        _menu.OnClearButtonPressed += () =>
+        _menu.OnClearButtonPressed += ClearMenu;
+
+        _transactionWindow = new TransactionWindow();
+
+        _menu.OnTransactionButtonPressed += () =>
         {
-            _menu.CreateInvalidInfoBox();
+            if (_transactionWindow.IsOpen)
+            {
+                _transactionWindow.Close();
+                return;
+            }
+
+            _transactionWindow.TransferNumberBox.Clear();
+            _transactionWindow.TransferAmountBox.Clear();
+            _transactionWindow.TransferReasonBox.Clear();
+
+            _transactionWindow.Open(_menu.Position + new Vector2(_menu.Width, 0)); //todo make this part of the window and not a separate thing that just gets plonked next to it
         };
+
+        _transactionWindow.TransactionConfirmAttempt += () =>
+        {
+            //todo make this a method yada yada
+            if (!VerifyTransaction(_transactionWindow.TransferNumberBox.Text, _transactionWindow.TransferAmount))
+            {
+                _transactionWindow.ReallyConfirmButton.Disabled = true;
+                return;
+            }
+
+            _transactionWindow.ReallyConfirmButton.Disabled = false;
+        };
+
+        _transactionWindow.TransactionConfirmed += () =>
+        {
+            //re-verify for safety
+            if (!VerifyTransaction(_transactionWindow.TransferNumberBox.Text, _transactionWindow.TransferAmount))
+            {
+                _transactionWindow.ReallyConfirmButton.Disabled = true;
+                return;
+            }
+
+            _transactionWindow.ReallyConfirmButton.Disabled = false;
+
+            //todo do this
+            /*
+            SendPredictedMessage();
+            */
+        };
+    }
+
+    protected override void Dispose(bool disposing)
+    {
+        base.Dispose(disposing);
+
+        if (!disposing)
+            return;
+
+        _menu?.Dispose();
+        _transactionWindow?.Dispose();
+    }
+
+    private void ClearMenu()
+    {
+        _menu?.CreateInvalidInfoBox();
+        _account = null;
+        _transactionWindow?.Close();
+    }
+
+    //todo move this into banking system
+    private bool VerifyTransaction(string recipient, int amount)
+    {
+        var verified = true;
+        //does the recipient exist?
+        if (recipient.Length != 4 || //is it a valid transfer number at all
+            !int.TryParse(recipient, out var number) ||
+            !_banking.TryGetAccountFromTransferNumber(number, out _) //does the recipient exist?
+           )
+        {
+            _transactionWindow!.TransactionRecipientDoesNotExistLabel.Visible = true;
+            verified = false;
+        }
+
+        //do we have enough money to make the transfer?
+        if (_account!.Balance < amount)
+        {
+            _transactionWindow!.TransactionNotEnoughFundsLabel.Visible = true;
+            verified = false;
+        }
+
+        return verified;
     }
 }
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml
index 32c6d33f63016..941c75f2bf9f6 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml
@@ -2,8 +2,8 @@
             xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
             xmlns:ui="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI"
             Title="{Loc 'atm-machine-window-title'}"
-            MinSize="512 256"
-            SetSize="512 256">
+            MinSize="512 320"
+            SetSize="512 320">
     <BoxContainer Orientation="Horizontal">
         <BoxContainer Orientation="Vertical">
             <Control VerticalExpand="True"/>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs
index 32680d1bf1758..1afeb406f2d93 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs
@@ -18,6 +18,7 @@ public sealed partial class ATMMenu : FancyWindow
 
     public Action<string>? OnNumberEntered;
     public Action? OnClearButtonPressed;
+    public Action? OnTransactionButtonPressed;
 
     private bool _accountOpen;
 
@@ -31,6 +32,11 @@ public ATMMenu()
     }
 
     protected override void FrameUpdate(FrameEventArgs args)
+    {
+        CheckForCard();
+    }
+
+    private void CheckForCard()
     {
         //if we've already got an open account, we are the server or the player entity does not have hands, return
         if (_accountOpen || _playerMan.LocalEntity is not { } realEnt || !_entityManager.HasComponent<HandsComponent>(realEnt))
@@ -69,6 +75,8 @@ public void CreateInfoBoxForAccount(BankAccount account)
         var infobox = new AccountInfoBox(account);
         AccountInfoContainer.AddChild(infobox);
         _accountOpen = true;
+
+        infobox.OnTransactionButtonPressed += () => OnTransactionButtonPressed?.Invoke();
     }
 
     public void ClearInfoBox()
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml
index 7c51d57d9605f..93be780299fa7 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml
@@ -14,7 +14,7 @@
         <BoxContainer Orientation="Horizontal" HorizontalExpand="True">
             <Label Text="{Loc transactions-container-title}"/>
             <Control HorizontalExpand="True"/>
-            <Button Name="TransferFundsButton" Text="{Loc transfer-funds-button-title}" HorizontalAlignment="Right"/>
+            <Button Name="TransferFundsButton" Text="{Loc transfer-funds-button-title}" HorizontalAlignment="Right" Access="Public"/>
         </BoxContainer>
     </BoxContainer>
 </BoxContainer>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs
index 77f0dfc4ee051..3ae598df211a1 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs
@@ -9,6 +9,9 @@ namespace Content.Client._Impstation.PersonalEconomy.UI;
 [GenerateTypedNameReferences]
 public sealed partial class AccountInfoBox : BoxContainer
 {
+
+    public Action? OnTransactionButtonPressed;
+
     public AccountInfoBox()
     {
         RobustXamlLoader.Load(this);
@@ -18,6 +21,8 @@ public AccountInfoBox(BankAccount account)
     {
         RobustXamlLoader.Load(this);
 
+        TransferFundsButton.OnPressed += _ => OnTransactionButtonPressed?.Invoke();
+
         AccNameLabel.Text = Loc.GetString("atm-machine-account-name-title", ("name", account.Name));
         AccAccessNumberLabel.Text = Loc.GetString("atm-machine-account-access-number-title", ("number", $"{account.AccessNumber:000000}"));
         AccTransferNumberLabel.Text = Loc.GetString("atm-machine-account-transfer-number-title", ("number", $"{account.TransferNumber:0000}"));
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml
index ab5d6ac4c9825..5cddc692c64ec 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml
@@ -2,7 +2,7 @@
     xmlns="https://spacestation14.io"
     xmlns:controls="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI">
     <BoxContainer Orientation="Vertical" HorizontalExpand="False" HorizontalAlignment="Left">
-        <Label Name="NumberDisplay" Align="Center" MinHeight="24"/> <!--todo make this a text entry field instead?-->
+        <LineEdit Name="NumberDisplay" HorizontalExpand="True" PlaceHolder="000000"/>
         <GridContainer Name="ButtonGrid" Columns="3" VSeparationOverride="0" HSeparationOverride="0">
             <Button Name="OneButton" Text="1" StyleClasses="OpenBoth" Margin="0"/>
             <Button Name="TwoButton" Text="2" StyleClasses="OpenBoth" Margin="0"/>
@@ -15,7 +15,7 @@
             <Button Name="NineButton" Text="9" StyleClasses="OpenBoth" Margin="0"/>
             <Button Name="EnterButton" Text="ENT" StyleClasses="OpenRight" Margin="0"/>
             <Button Name="ZeroButton" Text="0" StyleClasses="OpenBoth" Margin="0"/>
-            <Button Name="ClearButton" Text="CLR" StyleClasses="OpenBoth" Margin="0"/>
+            <Button Name="ClearButton" Text="CLR" StyleClasses="OpenBoth" Margin="0"/> <!--todo replace clr with a del button?-->
         </GridContainer>
     </BoxContainer>
 </controls:Keypad>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml.cs
index 1f5bc6b7ce91c..61e284aca9f11 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml.cs
@@ -1,4 +1,5 @@
-﻿using Robust.Client.AutoGenerated;
+﻿using System.Linq;
+using Robust.Client.AutoGenerated;
 using Robust.Client.UserInterface;
 using Robust.Client.UserInterface.XAML;
 
@@ -19,6 +20,26 @@ public Keypad()
         RobustXamlLoader.Load(this);
 
         SetupGrid();
+
+        NumberDisplay.OnTextChanged += args =>
+        {
+            var numberStr = string.Concat(args.Text.Where(char.IsDigit));
+            if (numberStr.Length > NumberLength)
+                numberStr = numberStr[..NumberLength];
+
+            NumberDisplay.Text = numberStr;
+            _enteredNumber = numberStr;
+        };
+
+        NumberDisplay.OnTextEntered += _ =>
+        {
+            EnterButtonPressed();
+        };
+
+        NumberDisplay.OnTextRemoved += _ =>
+        {
+            ClearButtonPressed();
+        };
     }
 
     public void SetEnteredNumber(string number)
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionWindow.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionWindow.xaml
new file mode 100644
index 0000000000000..481628f663ebd
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionWindow.xaml
@@ -0,0 +1,30 @@
+﻿<controls:FancyWindow
+    xmlns="https://spacestation14.io"
+    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
+    MinSize="320 320"
+    SetSize="320 320"
+    Title="{Loc transaction-window-title}">
+    <BoxContainer Orientation="Vertical" VerticalExpand="True">
+        <BoxContainer Orientation="Vertical">
+            <Label Text="{Loc atm-recipient-transfer-number}"/>
+            <LineEdit Name="TransferNumberBox" Access="Public" HorizontalExpand="True" PlaceHolder="0000"/>
+        </BoxContainer>
+        <BoxContainer Orientation="Vertical">
+            <Label Text="{Loc atm-transfer-amount}"/>
+            <LineEdit Name="TransferAmountBox" Access="Public" HorizontalExpand="True" PlaceHolder="000,000,000"/>
+        </BoxContainer>
+        <BoxContainer Orientation="Vertical">
+            <Label Text="{Loc atm-transfer-reason}"/>
+            <LineEdit Name="TransferReasonBox" Access="Public" HorizontalExpand="True" VerticalExpand="True" PlaceHolder="yada yada"/>
+            <Label Name="CharsRemainingLabel"/>
+        </BoxContainer>
+        <Control VerticalExpand="True"/>
+        <Label Name="TransactionRecipientDoesNotExistLabel" Visible="False" Text="{Loc transaction-no-recipient}" Access="Public"/>
+        <Label Name="TransactionNotEnoughFundsLabel" Visible="False" Text="{Loc transaction-low-funds}" Access="Public"/>
+        <BoxContainer Orientation="Horizontal">
+            <Button Name="CancelButton" Text="{Loc atm-cancel-button-label}" StyleClasses="OpenRight" Access="Public" HorizontalExpand="True"/>
+            <Button Name="ConfirmButton" Text="{Loc atm-confirm-button-label}" StyleClasses="OpenBoth" Access="Public" HorizontalExpand="True"/>
+            <Button Name="ReallyConfirmButton" Text="{Loc atm-really-confirm-label}" StyleClasses="OpenLeft" Disabled="True" Access="Public" HorizontalExpand="True"/>
+        </BoxContainer>
+    </BoxContainer>
+</controls:FancyWindow>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionWindow.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionWindow.xaml.cs
new file mode 100644
index 0000000000000..6f48596e61628
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionWindow.xaml.cs
@@ -0,0 +1,63 @@
+﻿using System.Linq;
+using Content.Client.UserInterface.Controls;
+using Robust.Client.AutoGenerated;
+using Robust.Client.UserInterface.XAML;
+
+namespace Content.Client._Impstation.PersonalEconomy.UI;
+
+[GenerateTypedNameReferences]
+public sealed partial class TransactionWindow : FancyWindow
+{
+
+    public Action? TransactionConfirmAttempt;
+    public Action? TransactionConfirmed;
+
+    public int TransferAmount;
+
+    public TransactionWindow()
+    {
+        RobustXamlLoader.Load(this);
+
+        ConfirmButton.OnPressed += _ => TransactionConfirmAttempt?.Invoke();
+
+        ReallyConfirmButton.OnPressed += _ => TransactionConfirmed?.Invoke();
+
+        CancelButton.OnPressed += _ => Close();
+
+        TransferNumberBox.OnTextChanged += args =>
+        {
+            var numberStr = string.Concat(args.Text.Where(char.IsDigit));
+            if (numberStr.Length > 4)
+                numberStr = numberStr[..4];
+
+            TransferNumberBox.Text = numberStr;
+        };
+
+        TransferAmountBox.OnTextChanged += args =>
+        {
+            var amountStr = string.Concat(args.Text.Where(char.IsDigit));
+            if (amountStr.Length > 9)
+                amountStr = amountStr[..9];
+
+            if (int.TryParse(amountStr, out var amount))
+            {
+                TransferAmount = amount;
+            }
+
+            TransferAmountBox.Text = $"{TransferAmount:n0}";
+        };
+
+        CharsRemainingLabel.Text = Loc.GetString("atm-transfer-reason-charcount", ("count", 64));
+
+        TransferReasonBox.OnTextChanged += args =>
+        {
+            var len = args.Text.Length;
+            if (len > 64)
+            {
+                TransferReasonBox.Text = args.Text[..64];
+            }
+
+            CharsRemainingLabel.Text = Loc.GetString("atm-transfer-reason-charcount", ("count", 64 - len));
+        };
+    }
+}
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs b/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
index cae30dec6aef9..d9d83fafd2f6d 100644
--- a/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
+++ b/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
@@ -55,18 +55,31 @@ public virtual (int AccessNumber, int TransferNumber) CreateNewAccount(string na
         return (0, 0);
     }
 
-    public bool TryGetAccount(int accessNumber, [NotNullWhen(true)] out BankAccount? account)
+    public bool TryGetAccountFromTransferNumber(int transferNumber, [NotNullWhen(true)] out BankAccount? account)
     {
         account = null;
 
         var serverQuery = EntityQueryEnumerator<RemoteBankServerComponent>();
         if (!serverQuery.MoveNext(out var uid, out var server) ||
+            !server.TransferNumberToAccountNumberDict.TryGetValue(transferNumber, out var accessNumber) ||
             !server.AccountDict.TryGetValue(accessNumber, out var bankAcc))
             return false;
 
         account = bankAcc;
         return true;
+    }
 
+    public bool TryGetAccount(int accessNumber, [NotNullWhen(true)] out BankAccount? account)
+    {
+        account = null;
+
+        var serverQuery = EntityQueryEnumerator<RemoteBankServerComponent>();
+        if (!serverQuery.MoveNext(out var uid, out var server) ||
+            !server.AccountDict.TryGetValue(accessNumber, out var bankAcc))
+            return false;
+
+        account = bankAcc;
+        return true;
     }
 
     /// <summary>
diff --git a/Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl b/Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl
index 4debf5c6009cc..fb79a2a22f583 100644
--- a/Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl
+++ b/Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl
@@ -17,3 +17,15 @@ atm-invalid-account-number = Please enter a valid account number or present a va
 
 transactions-container-title = Transactions
 transfer-funds-button-title = Transfer Funds
+
+#transaction window
+transaction-window-title = Transfer Funds
+atm-recipient-transfer-number = Recipient:
+atm-transfer-amount = Amount:
+atm-transfer-reason = Reason:
+atm-transfer-reason-charcount = {$count} Characters Remaining
+transaction-low-funds = Error : Not Enough Funds
+transaction-no-recipient = Error : Recipient Does Not Exist
+atm-cancel-button-label = Cancel
+atm-confirm-button-label = Confirm
+atm-really-confirm-label = Really Confirm

From fc9effea3f0b599bcf4ac910bd031d791064cf35 Mon Sep 17 00:00:00 2001
From: ruddygreat <ruddygreat1@gmail.com>
Date: Sat, 15 Nov 2025 14:49:38 +0000
Subject: [PATCH 05/10] ent-ify bank accounts. transactions next I prommy.

---
 .../BUI/ATMBoundUserInterface.cs              | 145 ++++++++++++++++++
 .../UI/ATMBoundUserInterface.cs               | 131 ----------------
 .../PersonalEconomy/UI/ATMMenu.xaml.cs        |   2 +-
 .../PersonalEconomy/UI/AccountInfoBox.xaml    |   2 +
 .../PersonalEconomy/UI/AccountInfoBox.xaml.cs |  33 ++--
 ...actionWindow.xaml => TransactionMenu.xaml} |   0
 ...Window.xaml.cs => TransactionMenu.xaml.cs} |   4 +-
 .../PersonalEconomy/ServerBankingSystem.cs    | 107 +++++++------
 .../Components/ATMComponent.cs                |  10 ++
 .../Components/BankAccountComponent.cs        |  43 ++++++
 .../Components/RemoteBankServerComponent.cs   |  28 +---
 .../Events/RequestTransactionMessage.cs       |  13 ++
 .../Systems/SharedBankingSystem.cs            |  84 +++++-----
 .../Entities/Objects/Misc/banking.yml         |  11 +-
 14 files changed, 351 insertions(+), 262 deletions(-)
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/BUI/ATMBoundUserInterface.cs
 delete mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/ATMBoundUserInterface.cs
 rename Content.Client/_Impstation/PersonalEconomy/UI/{TransactionWindow.xaml => TransactionMenu.xaml} (100%)
 rename Content.Client/_Impstation/PersonalEconomy/UI/{TransactionWindow.xaml.cs => TransactionMenu.xaml.cs} (95%)
 create mode 100644 Content.Shared/_Impstation/PersonalEconomy/Components/ATMComponent.cs
 create mode 100644 Content.Shared/_Impstation/PersonalEconomy/Components/BankAccountComponent.cs
 create mode 100644 Content.Shared/_Impstation/PersonalEconomy/Events/RequestTransactionMessage.cs

diff --git a/Content.Client/_Impstation/PersonalEconomy/BUI/ATMBoundUserInterface.cs b/Content.Client/_Impstation/PersonalEconomy/BUI/ATMBoundUserInterface.cs
new file mode 100644
index 0000000000000..2e7fdec3fc0b5
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/BUI/ATMBoundUserInterface.cs
@@ -0,0 +1,145 @@
+﻿using System.Numerics;
+using Content.Client._Impstation.PersonalEconomy.UI;
+using Content.Shared._Impstation.PersonalEconomy.Components;
+using Content.Shared._Impstation.PersonalEconomy.Events;
+using Robust.Client.UserInterface;
+
+namespace Content.Client._Impstation.PersonalEconomy.BUI;
+
+public sealed class ATMBoundUserInterface : BoundUserInterface
+{
+
+    private readonly ClientBankingSystem _banking;
+
+    private ATMMenu? _atmMenu;
+    private TransactionMenu? _transactionMenu;
+    private Entity<BankAccountComponent>? _account;
+
+    public ATMBoundUserInterface(EntityUid owner, Enum uiKey) : base(owner, uiKey)
+    {
+        _banking = EntMan.System<ClientBankingSystem>();
+    }
+
+    protected override void Open()
+    {
+        base.Open();
+
+        _atmMenu = this.CreateWindow<ATMMenu>();
+        _atmMenu.CreateInvalidInfoBox();
+        _atmMenu.OnClose += ClearMenu;
+
+        _atmMenu.OnNumberEntered += s =>
+        {
+            if (!int.TryParse(s, out var i) || !_banking.TryGetAccount(i, out var account))
+            {
+                ClearMenu();
+                return;
+            }
+
+            _atmMenu.CreateInfoBoxForAccount(account.Value);
+            _account = account.Value;
+        };
+
+        _atmMenu.OnClearButtonPressed += ClearMenu;
+
+        _transactionMenu = new TransactionMenu();
+
+        _atmMenu.OnTransactionButtonPressed += () =>
+        {
+            if (_transactionMenu.IsOpen)
+            {
+                _transactionMenu.Close();
+                return;
+            }
+
+            _transactionMenu.TransferNumberBox.Clear();
+            _transactionMenu.TransferAmountBox.Clear();
+            _transactionMenu.TransferReasonBox.Clear();
+            _transactionMenu.TransactionNotEnoughFundsLabel.Visible = false;
+            _transactionMenu.TransactionRecipientDoesNotExistLabel.Visible = false;
+
+            //todo make this part of the window and not a separate thing that just gets plonked next to it
+            //like the news manager console but I have no fucking idea how that does that tbh
+            _transactionMenu.Open(_atmMenu.Position + new Vector2(_atmMenu.Width, 0));
+        };
+
+        _transactionMenu.TransactionConfirmAttempt += () =>
+        {
+            //todo make this a method yada yada
+            if (!VerifyTransaction(_transactionMenu.TransferNumberBox.Text, _transactionMenu.TransferAmount))
+            {
+                _transactionMenu.ReallyConfirmButton.Disabled = true;
+                return;
+            }
+
+            _transactionMenu.ReallyConfirmButton.Disabled = false;
+        };
+
+        _transactionMenu.TransactionConfirmed += () =>
+        {
+            //re-verify for safety
+            if (!VerifyTransaction(_transactionMenu.TransferNumberBox.Text, _transactionMenu.TransferAmount))
+            {
+                _transactionMenu.ReallyConfirmButton.Disabled = true;
+                return;
+            }
+
+            _transactionMenu.ReallyConfirmButton.Disabled = false;
+
+            //not predicted because I'm handling all transaction things server-side
+            //might be annoying for some things? I can always move it to shared if it gets *too* annoying to deal with
+            SendPredictedMessage(
+                new RequestTransactionMessage(
+                    _account!.Value.Comp.AccessNumber,
+                    int.Parse(_transactionMenu.TransferNumberBox.Text),
+                    _transactionMenu.TransferAmount,
+                    _transactionMenu.TransferReasonBox.Text)
+                );
+
+            //finally, close the menu after a theoretically successful transaction
+            _transactionMenu.Close();
+        };
+    }
+
+    protected override void Dispose(bool disposing)
+    {
+        base.Dispose(disposing);
+
+        if (!disposing)
+            return;
+
+        _atmMenu?.Dispose();
+        _transactionMenu?.Dispose();
+    }
+
+    private void ClearMenu()
+    {
+        _atmMenu?.CreateInvalidInfoBox();
+        _account = null;
+        _transactionMenu?.Close();
+    }
+
+    //todo move this into banking system
+    private bool VerifyTransaction(string recipient, int amount)
+    {
+        var verified = true;
+        //does the recipient exist?
+        if (recipient.Length != 4 || //is it a valid transfer number at all
+            !int.TryParse(recipient, out var number) ||
+            !_banking.TryGetAccountFromTransferNumber(number, out _) //does the recipient exist?
+           )
+        {
+            _transactionMenu!.TransactionRecipientDoesNotExistLabel.Visible = true;
+            verified = false;
+        }
+
+        //do we have enough money to make the transfer?
+        if (_account!.Value.Comp.Balance < amount)
+        {
+            _transactionMenu!.TransactionNotEnoughFundsLabel.Visible = true;
+            verified = false;
+        }
+
+        return verified;
+    }
+}
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/ATMBoundUserInterface.cs b/Content.Client/_Impstation/PersonalEconomy/UI/ATMBoundUserInterface.cs
deleted file mode 100644
index 5a72fb197fcde..0000000000000
--- a/Content.Client/_Impstation/PersonalEconomy/UI/ATMBoundUserInterface.cs
+++ /dev/null
@@ -1,131 +0,0 @@
-﻿using System.Numerics;
-using Content.Shared._Impstation.PersonalEconomy.Components;
-using Robust.Client.UserInterface;
-
-namespace Content.Client._Impstation.PersonalEconomy.UI;
-
-public sealed class ATMBoundUserInterface : BoundUserInterface
-{
-
-    private readonly ClientBankingSystem _banking;
-
-    private ATMMenu? _menu;
-    private TransactionWindow? _transactionWindow;
-    private BankAccount? _account;
-
-    public ATMBoundUserInterface(EntityUid owner, Enum uiKey) : base(owner, uiKey)
-    {
-        _banking = EntMan.System<ClientBankingSystem>();
-    }
-
-    protected override void Open()
-    {
-        base.Open();
-
-        _menu = this.CreateWindow<ATMMenu>();
-        _menu.CreateInvalidInfoBox();
-        _menu.OnClose += ClearMenu;
-
-        _menu.OnNumberEntered += s =>
-        {
-            if (!int.TryParse(s, out var i) || !_banking.TryGetAccount(i, out var account))
-            {
-                ClearMenu();
-                return;
-            }
-
-            _menu.CreateInfoBoxForAccount(account);
-            _account = account;
-        };
-
-        _menu.OnClearButtonPressed += ClearMenu;
-
-        _transactionWindow = new TransactionWindow();
-
-        _menu.OnTransactionButtonPressed += () =>
-        {
-            if (_transactionWindow.IsOpen)
-            {
-                _transactionWindow.Close();
-                return;
-            }
-
-            _transactionWindow.TransferNumberBox.Clear();
-            _transactionWindow.TransferAmountBox.Clear();
-            _transactionWindow.TransferReasonBox.Clear();
-
-            _transactionWindow.Open(_menu.Position + new Vector2(_menu.Width, 0)); //todo make this part of the window and not a separate thing that just gets plonked next to it
-        };
-
-        _transactionWindow.TransactionConfirmAttempt += () =>
-        {
-            //todo make this a method yada yada
-            if (!VerifyTransaction(_transactionWindow.TransferNumberBox.Text, _transactionWindow.TransferAmount))
-            {
-                _transactionWindow.ReallyConfirmButton.Disabled = true;
-                return;
-            }
-
-            _transactionWindow.ReallyConfirmButton.Disabled = false;
-        };
-
-        _transactionWindow.TransactionConfirmed += () =>
-        {
-            //re-verify for safety
-            if (!VerifyTransaction(_transactionWindow.TransferNumberBox.Text, _transactionWindow.TransferAmount))
-            {
-                _transactionWindow.ReallyConfirmButton.Disabled = true;
-                return;
-            }
-
-            _transactionWindow.ReallyConfirmButton.Disabled = false;
-
-            //todo do this
-            /*
-            SendPredictedMessage();
-            */
-        };
-    }
-
-    protected override void Dispose(bool disposing)
-    {
-        base.Dispose(disposing);
-
-        if (!disposing)
-            return;
-
-        _menu?.Dispose();
-        _transactionWindow?.Dispose();
-    }
-
-    private void ClearMenu()
-    {
-        _menu?.CreateInvalidInfoBox();
-        _account = null;
-        _transactionWindow?.Close();
-    }
-
-    //todo move this into banking system
-    private bool VerifyTransaction(string recipient, int amount)
-    {
-        var verified = true;
-        //does the recipient exist?
-        if (recipient.Length != 4 || //is it a valid transfer number at all
-            !int.TryParse(recipient, out var number) ||
-            !_banking.TryGetAccountFromTransferNumber(number, out _) //does the recipient exist?
-           )
-        {
-            _transactionWindow!.TransactionRecipientDoesNotExistLabel.Visible = true;
-            verified = false;
-        }
-
-        //do we have enough money to make the transfer?
-        if (_account!.Balance < amount)
-        {
-            _transactionWindow!.TransactionNotEnoughFundsLabel.Visible = true;
-            verified = false;
-        }
-
-        return verified;
-    }
-}
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs
index 1afeb406f2d93..92737229c2dec 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs
@@ -68,7 +68,7 @@ public void CreateInvalidInfoBox()
         AccountInfoContainer.AddChild(infoBox);
     }
 
-    public void CreateInfoBoxForAccount(BankAccount account)
+    public void CreateInfoBoxForAccount(Entity<BankAccountComponent> account)
     {
         ClearInfoBox();
 
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml
index 93be780299fa7..c71d9d4a80ae6 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml
@@ -16,5 +16,7 @@
             <Control HorizontalExpand="True"/>
             <Button Name="TransferFundsButton" Text="{Loc transfer-funds-button-title}" HorizontalAlignment="Right" Access="Public"/>
         </BoxContainer>
+        <Label Name="NoTransactionsLabel" Text="{Loc atm-machine-no-transactions}" VerticalExpand="True" HorizontalExpand="True" Visible="False"/>
+        <ScrollContainer Name="TransactionInfoBoxScroller" Visible="False"/>
     </BoxContainer>
 </BoxContainer>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs
index 3ae598df211a1..6a745e1432a4c 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs
@@ -17,32 +17,35 @@ public AccountInfoBox()
         RobustXamlLoader.Load(this);
     }
 
-    public AccountInfoBox(BankAccount account)
+    public AccountInfoBox(Entity<BankAccountComponent> account)
     {
         RobustXamlLoader.Load(this);
 
         TransferFundsButton.OnPressed += _ => OnTransactionButtonPressed?.Invoke();
 
-        AccNameLabel.Text = Loc.GetString("atm-machine-account-name-title", ("name", account.Name));
-        AccAccessNumberLabel.Text = Loc.GetString("atm-machine-account-access-number-title", ("number", $"{account.AccessNumber:000000}"));
-        AccTransferNumberLabel.Text = Loc.GetString("atm-machine-account-transfer-number-title", ("number", $"{account.TransferNumber:0000}"));
-        AccBalanceLabel.Text = Loc.GetString("atm-machine-account-balance-title", ("balance", $"{account.Balance:n0}"));
+        AccNameLabel.Text = Loc.GetString("atm-machine-account-name-title", ("name", account.Comp.Name));
+        AccAccessNumberLabel.Text = Loc.GetString("atm-machine-account-access-number-title", ("number", $"{account.Comp.AccessNumber:000000}"));
+        AccTransferNumberLabel.Text = Loc.GetString("atm-machine-account-transfer-number-title", ("number", $"{account.Comp.TransferNumber:0000}"));
+        AccBalanceLabel.Text = Loc.GetString("atm-machine-account-balance-title", ("balance", $"{account.Comp.Balance:n0}"));
 
-        if (account.Transactions.Count == 0)
+        if (account.Comp.Transactions.Count == 0)
         {
-            var noTransactionsLabel = new Label();
-            noTransactionsLabel.Text = Loc.GetString("atm-machine-no-transactions");
-            noTransactionsLabel.VerticalExpand = true;
-            noTransactionsLabel.HorizontalExpand = true;
-            noTransactionsLabel.HorizontalAlignment = HAlignment.Center;
-            noTransactionsLabel.VerticalAlignment = VAlignment.Center;
-            noTransactionsLabel.Align = Label.AlignMode.Center;
-            TransactionInfoContainer.AddChild(noTransactionsLabel);
+            NoTransactionsLabel.Visible = true;
+            TransactionInfoBoxScroller.Visible = false;
         }
         else
         {
-            //actually fill out transaction data here
+            NoTransactionsLabel.Visible = false;
+            TransactionInfoBoxScroller.Visible = true;
+
+            FillOutTransactionInfo();
         }
     }
+
+    //todo write this
+    private void FillOutTransactionInfo()
+    {
+
+    }
 }
 
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionWindow.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml
similarity index 100%
rename from Content.Client/_Impstation/PersonalEconomy/UI/TransactionWindow.xaml
rename to Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionWindow.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml.cs
similarity index 95%
rename from Content.Client/_Impstation/PersonalEconomy/UI/TransactionWindow.xaml.cs
rename to Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml.cs
index 6f48596e61628..4c0856ba3461e 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionWindow.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml.cs
@@ -6,7 +6,7 @@
 namespace Content.Client._Impstation.PersonalEconomy.UI;
 
 [GenerateTypedNameReferences]
-public sealed partial class TransactionWindow : FancyWindow
+public sealed partial class TransactionMenu : FancyWindow
 {
 
     public Action? TransactionConfirmAttempt;
@@ -14,7 +14,7 @@ public sealed partial class TransactionWindow : FancyWindow
 
     public int TransferAmount;
 
-    public TransactionWindow()
+    public TransactionMenu()
     {
         RobustXamlLoader.Load(this);
 
diff --git a/Content.Server/_Impstation/PersonalEconomy/ServerBankingSystem.cs b/Content.Server/_Impstation/PersonalEconomy/ServerBankingSystem.cs
index c79251b55a080..7ca73e160a0d6 100644
--- a/Content.Server/_Impstation/PersonalEconomy/ServerBankingSystem.cs
+++ b/Content.Server/_Impstation/PersonalEconomy/ServerBankingSystem.cs
@@ -1,11 +1,14 @@
-﻿using Content.Server.GameTicking.Events;
+﻿using System.Diagnostics;
+using Content.Server.GameTicking.Events;
 using Content.Shared._Impstation.PersonalEconomy.Components;
+using Content.Shared._Impstation.PersonalEconomy.Events;
 using Content.Shared._Impstation.PersonalEconomy.Systems;
 using Content.Shared.GameTicking;
 using Robust.Server.GameStates;
 using Robust.Shared.Map;
 using Robust.Shared.Prototypes;
 using Robust.Shared.Random;
+using Robust.Shared.Utility;
 
 namespace Content.Server._Impstation.PersonalEconomy;
 
@@ -22,7 +25,7 @@ public sealed class ServerBankingSystem : SharedBankingSystem
     [Dependency] private IRobustRandom _random = null!;
 
     private EntityUid _cheeseWorld;
-    private readonly EntProtoId _remoteServerProto = "RemoteBankServer";
+    private readonly EntProtoId _bankAccountProto = "BankAccount";
 
     /// <inheritdoc/>
     public override void Initialize()
@@ -31,14 +34,18 @@ public override void Initialize()
 
         SubscribeLocalEvent<RoundStartingEvent>(OnRoundStart);
         SubscribeLocalEvent<RoundRestartCleanupEvent>(OnRoundRestart);
+        SubscribeLocalEvent<BankCardComponent, ComponentInit>(OnComponentInit);
+    }
+
+    //todo should this be a different event?
+    private void OnComponentInit(Entity<BankCardComponent> ent, ref ComponentInit args)
+    {
+        SetupID(ent);
     }
 
     private void OnRoundStart(RoundStartingEvent ev)
     {
         _cheeseWorld = EnsurePausedMap();
-        var server = Spawn(_remoteServerProto);
-        _xform.SetParent(server, _cheeseWorld);
-        _pvsOverride.AddForceSend(server); //probably not *great*, but every client needs to know about the banking server
     }
 
     /// <summary>
@@ -66,52 +73,64 @@ private EntityUid EnsurePausedMap()
         return mapUid;
     }
 
-    protected override void SetupID(Entity<BankCardComponent> ent)
+    private void SetupID(Entity<BankCardComponent> ent)
     {
-        var details = CreateNewAccount("Unknown");
-        ent.Comp.AccessNumber = details.AccessNumber;
-        ent.Comp.TransferNumber = details.TransferNumber;
-        SetAccountSalary(details.AccessNumber, ent.Comp.Salary);
-        SetAccountBalance(details.AccessNumber, ent.Comp.StartingBalance);
+        var account = CreateNewAccount("Unknown");
+        ent.Comp.AccessNumber = account.Comp.AccessNumber;
+        ent.Comp.TransferNumber = account.Comp.TransferNumber;
+        SetAccountSalary(account.Comp.AccessNumber, ent.Comp.Salary);
+        SetAccountBalance(account.Comp.AccessNumber, ent.Comp.StartingBalance);
         Dirty(ent);
     }
 
-    /// <inheritdoc/>
-    public override (int AccessNumber, int TransferNumber) CreateNewAccount(string name)
+    private Entity<BankAccountComponent> CreateNewAccount(string name)
     {
-        var serverQuery = EntityQueryEnumerator<RemoteBankServerComponent>();
-        if (serverQuery.MoveNext(out var uid, out var server))
+
+        //since we don't cache the list of accounts anywhere, first we need to build a list of every transfer number & every access number
+        //todo cache list of accounts
+        var accountNumbers = new HashSet<int>();
+        var transferNumbers = new HashSet<int>();
+
+        var accountQuery = EntityQueryEnumerator<BankAccountComponent>();
+        while (accountQuery.MoveNext(out var uid, out var comp))
+        {
+            DebugTools.Assert(!accountNumbers.Contains(comp.AccessNumber), "Duplicate account numbers should not exist");
+            DebugTools.Assert(!transferNumbers.Contains(comp.TransferNumber), "Duplicate transfer numbers should not exist");
+
+            accountNumbers.Add(comp.AccessNumber);
+            transferNumbers.Add(comp.TransferNumber);
+        }
+
+        //generate a unique ID
+        var accountNo = _random.Next(1, 1000000);
+        while (accountNumbers.Contains(accountNo))
+        {
+            accountNo = _random.Next(1, 1000000);
+        }
+
+        //generate a unique transfer number
+        var transferNo = _random.Next(1, 10000);
+        while (transferNumbers.Contains(accountNo))
         {
-            //generate a unique ID
-            var accountNo = _random.Next(0, 1000000);
-            while (server.AccountDict.ContainsKey(accountNo))
-            {
-                accountNo = _random.Next(0, 1000000);
-            }
-
-            //generate a unique transfer number
-            var transferNo = _random.Next(0, 10000);
-            while (server.TransferNumberToAccountNumberDict.ContainsKey(accountNo))
-            {
-                transferNo = _random.Next(0, 10000);
-            }
-
-            //create a new account, put it in the accounts dict
-            server.AccountDict[accountNo] = new BankAccount
-            {
-                AccessNumber = accountNo,
-                TransferNumber = transferNo,
-                Name = name,
-            };
-
-            //and map the transfer number to the account
-            server.TransferNumberToAccountNumberDict[transferNo] = accountNo;
-
-            Dirty<RemoteBankServerComponent>((uid, server));
-            return (accountNo, transferNo);
+            transferNo = _random.Next(1, 10000);
         }
 
-        //todo error handling for if the remote server somehow gets deleted
-        return (0, 0);
+        var newAccount = Spawn(_bankAccountProto);
+        _xform.SetParent(newAccount, _cheeseWorld);
+        //probably not *great*, but every client needs to know about every bank account at all times because of the way this whole system is set up
+        //bank accounts are relatively small (3 comps - xform, meta, bankacc) entities so it's probably fine?
+        //there'll also be like, maybe a hundred in a round max? if traitors are Doing Some Shit?
+        _pvsOverride.AddForceSend(newAccount);
+
+        //create new account
+        var bankComp = Comp<BankAccountComponent>(newAccount);
+
+        bankComp.AccessNumber = accountNo;
+        bankComp.TransferNumber = transferNo;
+        bankComp.Name = name;
+
+        //and send the comp back off to the client
+        Dirty<BankAccountComponent>((newAccount, bankComp));
+        return (newAccount, bankComp);
     }
 }
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Components/ATMComponent.cs b/Content.Shared/_Impstation/PersonalEconomy/Components/ATMComponent.cs
new file mode 100644
index 0000000000000..c730ced03b9f6
--- /dev/null
+++ b/Content.Shared/_Impstation/PersonalEconomy/Components/ATMComponent.cs
@@ -0,0 +1,10 @@
+﻿namespace Content.Shared._Impstation.PersonalEconomy.Components;
+
+/// <summary>
+/// This is used to mark things as ATMs.
+/// </summary>
+[RegisterComponent]
+public sealed partial class ATMComponent : Component
+{
+
+}
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Components/BankAccountComponent.cs b/Content.Shared/_Impstation/PersonalEconomy/Components/BankAccountComponent.cs
new file mode 100644
index 0000000000000..414fa69c74515
--- /dev/null
+++ b/Content.Shared/_Impstation/PersonalEconomy/Components/BankAccountComponent.cs
@@ -0,0 +1,43 @@
+﻿using Robust.Shared.GameStates;
+using Robust.Shared.Serialization;
+
+namespace Content.Shared._Impstation.PersonalEconomy.Components;
+
+/// <summary>
+/// This is used for keeping track of the data of one bank account
+/// </summary>
+[RegisterComponent, NetworkedComponent, AutoGenerateComponentState]
+public sealed partial class BankAccountComponent : Component
+{
+    [DataField, AutoNetworkedField]
+    public string Name = string.Empty;
+
+    [DataField, AutoNetworkedField]
+    public int AccessNumber = 0;
+    [DataField, AutoNetworkedField]
+    public int TransferNumber = 0;
+
+    [DataField, AutoNetworkedField]
+    public int Balance = 0;
+    [DataField, AutoNetworkedField]
+    public int Salary = 0;
+
+    //todo make this a server-only property in the comp, then have the data get sent back to the client via a BUI state?
+    //or just have it send a normal message back to the client system that then caches the values?
+    //or just do a field delta state for this field specifically?
+    //or ent-ify bank accounts? it'll be like 50 ents with 3 comps that get force-PVS'd to everyone which feels kinda sussy but it does massively reduce the size of each state
+    //basically I don't want to be replicating every transaction to every player all the time because that will potentially be a lot of network traffic
+    //ok yeah I think I've convinced myself that ent-ifying bank accounts is the way to go?
+    //I'll only store 10 transactions, also this lets everything be in shared, so everything seems good?
+    //let this be a testament to my madness (and 1 am programming prowess)
+    [DataField, AutoNetworkedField]
+    public List<BankTransaction> Transactions = []; //todo make this only be the last like 10-15 transactions?
+}
+
+[DataDefinition, Serializable, NetSerializable]
+public sealed partial class BankTransaction
+{
+    [DataField] public int Other = 0;
+    [DataField] public int Amount = 0;
+    [DataField] public string Reason = string.Empty; //limited to 64 chars
+}
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Components/RemoteBankServerComponent.cs b/Content.Shared/_Impstation/PersonalEconomy/Components/RemoteBankServerComponent.cs
index edc23a966c6cb..c8d0b3e28c7c1 100644
--- a/Content.Shared/_Impstation/PersonalEconomy/Components/RemoteBankServerComponent.cs
+++ b/Content.Shared/_Impstation/PersonalEconomy/Components/RemoteBankServerComponent.cs
@@ -9,12 +9,13 @@ namespace Content.Shared._Impstation.PersonalEconomy.Components;
 [RegisterComponent, NetworkedComponent, AutoGenerateComponentState]
 public sealed partial class RemoteBankServerComponent : Component
 {
-    //for now I'm being lazy and just sending all the data to every client, someone yell at me later if it's a problem, I know where & how I can do the client-server split
+    //does this even need to exist if accounts are ents?
+    //theoretically this is good for a cache but we can just build that in the shared system by subbing to comp startup / shutdown in the shared system?
     /// <summary>
     /// the one that stays on the server, the more detailed list of every acct's actions
     /// </summary>
     [DataField, AutoNetworkedField]
-    public Dictionary<int, BankAccount> AccountDict = [];
+    public Dictionary<int, EntityUid> AccountDict = [];
 
     /// <summary>
     /// Mapping of transfer numbers to actual account numbers
@@ -22,26 +23,3 @@ public sealed partial class RemoteBankServerComponent : Component
     [DataField, AutoNetworkedField]
     public Dictionary<int, int> TransferNumberToAccountNumberDict = [];
 }
-
-[DataDefinition, Serializable, NetSerializable]
-public sealed partial class BankAccount
-{
-    [DataField] public string Name = string.Empty;
-
-    [DataField] public int AccessNumber = 0;
-    [DataField] public int TransferNumber = 0;
-
-    [DataField] public int Balance = 0;
-    [DataField] public int Salary = 0;
-
-    [DataField] public List<BankTransaction> Transactions = []; //todo make this only be the last like 10-15 transactions?
-}
-
-[DataDefinition, Serializable, NetSerializable]
-public sealed partial class BankTransaction
-{
-    [DataField] public bool Outgoing = false;
-    [DataField] public string Other = string.Empty;
-    [DataField] public int Amount = 0;
-    [DataField] public string Reason = string.Empty; //todo limit this to like 64 chars or so
-}
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Events/RequestTransactionMessage.cs b/Content.Shared/_Impstation/PersonalEconomy/Events/RequestTransactionMessage.cs
new file mode 100644
index 0000000000000..1c26ab635f48d
--- /dev/null
+++ b/Content.Shared/_Impstation/PersonalEconomy/Events/RequestTransactionMessage.cs
@@ -0,0 +1,13 @@
+﻿using Robust.Shared.Serialization;
+
+namespace Content.Shared._Impstation.PersonalEconomy.Events;
+
+[Serializable, NetSerializable]
+public sealed class RequestTransactionMessage(int fromAccount, int toAccount, int amount, string reason)
+    : BoundUserInterfaceMessage
+{
+    public int FromAccount = fromAccount;
+    public int ToAccount = toAccount;
+    public int Amount = amount;
+    public string Reason = reason;
+}
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs b/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
index d9d83fafd2f6d..9078aebd411e2 100644
--- a/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
+++ b/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
@@ -1,5 +1,6 @@
 ﻿using System.Diagnostics.CodeAnalysis;
 using Content.Shared._Impstation.PersonalEconomy.Components;
+using Content.Shared._Impstation.PersonalEconomy.Events;
 using Content.Shared.Administration;
 using Content.Shared.Examine;
 using Content.Shared.Roles;
@@ -17,16 +18,22 @@ public abstract class SharedBankingSystem : EntitySystem
     public override void Initialize()
     {
         SubscribeLocalEvent<BankCardComponent, ExaminedEvent>(OnExamined);
-        SubscribeLocalEvent<BankCardComponent, ComponentInit>(OnComponentInit);
+        SubscribeLocalEvent<ATMComponent, RequestTransactionMessage>(OnTransactionRequested);
     }
 
-    private void OnComponentInit(Entity<BankCardComponent> ent, ref ComponentInit args)
+    private void OnTransactionRequested(Entity<ATMComponent> ent, ref RequestTransactionMessage args)
     {
-        SetupID(ent);
-    }
 
-    protected virtual void SetupID(Entity<BankCardComponent> ent)
-    {
+        //todo write this next
+        //todo actually, ent-ify accounts first
+        //verify transaction
+        //transfer money between accounts
+        //add transaction to both accounts
+        //dirty comps
+        //job's a goodun
+
+        var foo = 1;
+        foo++;
 
     }
 
@@ -42,44 +49,43 @@ private void OnExamined(Entity<BankCardComponent> ent, ref ExaminedEvent args)
             return;
 
         args.PushMarkup("The two below are for testing!", 3);
-        args.PushMarkup(Loc.GetString("bank-card-examine-balance", ("balance", account.Balance)), 2); //todo remove this
-        args.PushMarkup(Loc.GetString("bank-card-examine-salary", ("salary", account.Salary)), 1); //todo remove this
-    }
-
-    /// <summary>
-    /// Create a new account. does nothing if run on the client.
-    /// </summary>
-    /// <returns>the Access number &amp; transfer number for the account</returns>
-    public virtual (int AccessNumber, int TransferNumber) CreateNewAccount(string name)
-    {
-        return (0, 0);
+        args.PushMarkup(Loc.GetString("bank-card-examine-balance", ("balance", account.Value.Comp.Balance)), 2); //todo remove this
+        args.PushMarkup(Loc.GetString("bank-card-examine-salary", ("salary", account.Value.Comp.Salary)), 1); //todo remove this
     }
 
-    public bool TryGetAccountFromTransferNumber(int transferNumber, [NotNullWhen(true)] out BankAccount? account)
+    public bool TryGetAccountFromTransferNumber(int transferNumber, [NotNullWhen(true)] out Entity<BankAccountComponent>? account)
     {
         account = null;
 
-        var serverQuery = EntityQueryEnumerator<RemoteBankServerComponent>();
-        if (!serverQuery.MoveNext(out var uid, out var server) ||
-            !server.TransferNumberToAccountNumberDict.TryGetValue(transferNumber, out var accessNumber) ||
-            !server.AccountDict.TryGetValue(accessNumber, out var bankAcc))
-            return false;
-
-        account = bankAcc;
-        return true;
+        //todo cache this
+        var accountQuery = EntityQueryEnumerator<BankAccountComponent>();
+        while (accountQuery.MoveNext(out var uid, out var comp))
+        {
+            if (comp.TransferNumber == transferNumber)
+            {
+                account = (uid, comp);
+                return true;
+            }
+        }
+
+        return false;
     }
 
-    public bool TryGetAccount(int accessNumber, [NotNullWhen(true)] out BankAccount? account)
+    public bool TryGetAccount(int accessNumber, [NotNullWhen(true)] out Entity<BankAccountComponent>? account)
     {
         account = null;
 
-        var serverQuery = EntityQueryEnumerator<RemoteBankServerComponent>();
-        if (!serverQuery.MoveNext(out var uid, out var server) ||
-            !server.AccountDict.TryGetValue(accessNumber, out var bankAcc))
-            return false;
-
-        account = bankAcc;
-        return true;
+        var accountQuery = EntityQueryEnumerator<BankAccountComponent>();
+        while (accountQuery.MoveNext(out var uid, out var comp))
+        {
+            if (comp.AccessNumber == accessNumber)
+            {
+                account = (uid, comp);
+                return true;
+            }
+        }
+
+        return false;
     }
 
     /// <summary>
@@ -92,7 +98,7 @@ public virtual void SetAccountName(int accessNumber, string name)
         if (!TryGetAccount(accessNumber, out var account))
             return;
 
-        account.Name = name;
+        account.Value.Comp.Name = name;
     }
 
     public virtual void SetAccountSalary(int accessNumber, int salary)
@@ -100,7 +106,7 @@ public virtual void SetAccountSalary(int accessNumber, int salary)
         if (!TryGetAccount(accessNumber, out var account))
             return;
 
-        account.Salary = salary;
+        account.Value.Comp.Salary = salary;
     }
 
     public virtual void SetAccountBalance(int accessNumber, int balance)
@@ -108,7 +114,7 @@ public virtual void SetAccountBalance(int accessNumber, int balance)
         if (!TryGetAccount(accessNumber, out var account))
             return;
 
-        account.Balance = balance;
+        account.Value.Comp.Balance = balance;
     }
 
     /// <summary>
@@ -121,8 +127,8 @@ public virtual void UpdateCardDetails(Entity<BankCardComponent> card, int access
         if (!TryGetAccount(accessNumber, out var account))
             return;
 
-        SetCardName(card, account.Name);
-        SetCardNumber(card, account.AccessNumber);
+        SetCardName(card, account.Value.Comp.Name);
+        SetCardNumber(card, account.Value.Comp.AccessNumber);
     }
 
     /// <summary>
diff --git a/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml b/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml
index a25d0a2724f99..4a102f4262542 100644
--- a/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml
+++ b/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml
@@ -20,14 +20,14 @@
   - type: BankCard
 
 - type: entity
-  id: RemoteBankServer
-  name: remote bank server
-  description: if you're seeing this, something has gone horribly wrong
+  id: BankAccount
+  name: Bank Account
+  description: if you're seeing this, something has gone horribly wrong. admins yell at me and I'll make these properly interactable at some point.
   components:
-  - type: RemoteBankServer
+    - type: BankAccount
 
 - type: entity
-  parent: BaseComputerAiAccess
+  parent: BaseComputerAiAccess #ai accessible because it's really funny if the AI convinces someone to let it steal all of their money
   id: ATMMachine
   name: ATM Machine
   description: It's called that on purpose now
@@ -61,3 +61,4 @@
     radius: 2.4 #imp
     energy: 1.6
     color: "#e6e227"
+  - type: ATM

From b35b65881b926f949c46cf0f5af9676839640c48 Mon Sep 17 00:00:00 2001
From: ruddygreat <ruddygreat1@gmail.com>
Date: Sun, 16 Nov 2025 18:20:08 +0000
Subject: [PATCH 06/10] finish up transaction stuff, make transfer numbers and
 account numbers specific Types. PoS or Vending machines next?

---
 .../BUI/ATMBoundUserInterface.cs              |  3 +
 .../PersonalEconomy/UI/ATMMenu.xaml.cs        |  4 +-
 .../PersonalEconomy/UI/AccountInfoBox.xaml    |  9 ++-
 .../PersonalEconomy/UI/AccountInfoBox.xaml.cs | 65 +++++++++++----
 .../UI/TransactionInfoBox.xaml                | 10 +++
 .../UI/TransactionInfoBox.xaml.cs             | 38 +++++++++
 .../UI/TransactionMenu.xaml.cs                |  1 +
 .../Components/BankAccountComponent.cs        | 66 ++++++++++++++--
 .../Components/BankCardComponent.cs           |  4 +-
 .../Components/RemoteBankServerComponent.cs   | 25 ------
 .../Events/RequestTransactionMessage.cs       | 18 ++++-
 .../Systems/SharedBankingSystem.cs            | 79 ++++++++++++++-----
 .../Entities/Objects/Misc/banking.yml         |  1 +
 13 files changed, 249 insertions(+), 74 deletions(-)
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml.cs
 delete mode 100644 Content.Shared/_Impstation/PersonalEconomy/Components/RemoteBankServerComponent.cs

diff --git a/Content.Client/_Impstation/PersonalEconomy/BUI/ATMBoundUserInterface.cs b/Content.Client/_Impstation/PersonalEconomy/BUI/ATMBoundUserInterface.cs
index 2e7fdec3fc0b5..4df7a062d1e1f 100644
--- a/Content.Client/_Impstation/PersonalEconomy/BUI/ATMBoundUserInterface.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/BUI/ATMBoundUserInterface.cs
@@ -55,6 +55,7 @@ protected override void Open()
             _transactionMenu.TransferNumberBox.Clear();
             _transactionMenu.TransferAmountBox.Clear();
             _transactionMenu.TransferReasonBox.Clear();
+            _transactionMenu.ReallyConfirmButton.Disabled = true;
             _transactionMenu.TransactionNotEnoughFundsLabel.Visible = false;
             _transactionMenu.TransactionRecipientDoesNotExistLabel.Visible = false;
 
@@ -120,6 +121,8 @@ private void ClearMenu()
     }
 
     //todo move this into banking system
+    //actually this kinda can't get moved into there since it needs to convey UI-specific info? maybe have an overload that returns a TransactionFailureReason enum or smth?
+    //also want to probably enforce the TransferNumber type restriction here? this only gets called from a place where a transfernumber is guaranteed to be the intention but it feels wierd not doing that
     private bool VerifyTransaction(string recipient, int amount)
     {
         var verified = true;
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs
index 92737229c2dec..1eb2496f540fa 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs
@@ -54,8 +54,8 @@ private void CheckForCard()
                 continue;
 
             //set the entered number & invoke the number entered action
-            UIKeypad.SetEnteredNumber($"{comp.AccessNumber:000000}");
-            OnNumberEntered?.Invoke($"{comp.AccessNumber:000000}");
+            UIKeypad.SetEnteredNumber($"{comp.AccessNumber.Number:000000}");
+            OnNumberEntered?.Invoke($"{comp.AccessNumber.Number:000000}");
             break;
         }
     }
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml
index c71d9d4a80ae6..c1f113dbd46f2 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml
@@ -10,13 +10,16 @@
         <Label Name="AccBalanceLabel" VerticalAlignment="Center" Access="Public"/>
     </BoxContainer>
     <!--Transaction info todo border thing here-->
-    <BoxContainer Name="TransactionInfoContainer" Orientation="Vertical" VerticalExpand="True" HorizontalExpand="True">
+    <BoxContainer Orientation="Vertical" VerticalExpand="True" HorizontalExpand="True">
         <BoxContainer Orientation="Horizontal" HorizontalExpand="True">
             <Label Text="{Loc transactions-container-title}"/>
             <Control HorizontalExpand="True"/>
             <Button Name="TransferFundsButton" Text="{Loc transfer-funds-button-title}" HorizontalAlignment="Right" Access="Public"/>
         </BoxContainer>
-        <Label Name="NoTransactionsLabel" Text="{Loc atm-machine-no-transactions}" VerticalExpand="True" HorizontalExpand="True" Visible="False"/>
-        <ScrollContainer Name="TransactionInfoBoxScroller" Visible="False"/>
+        <ScrollContainer VerticalExpand="True" HorizontalExpand="True">
+            <BoxContainer Name="TransactionInfoContainer" Orientation="Horizontal" VerticalExpand="True" HorizontalExpand="True">
+                <Label Name="NoTransactionsLabel" Text="{Loc atm-machine-no-transactions}" VerticalExpand="True" HorizontalExpand="True"/>
+            </BoxContainer>
+        </ScrollContainer>
     </BoxContainer>
 </BoxContainer>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs
index 6a745e1432a4c..83b10f66f178d 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs
@@ -1,8 +1,10 @@
-﻿using Content.Shared._Impstation.PersonalEconomy.Components;
+﻿using System.Linq;
+using Content.Shared._Impstation.PersonalEconomy.Components;
 using Robust.Client.AutoGenerated;
 using Robust.Client.UserInterface;
 using Robust.Client.UserInterface.Controls;
 using Robust.Client.UserInterface.XAML;
+using Robust.Shared.Timing;
 
 namespace Content.Client._Impstation.PersonalEconomy.UI;
 
@@ -12,6 +14,8 @@ public sealed partial class AccountInfoBox : BoxContainer
 
     public Action? OnTransactionButtonPressed;
 
+    private Entity<BankAccountComponent> _account;
+
     public AccountInfoBox()
     {
         RobustXamlLoader.Load(this);
@@ -21,31 +25,64 @@ public AccountInfoBox(Entity<BankAccountComponent> account)
     {
         RobustXamlLoader.Load(this);
 
+        //todo make this update as transactions happen etc etc
+        //just frameUpdate this shit?
+        _account = account;
+
         TransferFundsButton.OnPressed += _ => OnTransactionButtonPressed?.Invoke();
 
         AccNameLabel.Text = Loc.GetString("atm-machine-account-name-title", ("name", account.Comp.Name));
-        AccAccessNumberLabel.Text = Loc.GetString("atm-machine-account-access-number-title", ("number", $"{account.Comp.AccessNumber:000000}"));
-        AccTransferNumberLabel.Text = Loc.GetString("atm-machine-account-transfer-number-title", ("number", $"{account.Comp.TransferNumber:0000}"));
-        AccBalanceLabel.Text = Loc.GetString("atm-machine-account-balance-title", ("balance", $"{account.Comp.Balance:n0}"));
+        AccAccessNumberLabel.Text = Loc.GetString("atm-machine-account-access-number-title", ("number", $"{account.Comp.AccessNumber.Number:000000}"));
+        AccTransferNumberLabel.Text = Loc.GetString("atm-machine-account-transfer-number-title", ("number", $"{account.Comp.TransferNumber.Number:0000}"));
+
+    }
+
+    protected override void FrameUpdate(FrameEventArgs args)
+    {
+        AccBalanceLabel.Text = Loc.GetString("atm-machine-account-balance-title", ("balance", $"{_account.Comp.Balance:n0}"));
+
+        if (_account.Comp.Transactions.Count == 0 || !NeedsInfoboxRebuild())
+            return;
 
-        if (account.Comp.Transactions.Count == 0)
+        TransactionInfoContainer.RemoveAllChildren();
+        foreach (var transaction in _account.Comp.Transactions)
         {
-            NoTransactionsLabel.Visible = true;
-            TransactionInfoBoxScroller.Visible = false;
+            var infobox = new TransactionInfoBox(transaction.OtherAccount, transaction.Amount, transaction.Reason);
+            TransactionInfoContainer.AddChild(infobox);
         }
-        else
+    }
+
+    private bool NeedsInfoboxRebuild()
+    {
+        //for every transaction in the list
+        foreach (var transaction in _account.Comp.Transactions)
         {
-            NoTransactionsLabel.Visible = false;
-            TransactionInfoBoxScroller.Visible = true;
+            var infoboxExists = false;
+            //if they don't have a corresponding info box, return true
+            //notionally this only needs to check the first transactioninfo container since transactions always get added to the front of the list, but let's be safe
+            foreach (var child in TransactionInfoContainer.Children)
+            {
+                //child isn't an infobox, ignore it
+                if (child is not TransactionInfoBox { } infoBox)
+                    continue;
 
-            FillOutTransactionInfo();
+                //they are equal, the infobox exists
+                if (CompTransactionToInfobox(transaction, infoBox))
+                    infoboxExists = true;
+            }
+
+            //if it doesn't exist, break out of the loop and rebuild the list
+            if (!infoboxExists)
+                return true;
         }
+
+        return false;
     }
 
-    //todo write this
-    private void FillOutTransactionInfo()
+    //todo need to add transaction timestamps
+    private bool CompTransactionToInfobox(BankTransaction transaction, TransactionInfoBox infoBox)
     {
-
+        return transaction.OtherAccount == infoBox.Other && transaction.Amount == infoBox.Amount && transaction.Reason == infoBox.Reason;
     }
 }
 
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml
new file mode 100644
index 0000000000000..5e1004145a13d
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml
@@ -0,0 +1,10 @@
+﻿<controls:TransactionInfoBox
+    xmlns="https://spacestation14.io"
+    xmlns:controls="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI"
+    VerticalExpand="True"
+    MouseFilter="Stop">
+    <BoxContainer Orientation="Vertical" VerticalExpand="True">
+        <Label Name="TransferNoLabel" VerticalExpand="True"/>
+        <Label Name="AmountLabel" VerticalExpand="True"/>
+    </BoxContainer>
+</controls:TransactionInfoBox>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml.cs
new file mode 100644
index 0000000000000..744d01bcc5f2a
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml.cs
@@ -0,0 +1,38 @@
+﻿using Content.Shared._Impstation.PersonalEconomy.Components;
+using Robust.Client.AutoGenerated;
+using Robust.Client.UserInterface;
+using Robust.Client.UserInterface.XAML;
+
+namespace Content.Client._Impstation.PersonalEconomy.UI;
+
+[GenerateTypedNameReferences]
+public sealed partial class TransactionInfoBox : Control
+{
+    public readonly TransferNumber Other;
+    public readonly int Amount;
+    public readonly string Reason = "";
+
+    public TransactionInfoBox()
+    {
+        RobustXamlLoader.Load(this);
+    }
+
+    public TransactionInfoBox(TransferNumber other, int amount, string reason)
+    {
+
+        RobustXamlLoader.Load(this);
+
+        Other = other;
+        Amount = amount;
+        Reason = reason;
+        
+        TransferNoLabel.Text = $"#{other.Number:0000}";
+        AmountLabel.Text = $"{amount:n0}";
+
+        //todo proper custom tooltipsupplier for this
+        ToolTip = amount < 0
+            ? $"You sent account #{other.Number:0000} ${int.Abs(amount)} spesitos for \"{reason}\""
+            : $"You recieved ${amount} spesitos from account #{other} for \"{reason}\"";
+    }
+}
+
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml.cs
index 4c0856ba3461e..b4fbb1058a5f5 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml.cs
@@ -33,6 +33,7 @@ public TransactionMenu()
             TransferNumberBox.Text = numberStr;
         };
 
+        //todo this acts strangely sometimes, need to figure out why
         TransferAmountBox.OnTextChanged += args =>
         {
             var amountStr = string.Concat(args.Text.Where(char.IsDigit));
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Components/BankAccountComponent.cs b/Content.Shared/_Impstation/PersonalEconomy/Components/BankAccountComponent.cs
index 414fa69c74515..128d8c9d26534 100644
--- a/Content.Shared/_Impstation/PersonalEconomy/Components/BankAccountComponent.cs
+++ b/Content.Shared/_Impstation/PersonalEconomy/Components/BankAccountComponent.cs
@@ -13,31 +13,85 @@ public sealed partial class BankAccountComponent : Component
     public string Name = string.Empty;
 
     [DataField, AutoNetworkedField]
-    public int AccessNumber = 0;
+    public AccessNumber AccessNumber;
     [DataField, AutoNetworkedField]
-    public int TransferNumber = 0;
+    public TransferNumber TransferNumber;
 
     [DataField, AutoNetworkedField]
     public int Balance = 0;
     [DataField, AutoNetworkedField]
     public int Salary = 0;
 
-    //todo make this a server-only property in the comp, then have the data get sent back to the client via a BUI state?
+    //ok so all of this rambling is irrelevant now that I've ent-ified things, but I'll leave it here as a testament to 1 am me's thought process :)
+    //make this a server-only property in the comp, then have the data get sent back to the client via a BUI state?
     //or just have it send a normal message back to the client system that then caches the values?
     //or just do a field delta state for this field specifically?
     //or ent-ify bank accounts? it'll be like 50 ents with 3 comps that get force-PVS'd to everyone which feels kinda sussy but it does massively reduce the size of each state
     //basically I don't want to be replicating every transaction to every player all the time because that will potentially be a lot of network traffic
     //ok yeah I think I've convinced myself that ent-ifying bank accounts is the way to go?
     //I'll only store 10 transactions, also this lets everything be in shared, so everything seems good?
-    //let this be a testament to my madness (and 1 am programming prowess)
     [DataField, AutoNetworkedField]
-    public List<BankTransaction> Transactions = []; //todo make this only be the last like 10-15 transactions?
+    public List<BankTransaction> Transactions = [];
 }
 
 [DataDefinition, Serializable, NetSerializable]
 public sealed partial class BankTransaction
 {
-    [DataField] public int Other = 0;
+    public BankTransaction(TransferNumber other, int amount, string reason)
+    {
+        OtherAccount = other;
+        Amount = amount;
+        Reason = reason;
+    }
+
+    //todo let this take a name string as well as an account number
+    //todo also this needs a timestamp to make transactions with an identical body still be different
+    [DataField] public TransferNumber OtherAccount= 0;
     [DataField] public int Amount = 0;
     [DataField] public string Reason = string.Empty; //limited to 64 chars
 }
+
+/// <summary>
+/// Used to enforce transfer number / access number constraints. technically I think they can just be freely casted between because they can both go to and from int but this makes me feel smart.
+/// </summary>
+[DataDefinition, Serializable, NetSerializable]
+public partial struct AccessNumber
+{
+    public readonly int Number;
+
+    public AccessNumber(int number)
+    {
+        Number = number;
+    }
+
+    public static implicit operator int(AccessNumber number)
+    {
+        return number.Number;
+    }
+
+    public static implicit operator AccessNumber(int number)
+    {
+        return new AccessNumber(number);
+    }
+}
+
+[DataDefinition, Serializable, NetSerializable]
+public partial struct TransferNumber
+{
+    public readonly int Number;
+
+    public TransferNumber(int number)
+    {
+        Number = number;
+    }
+
+    public static implicit operator int(TransferNumber number)
+    {
+        return number.Number;
+    }
+
+    public static implicit operator TransferNumber(int number)
+    {
+        return new TransferNumber(number);
+    }
+}
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Components/BankCardComponent.cs b/Content.Shared/_Impstation/PersonalEconomy/Components/BankCardComponent.cs
index 90ed0a00aedfa..37038fefbb6f9 100644
--- a/Content.Shared/_Impstation/PersonalEconomy/Components/BankCardComponent.cs
+++ b/Content.Shared/_Impstation/PersonalEconomy/Components/BankCardComponent.cs
@@ -10,10 +10,10 @@ public sealed partial class BankCardComponent : Component
 {
 
     [DataField, AutoNetworkedField]
-    public int AccessNumber = 0; //todo this can probably just be a number that gets formatted actually?
+    public AccessNumber AccessNumber;
 
     [DataField, AutoNetworkedField]
-    public int TransferNumber = 0;
+    public TransferNumber TransferNumber;
 
     [DataField, AutoNetworkedField]
     public string Name = "Unknown";
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Components/RemoteBankServerComponent.cs b/Content.Shared/_Impstation/PersonalEconomy/Components/RemoteBankServerComponent.cs
deleted file mode 100644
index c8d0b3e28c7c1..0000000000000
--- a/Content.Shared/_Impstation/PersonalEconomy/Components/RemoteBankServerComponent.cs
+++ /dev/null
@@ -1,25 +0,0 @@
-﻿using Robust.Shared.GameStates;
-using Robust.Shared.Serialization;
-
-namespace Content.Shared._Impstation.PersonalEconomy.Components;
-
-/// <summary>
-/// This is used to keep track of what bank accounts exist and how much money they have
-/// </summary>
-[RegisterComponent, NetworkedComponent, AutoGenerateComponentState]
-public sealed partial class RemoteBankServerComponent : Component
-{
-    //does this even need to exist if accounts are ents?
-    //theoretically this is good for a cache but we can just build that in the shared system by subbing to comp startup / shutdown in the shared system?
-    /// <summary>
-    /// the one that stays on the server, the more detailed list of every acct's actions
-    /// </summary>
-    [DataField, AutoNetworkedField]
-    public Dictionary<int, EntityUid> AccountDict = [];
-
-    /// <summary>
-    /// Mapping of transfer numbers to actual account numbers
-    /// </summary>
-    [DataField, AutoNetworkedField]
-    public Dictionary<int, int> TransferNumberToAccountNumberDict = [];
-}
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Events/RequestTransactionMessage.cs b/Content.Shared/_Impstation/PersonalEconomy/Events/RequestTransactionMessage.cs
index 1c26ab635f48d..810a24048e6f7 100644
--- a/Content.Shared/_Impstation/PersonalEconomy/Events/RequestTransactionMessage.cs
+++ b/Content.Shared/_Impstation/PersonalEconomy/Events/RequestTransactionMessage.cs
@@ -1,13 +1,23 @@
-﻿using Robust.Shared.Serialization;
+﻿using Content.Shared._Impstation.PersonalEconomy.Components;
+using Robust.Shared.Serialization;
 
 namespace Content.Shared._Impstation.PersonalEconomy.Events;
 
+/// <summary>
+/// Request that a transaction attempt is made.
+/// </summary>
+/// <param name="senderAccount"> the sender's account access number</param>
+/// <param name="recipientAccount"> the recipient's account transfer number</param>
+/// <param name="amount"> the amount of money to be transferred</param>
+/// <param name="reason"> the reason for the transfer</param>
 [Serializable, NetSerializable]
-public sealed class RequestTransactionMessage(int fromAccount, int toAccount, int amount, string reason)
+//todo will probably need different types of message / transactions
+//mainly for "sending" money to arbitrary people
+public sealed class RequestTransactionMessage(AccessNumber senderAccount, TransferNumber recipientAccount, int amount, string reason)
     : BoundUserInterfaceMessage
 {
-    public int FromAccount = fromAccount;
-    public int ToAccount = toAccount;
+    public AccessNumber SenderAccount = senderAccount;
+    public TransferNumber RecipientAccount = recipientAccount;
     public int Amount = amount;
     public string Reason = reason;
 }
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs b/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
index 9078aebd411e2..9aba98ed7fc35 100644
--- a/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
+++ b/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
@@ -24,17 +24,60 @@ public override void Initialize()
     private void OnTransactionRequested(Entity<ATMComponent> ent, ref RequestTransactionMessage args)
     {
 
-        //todo write this next
-        //todo actually, ent-ify accounts first
-        //verify transaction
-        //transfer money between accounts
-        //add transaction to both accounts
-        //dirty comps
-        //job's a goodun
+        TryMakeTransaction(args.SenderAccount, args.RecipientAccount, args.Amount, args.Reason);
 
-        var foo = 1;
-        foo++;
+    }
+
+    public bool TryMakeTransaction(AccessNumber sender, TransferNumber recipient, int amount, string reason)
+    {
+        if (!VerifyTransaction(sender, recipient, amount))
+            return false;
+
+        MakeTransaction(sender, recipient, amount, reason);
+        return true;
+
+    }
+
+    private bool VerifyTransaction(AccessNumber sender, TransferNumber recipient, int amount)
+    {
+        //return false if neither account exists
+        if (!TryGetAccount(sender, out var senderAccount) ||
+            !TryGetAccountFromTransferNumber(recipient, out var recipientAccount))
+            return false;
+
+        //return true if the sender has enough money
+        return senderAccount.Value.Comp.Balance >= amount;
+    }
+
+    private void MakeTransaction(AccessNumber sender, TransferNumber recipient, int amount, string reason)
+    {
+        //this should always be true by the time this gets called but
+        //could make these out variables from the verify method, maybe?
+        //will matter less when I've got a proper cache in buuuuuut
+        if (!TryGetAccount(sender, out var senderAccount) || !TryGetAccountFromTransferNumber(recipient, out var recipientAccount))
+            return;
+
+        //adjust balances
+        senderAccount.Value.Comp.Balance -= amount;
+        recipientAccount.Value.Comp.Balance += amount;
+
+        //add transactions!
+        AddTransaction(senderAccount.Value, -amount, recipient, reason);
+        AddTransaction(recipientAccount.Value, amount, senderAccount.Value.Comp.TransferNumber, reason);
+    }
+
+    private void AddTransaction(Entity<BankAccountComponent> account, int amount, int from, string reason)
+    {
+        //limit reason length
+        if (reason.Length > 64) //todo make this length limit a cvar?
+            reason = reason[..64];
+
+        var transaction = new BankTransaction(from, amount, reason);
+        account.Comp.Transactions.Insert(0, transaction);
+        if (account.Comp.Transactions.Count > 10) //todo make the history limit a cvar?
+            account.Comp.Transactions.RemoveAt(10);
 
+        Dirty(account);
     }
 
     private void OnExamined(Entity<BankCardComponent> ent, ref ExaminedEvent args)
@@ -42,8 +85,8 @@ private void OnExamined(Entity<BankCardComponent> ent, ref ExaminedEvent args)
         if (!args.IsInDetailsRange)
             return;
 
-        args.PushMarkup(Loc.GetString("bank-card-examine-access-number", ("number", $"{ent.Comp.AccessNumber:000000}")),4);
-        args.PushMarkup(Loc.GetString("bank-card-examine-transfer-number", ("number", $"{ent.Comp.TransferNumber:0000}")),4);
+        args.PushMarkup(Loc.GetString("bank-card-examine-access-number", ("number", $"{ent.Comp.AccessNumber.Number:000000}")),4);
+        args.PushMarkup(Loc.GetString("bank-card-examine-transfer-number", ("number", $"{ent.Comp.TransferNumber.Number:0000}")),4);
 
         if (!TryGetAccount(ent.Comp.AccessNumber, out var account))
             return;
@@ -53,7 +96,7 @@ private void OnExamined(Entity<BankCardComponent> ent, ref ExaminedEvent args)
         args.PushMarkup(Loc.GetString("bank-card-examine-salary", ("salary", account.Value.Comp.Salary)), 1); //todo remove this
     }
 
-    public bool TryGetAccountFromTransferNumber(int transferNumber, [NotNullWhen(true)] out Entity<BankAccountComponent>? account)
+    public bool TryGetAccountFromTransferNumber(TransferNumber transferNumber, [NotNullWhen(true)] out Entity<BankAccountComponent>? account)
     {
         account = null;
 
@@ -71,7 +114,7 @@ public bool TryGetAccountFromTransferNumber(int transferNumber, [NotNullWhen(tru
         return false;
     }
 
-    public bool TryGetAccount(int accessNumber, [NotNullWhen(true)] out Entity<BankAccountComponent>? account)
+    public bool TryGetAccount(AccessNumber accessNumber, [NotNullWhen(true)] out Entity<BankAccountComponent>? account)
     {
         account = null;
 
@@ -93,7 +136,7 @@ public bool TryGetAccount(int accessNumber, [NotNullWhen(true)] out Entity<BankA
     /// </summary>
     /// <param name="accessNumber"></param>
     /// <param name="name"></param>
-    public virtual void SetAccountName(int accessNumber, string name)
+    public virtual void SetAccountName(AccessNumber accessNumber, string name)
     {
         if (!TryGetAccount(accessNumber, out var account))
             return;
@@ -101,7 +144,7 @@ public virtual void SetAccountName(int accessNumber, string name)
         account.Value.Comp.Name = name;
     }
 
-    public virtual void SetAccountSalary(int accessNumber, int salary)
+    public virtual void SetAccountSalary(AccessNumber accessNumber, int salary)
     {
         if (!TryGetAccount(accessNumber, out var account))
             return;
@@ -109,7 +152,7 @@ public virtual void SetAccountSalary(int accessNumber, int salary)
         account.Value.Comp.Salary = salary;
     }
 
-    public virtual void SetAccountBalance(int accessNumber, int balance)
+    public virtual void SetAccountBalance(AccessNumber accessNumber, int balance)
     {
         if (!TryGetAccount(accessNumber, out var account))
             return;
@@ -122,7 +165,7 @@ public virtual void SetAccountBalance(int accessNumber, int balance)
     /// </summary>
     /// <param name="card"></param>
     /// <param name="accessNumber"></param>
-    public virtual void UpdateCardDetails(Entity<BankCardComponent> card, int accessNumber)
+    public virtual void UpdateCardDetails(Entity<BankCardComponent> card, AccessNumber accessNumber)
     {
         if (!TryGetAccount(accessNumber, out var account))
             return;
@@ -148,7 +191,7 @@ public virtual void SetCardName(Entity<BankCardComponent> card, string name)
     /// </summary>
     /// <param name="card"></param>
     /// <param name="accessNumber"></param>
-    public virtual void SetCardNumber(Entity<BankCardComponent> card, int accessNumber)
+    public virtual void SetCardNumber(Entity<BankCardComponent> card, AccessNumber accessNumber)
     {
         card.Comp.AccessNumber = accessNumber;
         Dirty(card);
diff --git a/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml b/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml
index 4a102f4262542..d23f76c82ad5b 100644
--- a/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml
+++ b/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml
@@ -49,6 +49,7 @@
       sprite: _Impstation/Structures/Machines/computers.rsi # imp
   - type: ActivatableUI
     key: enum.ATMUIKey.Key
+    singleUser: true
   - type: UserInterface
     interfaces:
       enum.ATMUIKey.Key:

From 3883023214ce689ca27b75a4f6b6d8c46072a51c Mon Sep 17 00:00:00 2001
From: ruddygreat <ruddygreat1@gmail.com>
Date: Wed, 19 Nov 2025 16:00:37 +0000
Subject: [PATCH 07/10] POS implementation. either wages or vending machines
 next.

---
 .../BUI/ATMBoundUserInterface.cs              |   2 +-
 .../BUI/POSBoundUserInterface.cs              | 195 ++++++++++++++++++
 .../PersonalEconomy/UI/ATMMenu.xaml.cs        |   2 +
 .../PersonalEconomy/UI/AccountInfoBox.xaml.cs |   9 +-
 .../PersonalEconomy/UI/Keypad.xaml.cs         |   2 +-
 .../PersonalEconomy/UI/POS/POSMenu.xaml       |  16 ++
 .../PersonalEconomy/UI/POS/POSMenu.xaml.cs    | 103 +++++++++
 .../PersonalEconomy/UI/POS/PoSPaymentBox.xaml |  25 +++
 .../UI/POS/PoSPaymentBox.xaml.cs              |  39 ++++
 .../PersonalEconomy/UI/POS/PoSSetupBox.xaml   |  29 +++
 .../UI/POS/PoSSetupBox.xaml.cs                |  94 +++++++++
 .../UI/TransactionInfoBox.xaml                |   7 +-
 .../UI/TransactionInfoBox.xaml.cs             |  14 +-
 .../UI/TransactionMenu.xaml.cs                |   2 +
 .../UserInterface/ActivatableUISystem.cs      |   6 +-
 .../PersonalEconomy/BankingUIKeys.cs          |   6 +
 .../Components/BankAccountComponent.cs        |  42 +++-
 .../Components/POSSystemComponent.cs          |  20 ++
 .../Events/PoSTransactionFailedMessage.cs     |  10 +
 .../Events/PoSTransactionSuccededMessage.cs   |  10 +
 .../Events/UpdatePoSSettingsMessage.cs        |  12 ++
 .../Systems/SharedBankingSystem.cs            |  37 +++-
 .../_Impstation/personal-economy/banking.ftl  |  22 ++
 .../Entities/Objects/Misc/banking.yml         |  35 +++-
 24 files changed, 709 insertions(+), 30 deletions(-)
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/BUI/POSBoundUserInterface.cs
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/POS/POSMenu.xaml
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/POS/POSMenu.xaml.cs
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSPaymentBox.xaml
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSPaymentBox.xaml.cs
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSSetupBox.xaml
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSSetupBox.xaml.cs
 create mode 100644 Content.Shared/_Impstation/PersonalEconomy/Components/POSSystemComponent.cs
 create mode 100644 Content.Shared/_Impstation/PersonalEconomy/Events/PoSTransactionFailedMessage.cs
 create mode 100644 Content.Shared/_Impstation/PersonalEconomy/Events/PoSTransactionSuccededMessage.cs
 create mode 100644 Content.Shared/_Impstation/PersonalEconomy/Events/UpdatePoSSettingsMessage.cs

diff --git a/Content.Client/_Impstation/PersonalEconomy/BUI/ATMBoundUserInterface.cs b/Content.Client/_Impstation/PersonalEconomy/BUI/ATMBoundUserInterface.cs
index 4df7a062d1e1f..1c55230c2c494 100644
--- a/Content.Client/_Impstation/PersonalEconomy/BUI/ATMBoundUserInterface.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/BUI/ATMBoundUserInterface.cs
@@ -25,7 +25,6 @@ protected override void Open()
         base.Open();
 
         _atmMenu = this.CreateWindow<ATMMenu>();
-        _atmMenu.CreateInvalidInfoBox();
         _atmMenu.OnClose += ClearMenu;
 
         _atmMenu.OnNumberEntered += s =>
@@ -55,6 +54,7 @@ protected override void Open()
             _transactionMenu.TransferNumberBox.Clear();
             _transactionMenu.TransferAmountBox.Clear();
             _transactionMenu.TransferReasonBox.Clear();
+            _transactionMenu.TransferAmount = 0;
             _transactionMenu.ReallyConfirmButton.Disabled = true;
             _transactionMenu.TransactionNotEnoughFundsLabel.Visible = false;
             _transactionMenu.TransactionRecipientDoesNotExistLabel.Visible = false;
diff --git a/Content.Client/_Impstation/PersonalEconomy/BUI/POSBoundUserInterface.cs b/Content.Client/_Impstation/PersonalEconomy/BUI/POSBoundUserInterface.cs
new file mode 100644
index 0000000000000..43d6c1e420fc0
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/BUI/POSBoundUserInterface.cs
@@ -0,0 +1,195 @@
+﻿using Content.Client._Impstation.PersonalEconomy.UI.POS;
+using Content.Shared._Impstation.PersonalEconomy.Components;
+using Content.Shared._Impstation.PersonalEconomy.Events;
+using Content.Shared.Xenoarchaeology.Artifact.XAE;
+using Robust.Client.UserInterface;
+
+namespace Content.Client._Impstation.PersonalEconomy.BUI;
+
+public sealed class POSBoundUserInterface : BoundUserInterface
+{
+
+    private ClientBankingSystem _banking;
+    private POSMenu? _menu;
+
+    public POSBoundUserInterface(EntityUid owner, Enum uiKey) : base(owner, uiKey)
+    {
+        _banking = EntMan.System<ClientBankingSystem>();
+    }
+
+    //why does doing any UI work make me feel like I've been kicked in the head by a horse
+    //abandon hope, all ye who enter here
+
+    //ok I'm writing out a flowchart for this since I can't keep it all in my head
+    //UI opened
+        //if we don't have a recipient account or the user is the recipient, create a setup box
+            //if the user isn't holding a card, tell them to present one or enter a number
+            //if they are, give them the big ol' setup button
+                //setup button pressed, hide the button & label, show the proper setup dialogue - this can just go in the setup box
+                    //confirm button pressed, set everything up on the comp, keep the window open?
+        //if we have a recipient, create a payment box
+            //if the user isn't holding a card, tell them to present one or enter a number
+            //if they are, give them the payment dialogue
+
+    protected override void Open()
+    {
+        base.Open();
+
+        var comp = EntMan.GetComponent<PosSystemComponent>(Owner);
+        var recipientAccount = comp.RecipientAccount;
+        var hasRecipient = recipientAccount != 0;
+
+        _menu = this.CreateWindow<POSMenu>();
+
+        CreateRelevantBox(hasRecipient);
+
+        _menu.OnNumberEntered += s =>
+        {
+            //these don't actually need to be here but I'm too lazy to fix it rn
+            var localComp = EntMan.GetComponent<PosSystemComponent>(Owner);
+            var localRecipientAccount = localComp.RecipientAccount;
+            var localHasRecipient = localRecipientAccount != 0;
+
+            //if an invalid number was entered
+            if (!int.TryParse(s, out var userAccount) || !_banking.TryGetAccount(userAccount, out var account))
+            {
+                CreateRelevantBox(localHasRecipient);
+                return;
+            }
+
+            //else, if the we have a recipient, open the "payment" menu unless the recipient is the one opening the menu
+            var isRecipient = localRecipientAccount == account.Value.Comp.TransferNumber;
+            if (localHasRecipient && !isRecipient)
+            {
+                //we have a recipient and a valid number, set up the payment menu
+                var box = _menu.CreatePaymentBox();
+                _banking.TryGetAccountFromTransferNumber(localComp.RecipientAccount, out var recipient);
+                //just assume that the bank account will not be null at this point. god help me for when I get around to deleting accounts (:
+                //todo make this whole UI account for the fact that these accounts could all get deleted at some point
+                //todo also do that for the other one
+                box.FillOutDetails(recipient!.Value.Comp.Name, recipient.Value.Comp.TransferNumber, localComp.Amount, localComp.Reason);
+
+                box.TransactionCancelled += () => _menu.UIKeypad.ClearButtonPressed(); //evil jank go!
+
+                box.TransactionConfirmed += () =>
+                {
+                    //ok, so, we want to do a transaction finally
+                    //we need to verify we can do it, then we either send off the "transaction confirmed" message or the "transaction declined" message
+                    if (!VerifyTransaction(localComp.RecipientAccount, userAccount, localComp.Amount))
+                    {
+                        box.NoFundsLabel.Visible = true;
+                        SendPredictedMessage(new PoSTransactionFailedMessage(userAccount));
+                    }
+                    else
+                    {
+                        box.NoFundsLabel.Visible = false;
+                        SendPredictedMessage(new PoSTransactionSuccededMessage(userAccount));
+                        Close();
+                    }
+                };
+            }
+            else
+            {
+                var box = _menu.CreateSetupBox();
+                if (localHasRecipient)
+                {
+                    //if we have a recipient, fill out all the details from the component
+                    box.FillOutDetails(localComp.RecipientAccount, localComp.Amount, localComp.Reason);
+                }
+                else
+                {
+                    //else, just fill out the transfer number for the current user
+                    box.TransferNoEntryBox.Text = $"{account.Value.Comp.TransferNumber.Number:0000}";
+                }
+
+                box.OnSetupCleared += () =>
+                {
+                    SendPredictedMessage(new UpdatePoSSettingsMessage(0, 0, ""));
+                    _menu.CreateInvalidSetupBox();
+                };
+
+                box.OnSetupConfirmed += () =>
+                {
+                    var valid = true;
+                    //if the recipient doesn't exist, say what's going wrong and mark this as invalid
+                    if (!VerifyRecipient(box.TransferNoEntryBox.Text, out var number))
+                    {
+                        box.InvalidRecipientLabel.Visible = true;
+                        valid = false;
+                    }
+
+                    //if the transfer amount is 0, say what's going on and mark this as invalid
+                    if (box.TransferAmount == 0)
+                    {
+                        box.InvalidTransferAmountLabel.Visible = true;
+                        valid = false;
+                    }
+
+                    if (!valid)
+                    {
+                        box.SetupConfirmedLabel.Visible = false;
+                        return;
+                    }
+
+                    box.InvalidRecipientLabel.Visible = false;
+                    box.InvalidTransferAmountLabel.Visible = false;
+                    box.SetupConfirmedLabel.Visible = true;
+
+                    var amount = box.TransferAmount;
+                    var reason = box.TransferReasonEntryBox.Text;
+
+                    SendPredictedMessage(new UpdatePoSSettingsMessage(number, amount, reason));
+                };
+            }
+        };
+
+        _menu.OnClearButtonPressed += () =>
+        {
+            CreateRelevantBox(comp.RecipientAccount != 0);
+        };
+    }
+
+    private void CreateRelevantBox(bool hasRecipient)
+    {
+        //if we have a recipient account set, setup a payment menu
+        if (hasRecipient)
+        {
+            _menu!.CreateInvalidPaymentBox();
+        }
+        else //else, setup a setup menu
+        {
+           _menu!.CreateInvalidSetupBox();
+        }
+    }
+
+    //todo these should probably be in a helpers file?
+    private bool VerifyRecipient(string recipient, out int recipientNumber)
+    {
+        recipientNumber = 0;
+
+        var rightLength = recipient.Length == 4;
+        if (!rightLength)
+            return false;
+
+        var isInt = int.TryParse(recipient, out var number);
+        if (!isInt)
+            return false;
+
+        var exists = _banking.TryGetAccountFromTransferNumber(number, out _);
+        if (!exists)
+            return false;
+
+        recipientNumber = number;
+        return true;
+    }
+
+    private bool VerifyTransaction(int recipient, int sender, int amount)
+    {
+        if (!_banking.TryGetAccountFromTransferNumber(recipient, out var recipientAcc) ||
+            !_banking.TryGetAccount(sender, out var senderAcc) ||
+            !(senderAcc.Value.Comp.Balance > amount))
+            return false;
+
+        return true;
+    }
+}
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs
index 1eb2496f540fa..c5477a6071757 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs
@@ -27,6 +27,8 @@ public ATMMenu()
         RobustXamlLoader.Load(this);
         IoCManager.InjectDependencies(this);
 
+        CreateInvalidInfoBox();
+
         UIKeypad.OnEnterButtonPressed += s => OnNumberEntered?.Invoke(s);
         UIKeypad.OnClearButtonPressed += () => OnClearButtonPressed?.Invoke();
     }
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs
index 83b10f66f178d..822c11e5b518e 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs
@@ -47,7 +47,7 @@ protected override void FrameUpdate(FrameEventArgs args)
         TransactionInfoContainer.RemoveAllChildren();
         foreach (var transaction in _account.Comp.Transactions)
         {
-            var infobox = new TransactionInfoBox(transaction.OtherAccount, transaction.Amount, transaction.Reason);
+            var infobox = new TransactionInfoBox(transaction.OtherAccount, transaction.Name, transaction.Amount, transaction.Timestamp, transaction.Reason);
             TransactionInfoContainer.AddChild(infobox);
         }
     }
@@ -79,10 +79,13 @@ private bool NeedsInfoboxRebuild()
         return false;
     }
 
-    //todo need to add transaction timestamps
     private bool CompTransactionToInfobox(BankTransaction transaction, TransactionInfoBox infoBox)
     {
-        return transaction.OtherAccount == infoBox.Other && transaction.Amount == infoBox.Amount && transaction.Reason == infoBox.Reason;
+        return transaction.OtherAccount == infoBox.Other &&
+               transaction.Amount == infoBox.Amount &&
+               // ReSharper disable once CompareOfFloatsByEqualityOperator - we only care if the timestamps are literally identical
+               transaction.Timestamp == infoBox.Timestamp &&
+               transaction.Reason == infoBox.Reason;
     }
 }
 
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml.cs
index 61e284aca9f11..6f1833f5256da 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml.cs
@@ -57,7 +57,7 @@ private void NumberButtonPressed(int i)
         NumberDisplay.Text = _enteredNumber;
     }
 
-    private void ClearButtonPressed()
+    public void ClearButtonPressed()
     {
         _enteredNumber = "";
         NumberDisplay.Text = _enteredNumber;
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/POS/POSMenu.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/POS/POSMenu.xaml
new file mode 100644
index 0000000000000..36149e8321dde
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/POS/POSMenu.xaml
@@ -0,0 +1,16 @@
+﻿<controls1:FancyWindow
+    xmlns="https://spacestation14.io"
+    xmlns:controls1="clr-namespace:Content.Client.UserInterface.Controls"
+    xmlns:ui="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI"
+    Title="{Loc pos-window-title}"
+    MinSize="512 320"
+    SetSize="512 320">
+    <BoxContainer Orientation="Horizontal">
+        <BoxContainer Orientation="Vertical">
+            <Control VerticalExpand="True"/>
+            <ui:Keypad Name="UIKeypad" NumberLength="6" Access="Public"/>
+            <Control VerticalExpand="True"/>
+        </BoxContainer>
+        <BoxContainer Name="MainContainer" Orientation="Vertical" VerticalExpand="True" HorizontalExpand="True"/>
+    </BoxContainer>
+</controls1:FancyWindow>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/POS/POSMenu.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/POS/POSMenu.xaml.cs
new file mode 100644
index 0000000000000..46cbb1e3f841e
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/POS/POSMenu.xaml.cs
@@ -0,0 +1,103 @@
+﻿using Content.Client.UserInterface.Controls;
+using Content.Shared._Impstation.PersonalEconomy.Components;
+using Content.Shared.Hands.Components;
+using Content.Shared.Hands.EntitySystems;
+using Robust.Client.AutoGenerated;
+using Robust.Client.Player;
+using Robust.Client.UserInterface;
+using Robust.Client.UserInterface.XAML;
+using Robust.Shared.Timing;
+
+namespace Content.Client._Impstation.PersonalEconomy.UI.POS;
+
+[GenerateTypedNameReferences]
+public sealed partial class POSMenu : FancyWindow
+{
+    [Dependency] private readonly IPlayerManager _playerMan = null!;
+    [Dependency] private readonly IEntityManager _entityManager = null!;
+
+    public Action<string>? OnNumberEntered;
+    public Action? OnClearButtonPressed;
+
+    private bool _accountOpen;
+
+    public POSMenu()
+    {
+        RobustXamlLoader.Load(this);
+        IoCManager.InjectDependencies(this);
+
+        CreateInvalidSetupBox();
+
+        UIKeypad.OnClearButtonPressed += () => OnClearButtonPressed?.Invoke();
+    }
+
+    protected override void FrameUpdate(FrameEventArgs args)
+    {
+        CheckForCard();
+    }
+
+    private void CheckForCard()
+    {
+        //if we've already got an open account, we are the server or the player entity does not have hands, return
+        if (_accountOpen || _playerMan.LocalEntity is not { } realEnt || !_entityManager.HasComponent<HandsComponent>(realEnt))
+        {
+            return;
+        }
+
+        //go over everything that's held by the player
+        var handSystem = _entityManager.System<SharedHandsSystem>();
+        var held = handSystem.EnumerateHeld(realEnt);
+        foreach (var ent in held)
+        {
+            //if it's not a bank card, continue
+            if (!_entityManager.TryGetComponent<BankCardComponent>(ent, out var comp))
+                continue;
+
+            //set the entered number & invoke the number entered action
+            UIKeypad.SetEnteredNumber($"{comp.AccessNumber.Number:000000}");
+            OnNumberEntered?.Invoke($"{comp.AccessNumber.Number:000000}");
+            break;
+        }
+    }
+
+    public void ClearBox()
+    {
+        MainContainer.RemoveAllChildren();
+        _accountOpen = false;
+    }
+
+    public PoSSetupBox CreateSetupBox()
+    {
+        ClearBox();
+        var box = new PoSSetupBox(true);
+        MainContainer.AddChild(box);
+        _accountOpen = true;
+        return box;
+    }
+
+    public PoSSetupBox CreateInvalidSetupBox()
+    {
+        ClearBox();
+        var box = new PoSSetupBox(false);
+        MainContainer.AddChild(box);
+        return box;
+    }
+
+    public PoSPaymentBox CreatePaymentBox()
+    {
+        ClearBox();
+        var box = new PoSPaymentBox(true);
+        MainContainer.AddChild(box);
+        _accountOpen = true;
+        return box;
+    }
+
+    public PoSPaymentBox CreateInvalidPaymentBox()
+    {
+        ClearBox();
+        var box = new PoSPaymentBox(false);
+        MainContainer.AddChild(box);
+        return box;
+    }
+}
+
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSPaymentBox.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSPaymentBox.xaml
new file mode 100644
index 0000000000000..49038a81f1975
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSPaymentBox.xaml
@@ -0,0 +1,25 @@
+﻿<controls:PoSPaymentBox
+    xmlns="https://spacestation14.io"
+    xmlns:controls="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI.POS"
+    VerticalExpand="True"
+    HorizontalExpand="True">
+    <BoxContainer Orientation="Vertical" VerticalExpand="True">
+        <BoxContainer Name="NamingThingsIsHard" Orientation="Vertical" VerticalExpand="True" HorizontalExpand="True">
+            <Control VerticalExpand="True"/>
+            <RichTextLabel Name="ValidityLabel" Text="{Loc pos-payment-present-card}" HorizontalAlignment="Center"/>
+            <Control VerticalExpand="True"/>
+        </BoxContainer>
+        <BoxContainer Name="MainContainer" Orientation="Vertical" VerticalExpand="True" HorizontalExpand="True" Visible="False">
+            <Label Name="NameAndNumberLabel"/>
+            <Label Text="{Loc pos-payment-is-trying-to-charge}"/>
+            <Label Name="ChargeAmountLabel"/>
+            <Label Name="ChargeReasonLabel"/>
+            <Control VerticalExpand="True"/>
+            <Label Name="NoFundsLabel" Text="{Loc transaction-low-funds}" Visible="False" Access="Public"/>
+            <BoxContainer Orientation="Horizontal" HorizontalExpand="True">
+                <Button Name="CancelButton" Text="{Loc pos-payment-cancel-button-label}" HorizontalExpand="True" StyleClasses="OpenRight"/>
+                <Button Name="ConfirmTransactionButton" Text="{Loc pos-payment-confirm-button-label}" HorizontalExpand="True" StyleClasses="OpenLeft"/>
+            </BoxContainer>
+        </BoxContainer>
+    </BoxContainer>
+</controls:PoSPaymentBox>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSPaymentBox.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSPaymentBox.xaml.cs
new file mode 100644
index 0000000000000..47016a8099d70
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSPaymentBox.xaml.cs
@@ -0,0 +1,39 @@
+﻿using Content.Shared._Impstation.PersonalEconomy.Components;
+using Robust.Client.AutoGenerated;
+using Robust.Client.UserInterface;
+using Robust.Client.UserInterface.XAML;
+
+namespace Content.Client._Impstation.PersonalEconomy.UI.POS;
+
+[GenerateTypedNameReferences]
+public sealed partial class PoSPaymentBox : Control
+{
+
+    public Action? TransactionConfirmed;
+    public Action? TransactionCancelled;
+
+    public PoSPaymentBox()
+    {
+        RobustXamlLoader.Load(this);
+    }
+
+    public PoSPaymentBox(bool valid)
+    {
+        RobustXamlLoader.Load(this);
+        MainContainer.Visible = valid;
+        NamingThingsIsHard.Visible = !valid;
+
+        ConfirmTransactionButton.OnPressed += _ => TransactionConfirmed?.Invoke();
+        CancelButton.OnPressed += _ => TransactionCancelled?.Invoke();
+    }
+
+    public void FillOutDetails(string name, TransferNumber number, int amount, string reason)
+    {
+        NameAndNumberLabel.Text =
+            Loc.GetString("pos-payment-name-and-number", ("name", name), ("number", $"{number.Number:0000}"));
+        ChargeAmountLabel.Text = Loc.GetString("pos-payment-spesito-amount", ("amount", $"{amount:n0}"));
+        ChargeReasonLabel.Text =
+            string.IsNullOrEmpty(reason) ? "" : Loc.GetString("pos-payment-reason", ("reason", reason));
+    }
+}
+
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSSetupBox.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSSetupBox.xaml
new file mode 100644
index 0000000000000..25e082cc58271
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSSetupBox.xaml
@@ -0,0 +1,29 @@
+﻿<controls:PoSSetupBox
+    xmlns="https://spacestation14.io"
+    xmlns:controls="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI.POS"
+    VerticalExpand="True"
+    HorizontalExpand="True">
+    <BoxContainer Name="NamingThingsIsHard" Orientation="Vertical" VerticalExpand="True" HorizontalExpand="True">
+        <Control VerticalExpand="True"/>
+        <RichTextLabel Name="ValidityLabel" HorizontalAlignment="Center"/>
+        <Button Name="BeginSetupButton" Text="{Loc pos-begin-setup-button-text}"/>
+        <Control VerticalExpand="True"/>
+    </BoxContainer>
+    <BoxContainer Name="SetupDialogueBox" Orientation="Vertical" VerticalExpand="True" HorizontalExpand="True" Visible="False">
+        <Label Name="TransferNoLabel" Text="{Loc pos-setup-recipient-account-number}"/>
+        <LineEdit Name="TransferNoEntryBox" HorizontalExpand="True" PlaceHolder="0000" Access="Public"/>
+        <Label Name="TransferAmountLabel" Text="{Loc pos-setup-charge-amount}"/>
+        <LineEdit Name="TransferAmountEntryBox" HorizontalExpand="True" PlaceHolder="000,000,000" Access="Public"/>
+        <Label Name="TransferReasonLabel" Text="{Loc pos-setup-reason}"/>
+        <LineEdit Name="TransferReasonEntryBox" HorizontalExpand="True" PlaceHolder="yada yada" Access="Public"/>
+        <Label Name="CharsRemainingLabel"/>
+        <Control VerticalExpand="True"/>
+        <Label Name="InvalidRecipientLabel" Text="{Loc pos-setup-err-invalid-recipient}" Visible="False" Access="Public"/>
+        <Label Name="InvalidTransferAmountLabel" Text="{Loc pos-setup-err-invalid-transfer-amount}" Visible="False" Access="Public"/>
+        <Label Name="SetupConfirmedLabel" Text="{Loc pos-setup-confirmed}" Visible="False" Access="Public"/>
+        <BoxContainer Orientation="Horizontal" HorizontalExpand="True">
+            <Button Name="SetupClearButton" Text="{Loc pos-clear-setup-button-label}" HorizontalExpand="True" StyleClasses="OpenRight"/>
+            <Button Name="SetupConfirmButton" Text="{Loc pos-confirm-setup-button-label}" HorizontalExpand="True" StyleClasses="OpenLeft"/>
+        </BoxContainer>
+    </BoxContainer>
+</controls:PoSSetupBox>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSSetupBox.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSSetupBox.xaml.cs
new file mode 100644
index 0000000000000..c316a9c39c7e9
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSSetupBox.xaml.cs
@@ -0,0 +1,94 @@
+﻿using System.Linq;
+using Robust.Client.AutoGenerated;
+using Robust.Client.UserInterface;
+using Robust.Client.UserInterface.XAML;
+
+namespace Content.Client._Impstation.PersonalEconomy.UI.POS;
+
+[GenerateTypedNameReferences]
+public sealed partial class PoSSetupBox : Control
+{
+    public Action? OnSetupConfirmed;
+    public Action? OnSetupCleared;
+
+    public int TransferAmount = 0;
+
+    public PoSSetupBox()
+    {
+        RobustXamlLoader.Load(this);
+    }
+
+    public PoSSetupBox(bool valid)
+    {
+        RobustXamlLoader.Load(this);
+
+        ValidityLabel.Text =
+            valid ? Loc.GetString("pos-begin-setup-text") : Loc.GetString("pos-begin-setup-present-card");
+        BeginSetupButton.Disabled = !valid;
+
+        BeginSetupButton.OnPressed += _ =>
+        {
+            StartDialogueBox();
+        };
+
+        SetupClearButton.OnPressed += _ => OnSetupCleared?.Invoke();
+        SetupConfirmButton.OnPressed += _ => OnSetupConfirmed?.Invoke();
+
+        //todo should probably pr a NumberEdit thing to wizden to make this less evil
+        TransferNoEntryBox.OnTextChanged += args =>
+        {
+            var numberStr = string.Concat(args.Text.Where(char.IsDigit));
+            if (numberStr.Length > 4)
+                numberStr = numberStr[..4];
+
+            TransferNoEntryBox.Text = numberStr;
+        };
+
+        //todo need to make it so that this can be fully cleared
+        TransferAmountEntryBox.OnTextChanged += args =>
+        {
+            var amountStr = string.Concat(args.Text.Where(char.IsDigit));
+            if (amountStr.Length > 9)
+                amountStr = amountStr[..9];
+
+            if (int.TryParse(amountStr, out var amount))
+            {
+                TransferAmount = amount;
+            }
+
+            TransferAmountEntryBox.Text = $"{TransferAmount:n0}";
+        };
+
+        CharsRemainingLabel.Text = Loc.GetString("atm-transfer-reason-charcount", ("count", 64));
+
+        TransferReasonEntryBox.OnTextChanged += args =>
+        {
+            var len = args.Text.Length;
+            if (len > 64)
+            {
+                TransferReasonEntryBox.Text = args.Text[..64];
+            }
+
+            CharsRemainingLabel.Text = Loc.GetString("atm-transfer-reason-charcount", ("count", 64 - len));
+        };
+    }
+
+    public void FillOutDetails(int accountNo, int amount, string reason)
+    {
+        StartDialogueBox();
+
+        if (accountNo != 0)
+            TransferNoEntryBox.Text = $"{accountNo:0000}";
+        if (amount != 0)
+            TransferAmountEntryBox.Text = $"{amount:n0}";
+        if (!string.IsNullOrEmpty(reason))
+            TransferReasonEntryBox.Text = reason;
+    }
+
+    private void StartDialogueBox()
+    {
+        NamingThingsIsHard.Visible = false;
+        SetupDialogueBox.Visible = true;
+    }
+}
+
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml
index 5e1004145a13d..0d47c103319f9 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml
@@ -4,7 +4,10 @@
     VerticalExpand="True"
     MouseFilter="Stop">
     <BoxContainer Orientation="Vertical" VerticalExpand="True">
-        <Label Name="TransferNoLabel" VerticalExpand="True"/>
-        <Label Name="AmountLabel" VerticalExpand="True"/>
+        <Control VerticalExpand="True"/>
+        <Label Name="TransferNoLabel"/>
+        <Label Name="TimestampLabel"/>
+        <Label Name="AmountLabel"/>
+        <Control VerticalExpand="True"/>
     </BoxContainer>
 </controls:TransactionInfoBox>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml.cs
index 744d01bcc5f2a..b051c66d7b62c 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml.cs
@@ -9,7 +9,9 @@ namespace Content.Client._Impstation.PersonalEconomy.UI;
 public sealed partial class TransactionInfoBox : Control
 {
     public readonly TransferNumber Other;
+    public readonly string OtherName = "";
     public readonly int Amount;
+    public readonly double Timestamp;
     public readonly string Reason = "";
 
     public TransactionInfoBox()
@@ -17,22 +19,26 @@ public TransactionInfoBox()
         RobustXamlLoader.Load(this);
     }
 
-    public TransactionInfoBox(TransferNumber other, int amount, string reason)
+    public TransactionInfoBox(TransferNumber other, string name, int amount, double timestamp, string reason)
     {
 
         RobustXamlLoader.Load(this);
 
         Other = other;
+        OtherName = name;
         Amount = amount;
+        Timestamp = timestamp;
         Reason = reason;
-        
+
         TransferNoLabel.Text = $"#{other.Number:0000}";
+        TimestampLabel.Text = $@"{TimeSpan.FromSeconds(timestamp):hh\:mm\:ss}";
         AmountLabel.Text = $"{amount:n0}";
 
         //todo proper custom tooltipsupplier for this
+        //todo locstringify this
         ToolTip = amount < 0
-            ? $"You sent account #{other.Number:0000} ${int.Abs(amount)} spesitos for \"{reason}\""
-            : $"You recieved ${amount} spesitos from account #{other} for \"{reason}\"";
+            ? $"You sent {OtherName} (#{other.Number:0000}) ${int.Abs(amount)} spesitos for \"{reason}\""
+            : $"You recieved ${amount} spesitos from {OtherName} (#{other}) for \"{reason}\"";
     }
 }
 
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml.cs
index b4fbb1058a5f5..1d19bf945df6a 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml.cs
@@ -34,6 +34,8 @@ public TransactionMenu()
         };
 
         //todo this acts strangely sometimes, need to figure out why
+        //specifically it can't be fully cleared
+        //generally need to clean up all of this text entry stuff
         TransferAmountBox.OnTextChanged += args =>
         {
             var amountStr = string.Concat(args.Text.Where(char.IsDigit));
diff --git a/Content.Shared/UserInterface/ActivatableUISystem.cs b/Content.Shared/UserInterface/ActivatableUISystem.cs
index d1df375ccb544..e2abe85dfaa20 100644
--- a/Content.Shared/UserInterface/ActivatableUISystem.cs
+++ b/Content.Shared/UserInterface/ActivatableUISystem.cs
@@ -155,8 +155,10 @@ private void OnInteractUsing(EntityUid uid, ActivatableUIComponent component, In
         if (component.VerbOnly)
             return;
 
-        if (component.RequiredItems == null)
-            return;
+        //imp edit - not entirely sure why this check is here since the whitelist check will automatically pass if the list is null
+        //either way, removing this so that you can use bank cards on ATMs & PoS systems
+        //if (component.RequiredItems == null)
+        //    return;
 
         if (_whitelistSystem.IsWhitelistFail(component.RequiredItems, args.Used))
             return;
diff --git a/Content.Shared/_Impstation/PersonalEconomy/BankingUIKeys.cs b/Content.Shared/_Impstation/PersonalEconomy/BankingUIKeys.cs
index 6c56c4c8dade5..412b6a65bb98a 100644
--- a/Content.Shared/_Impstation/PersonalEconomy/BankingUIKeys.cs
+++ b/Content.Shared/_Impstation/PersonalEconomy/BankingUIKeys.cs
@@ -8,3 +8,9 @@ public enum ATMUIKey : byte
     Key
 }
 
+[Serializable, NetSerializable]
+public enum POSUIKey : byte
+{
+    Key
+}
+
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Components/BankAccountComponent.cs b/Content.Shared/_Impstation/PersonalEconomy/Components/BankAccountComponent.cs
index 128d8c9d26534..88de691365021 100644
--- a/Content.Shared/_Impstation/PersonalEconomy/Components/BankAccountComponent.cs
+++ b/Content.Shared/_Impstation/PersonalEconomy/Components/BankAccountComponent.cs
@@ -37,17 +37,19 @@ public sealed partial class BankAccountComponent : Component
 [DataDefinition, Serializable, NetSerializable]
 public sealed partial class BankTransaction
 {
-    public BankTransaction(TransferNumber other, int amount, string reason)
+    public BankTransaction(TransferNumber other, string name, int amount, double timestamp, string reason)
     {
         OtherAccount = other;
+        Name = name;
         Amount = amount;
+        Timestamp = timestamp;
         Reason = reason;
     }
 
-    //todo let this take a name string as well as an account number
-    //todo also this needs a timestamp to make transactions with an identical body still be different
     [DataField] public TransferNumber OtherAccount= 0;
+    [DataField] public string Name = ""; //technically this doesn't need to be stored here since both the client & server will know what all account names are at all times, but I like it keeping track of renambed accounts for scams and such
     [DataField] public int Amount = 0;
+    [DataField] public double Timestamp = 0;
     [DataField] public string Reason = string.Empty; //limited to 64 chars
 }
 
@@ -55,7 +57,7 @@ public BankTransaction(TransferNumber other, int amount, string reason)
 /// Used to enforce transfer number / access number constraints. technically I think they can just be freely casted between because they can both go to and from int but this makes me feel smart.
 /// </summary>
 [DataDefinition, Serializable, NetSerializable]
-public partial struct AccessNumber
+public partial struct AccessNumber : IEquatable<AccessNumber>
 {
     public readonly int Number;
 
@@ -73,10 +75,25 @@ public static implicit operator AccessNumber(int number)
     {
         return new AccessNumber(number);
     }
+
+    public bool Equals(AccessNumber other)
+    {
+        return Number == other.Number;
+    }
+
+    public override bool Equals(object? obj)
+    {
+        return obj is AccessNumber other && Equals(other);
+    }
+
+    public override int GetHashCode()
+    {
+        return Number;
+    }
 }
 
 [DataDefinition, Serializable, NetSerializable]
-public partial struct TransferNumber
+public partial struct TransferNumber : IEquatable<TransferNumber>
 {
     public readonly int Number;
 
@@ -94,4 +111,19 @@ public static implicit operator TransferNumber(int number)
     {
         return new TransferNumber(number);
     }
+
+    public bool Equals(TransferNumber other)
+    {
+        return Number == other.Number;
+    }
+
+    public override bool Equals(object? obj)
+    {
+        return obj is TransferNumber other && Equals(other);
+    }
+
+    public override int GetHashCode()
+    {
+        return Number;
+    }
 }
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Components/POSSystemComponent.cs b/Content.Shared/_Impstation/PersonalEconomy/Components/POSSystemComponent.cs
new file mode 100644
index 0000000000000..7a5df48278cc4
--- /dev/null
+++ b/Content.Shared/_Impstation/PersonalEconomy/Components/POSSystemComponent.cs
@@ -0,0 +1,20 @@
+﻿using Robust.Shared.GameStates;
+
+namespace Content.Shared._Impstation.PersonalEconomy.Components;
+
+/// <summary>
+/// This stores the destination account, charge & reason for a PoS system.
+/// </summary>
+[RegisterComponent, NetworkedComponent, AutoGenerateComponentState]
+public sealed partial class PosSystemComponent : Component
+{
+    [AutoNetworkedField]
+    public TransferNumber RecipientAccount = 0;
+
+    [AutoNetworkedField]
+    public int Amount = 0;
+
+    [AutoNetworkedField]
+    public string Reason = "";
+
+}
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Events/PoSTransactionFailedMessage.cs b/Content.Shared/_Impstation/PersonalEconomy/Events/PoSTransactionFailedMessage.cs
new file mode 100644
index 0000000000000..4d803df80774f
--- /dev/null
+++ b/Content.Shared/_Impstation/PersonalEconomy/Events/PoSTransactionFailedMessage.cs
@@ -0,0 +1,10 @@
+﻿using Content.Shared._Impstation.PersonalEconomy.Components;
+using Robust.Shared.Serialization;
+
+namespace Content.Shared._Impstation.PersonalEconomy.Events;
+
+[Serializable, NetSerializable]
+public sealed class PoSTransactionFailedMessage(AccessNumber account) : BoundUserInterfaceMessage
+{
+    public AccessNumber Account = account;
+}
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Events/PoSTransactionSuccededMessage.cs b/Content.Shared/_Impstation/PersonalEconomy/Events/PoSTransactionSuccededMessage.cs
new file mode 100644
index 0000000000000..b5b3475f9ed5a
--- /dev/null
+++ b/Content.Shared/_Impstation/PersonalEconomy/Events/PoSTransactionSuccededMessage.cs
@@ -0,0 +1,10 @@
+﻿using Content.Shared._Impstation.PersonalEconomy.Components;
+using Robust.Shared.Serialization;
+
+namespace Content.Shared._Impstation.PersonalEconomy.Events;
+
+[Serializable, NetSerializable]
+public sealed class PoSTransactionSuccededMessage(AccessNumber account) : BoundUserInterfaceMessage
+{
+    public AccessNumber Account = account;
+}
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Events/UpdatePoSSettingsMessage.cs b/Content.Shared/_Impstation/PersonalEconomy/Events/UpdatePoSSettingsMessage.cs
new file mode 100644
index 0000000000000..20382e2d8b653
--- /dev/null
+++ b/Content.Shared/_Impstation/PersonalEconomy/Events/UpdatePoSSettingsMessage.cs
@@ -0,0 +1,12 @@
+﻿using Content.Shared._Impstation.PersonalEconomy.Components;
+using Robust.Shared.Serialization;
+
+namespace Content.Shared._Impstation.PersonalEconomy.Events;
+
+[Serializable, NetSerializable]
+public sealed class UpdatePoSSettingsMessage(TransferNumber recipient, int amount, string reason) : BoundUserInterfaceMessage
+{
+    public TransferNumber Recipient = recipient;
+    public int Amount = amount;
+    public string Reason = reason;
+}
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs b/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
index 9aba98ed7fc35..5ece8985b1ba9 100644
--- a/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
+++ b/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
@@ -3,7 +3,9 @@
 using Content.Shared._Impstation.PersonalEconomy.Events;
 using Content.Shared.Administration;
 using Content.Shared.Examine;
+using Content.Shared.Interaction;
 using Content.Shared.Roles;
+using Robust.Shared.Timing;
 
 namespace Content.Shared._Impstation.PersonalEconomy.Systems;
 
@@ -13,19 +15,41 @@ namespace Content.Shared._Impstation.PersonalEconomy.Systems;
 public abstract class SharedBankingSystem : EntitySystem
 {
     [Dependency] private readonly MetaDataSystem _metaData = null!;
+    [Dependency] private readonly IGameTiming _timing = null!;
 
     /// <inheritdoc/>
     public override void Initialize()
     {
         SubscribeLocalEvent<BankCardComponent, ExaminedEvent>(OnExamined);
         SubscribeLocalEvent<ATMComponent, RequestTransactionMessage>(OnTransactionRequested);
+        SubscribeLocalEvent<PosSystemComponent, UpdatePoSSettingsMessage>(OnPoSSettingsUpdate);
+        SubscribeLocalEvent<PosSystemComponent, PoSTransactionSuccededMessage>(OnTransactionSucceded);
+        SubscribeLocalEvent<PosSystemComponent, PoSTransactionFailedMessage>(OnTransactionFailed);
     }
 
-    private void OnTransactionRequested(Entity<ATMComponent> ent, ref RequestTransactionMessage args)
+    private void OnTransactionFailed(Entity<PosSystemComponent> ent, ref PoSTransactionFailedMessage args)
     {
+        //todo implement this : have the pos system put out a "transaction failed" signal
+    }
 
-        TryMakeTransaction(args.SenderAccount, args.RecipientAccount, args.Amount, args.Reason);
+    private void OnTransactionSucceded(Entity<PosSystemComponent> ent, ref PoSTransactionSuccededMessage args)
+    {
+        //todo make this put out a "transaction succeded" signal
+        TryMakeTransaction(args.Account, ent.Comp.RecipientAccount, ent.Comp.Amount, ent.Comp.Reason);
+    }
+
+    private void OnPoSSettingsUpdate(Entity<PosSystemComponent> ent, ref UpdatePoSSettingsMessage args)
+    {
+        ent.Comp.RecipientAccount = args.Recipient;
+        ent.Comp.Amount = args.Amount;
+        ent.Comp.Reason = args.Reason;
+
+        Dirty(ent);
+    }
 
+    private void OnTransactionRequested(Entity<ATMComponent> ent, ref RequestTransactionMessage args)
+    {
+        TryMakeTransaction(args.SenderAccount, args.RecipientAccount, args.Amount, args.Reason);
     }
 
     public bool TryMakeTransaction(AccessNumber sender, TransferNumber recipient, int amount, string reason)
@@ -62,17 +86,18 @@ private void MakeTransaction(AccessNumber sender, TransferNumber recipient, int
         recipientAccount.Value.Comp.Balance += amount;
 
         //add transactions!
-        AddTransaction(senderAccount.Value, -amount, recipient, reason);
-        AddTransaction(recipientAccount.Value, amount, senderAccount.Value.Comp.TransferNumber, reason);
+        AddTransaction(senderAccount.Value, recipientAccount.Value.Comp.Name, -amount, recipient, reason);
+        AddTransaction(recipientAccount.Value, senderAccount.Value.Comp.Name, amount, senderAccount.Value.Comp.TransferNumber, reason);
     }
 
-    private void AddTransaction(Entity<BankAccountComponent> account, int amount, int from, string reason)
+    private void AddTransaction(Entity<BankAccountComponent> account, string otherName, int amount, int from, string reason)
     {
         //limit reason length
         if (reason.Length > 64) //todo make this length limit a cvar?
             reason = reason[..64];
 
-        var transaction = new BankTransaction(from, amount, reason);
+        var timestamp = _timing.CurTime.TotalSeconds;
+        var transaction = new BankTransaction(from, otherName, amount, timestamp, reason);
         account.Comp.Transactions.Insert(0, transaction);
         if (account.Comp.Transactions.Count > 10) //todo make the history limit a cvar?
             account.Comp.Transactions.RemoveAt(10);
diff --git a/Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl b/Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl
index fb79a2a22f583..2092dadac923e 100644
--- a/Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl
+++ b/Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl
@@ -29,3 +29,25 @@ transaction-no-recipient = Error : Recipient Does Not Exist
 atm-cancel-button-label = Cancel
 atm-confirm-button-label = Confirm
 atm-really-confirm-label = Really Confirm
+
+#pos system
+pos-window-title = Point-of-sale system
+pos-begin-setup-text = This device has not been set up, please press the button below to begin
+pos-begin-setup-present-card = This device has not been set up, please enter a valid account number or present a valid bank card to continue
+pos-begin-setup-button-text = Begin Setup
+pos-setup-recipient-account-number = Recipient Account Number
+pos-setup-charge-amount = Charge Amount
+pos-setup-reason = Reason
+pos-setup-err-invalid-recipient = Error : invalid recipient
+pos-setup-err-invalid-transfer-amount = Error : please specify charge amount
+pos-setup-confirmed = settings updated!
+pos-clear-setup-button-label = Clear Setup
+pos-confirm-setup-button-label = Confirm
+
+pos-payment-present-card = Please enter a valid account number or present a valid bank card to continue
+pos-payment-name-and-number = {$name} (#{$number})
+pos-payment-is-trying-to-charge = Is trying to charge you
+pos-payment-spesito-amount = ${$amount} Spesitos
+pos-payment-reason = for "{$reason}"
+pos-payment-confirm-button-label = Confirm Transaction
+pos-payment-cancel-button-label = Cancel
diff --git a/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml b/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml
index d23f76c82ad5b..9ed60c94306ef 100644
--- a/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml
+++ b/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml
@@ -5,14 +5,14 @@
   description: The little square of plastic that controls your life. No, the other one.
   components:
   - type: Sprite
-    sprite: _Impstation/Objects/Misc/id_cards.rsi #imp edit
+    sprite: _Impstation/Objects/Misc/id_cards.rsi
     layers:
     - state: default
     - state: idpassenger
   - type: Clothing
     slots:
     - idcard
-    sprite: _Impstation/Objects/Misc/id_cards.rsi #imp edit
+    sprite: _Impstation/Objects/Misc/id_cards.rsi
   - type: Item
     size: Small
     heldPrefix: default
@@ -36,17 +36,17 @@
     layers:
     - map: ["computerLayerBody"]
       state: computer
-      sprite: _Impstation/Structures/Machines/computers.rsi # imp
+      sprite: _Impstation/Structures/Machines/computers.rsi
     - map: ["computerLayerKeyboard"]
       state: generic_keyboard
-      sprite: _Impstation/Structures/Machines/computers.rsi # imp
+      sprite: _Impstation/Structures/Machines/computers.rsi
     - map: ["computerLayerScreen"]
       state: solar_screen
     - map: ["computerLayerKeys"]
       state: generic_keys
     - map: [ "enum.WiresVisualLayers.MaintenancePanel" ]
       state: generic_panel_open
-      sprite: _Impstation/Structures/Machines/computers.rsi # imp
+      sprite: _Impstation/Structures/Machines/computers.rsi
   - type: ActivatableUI
     key: enum.ATMUIKey.Key
     singleUser: true
@@ -59,7 +59,30 @@
   - type: Computer
     board: RadarConsoleCircuitboard
   - type: PointLight
-    radius: 2.4 #imp
+    radius: 2.4
     energy: 1.6
     color: "#e6e227"
   - type: ATM
+
+- type: entity
+  parent: BaseItem
+  id: POSSystem
+  name: point-of-sale system
+  description: Historians believe these were invented largely to assist with scams and bribery. Nanotrasen continues this proud tradition, in the name of historical preservation.
+  components:
+  - type: Sprite
+    sprite: Objects/Tools/hand_labeler.rsi
+    state: hand_labeler
+  - type: Item
+    sprite: Objects/Tools/hand_labeler.rsi
+    storedRotation: -90
+  - type: UseDelay
+    delay: 0.5
+  - type: ActivatableUI
+    key: enum.POSUIKey.Key
+  - type: UserInterface
+    interfaces:
+      enum.POSUIKey.Key:
+        type: POSBoundUserInterface
+  - type: PosSystem
+  - type: PhysicalComposition

From a478aafb756fb341e1a30be1c6d3139fa1717031 Mon Sep 17 00:00:00 2001
From: ruddygreat <ruddygreat1@gmail.com>
Date: Wed, 19 Nov 2025 23:27:03 +0000
Subject: [PATCH 08/10] going to checkout another Pr. vending machine
 integration next

---
 .../PersonalEconomy/BUI/POSBoundUserInterface.cs            | 2 ++
 .../_Impstation/PersonalEconomy/UI/POS/PoSSetupBox.xaml.cs  | 6 +++---
 .../PersonalEconomy/Systems/SharedBankingSystem.cs          | 1 +
 3 files changed, 6 insertions(+), 3 deletions(-)

diff --git a/Content.Client/_Impstation/PersonalEconomy/BUI/POSBoundUserInterface.cs b/Content.Client/_Impstation/PersonalEconomy/BUI/POSBoundUserInterface.cs
index 43d6c1e420fc0..f77e79e776c46 100644
--- a/Content.Client/_Impstation/PersonalEconomy/BUI/POSBoundUserInterface.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/BUI/POSBoundUserInterface.cs
@@ -20,6 +20,8 @@ public POSBoundUserInterface(EntityUid owner, Enum uiKey) : base(owner, uiKey)
     //why does doing any UI work make me feel like I've been kicked in the head by a horse
     //abandon hope, all ye who enter here
 
+    //todo need to make this allow for negative charge amounts so that pawn shops can exist? or do I want to force them to go through cash only?
+
     //ok I'm writing out a flowchart for this since I can't keep it all in my head
     //UI opened
         //if we don't have a recipient account or the user is the recipient, create a setup box
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSSetupBox.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSSetupBox.xaml.cs
index c316a9c39c7e9..0ae6bf0194382 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSSetupBox.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSSetupBox.xaml.cs
@@ -78,11 +78,11 @@ public void FillOutDetails(int accountNo, int amount, string reason)
         StartDialogueBox();
 
         if (accountNo != 0)
-            TransferNoEntryBox.Text = $"{accountNo:0000}";
+            TransferNoEntryBox.SetText($"{accountNo:0000}");
         if (amount != 0)
-            TransferAmountEntryBox.Text = $"{amount:n0}";
+            TransferAmountEntryBox.SetText($"{amount}");
         if (!string.IsNullOrEmpty(reason))
-            TransferReasonEntryBox.Text = reason;
+            TransferReasonEntryBox.SetText(reason);
     }
 
     private void StartDialogueBox()
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs b/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
index 5ece8985b1ba9..72fb0fd147c7e 100644
--- a/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
+++ b/Content.Shared/_Impstation/PersonalEconomy/Systems/SharedBankingSystem.cs
@@ -54,6 +54,7 @@ private void OnTransactionRequested(Entity<ATMComponent> ent, ref RequestTransac
 
     public bool TryMakeTransaction(AccessNumber sender, TransferNumber recipient, int amount, string reason)
     {
+        //todo need to do something for if a transaction becomes invalid after a client confirms it
         if (!VerifyTransaction(sender, recipient, amount))
             return false;
 

From d72481f28fec51b571665d3d2ea766481eb71446 Mon Sep 17 00:00:00 2001
From: ruddygreat <ruddygreat1@gmail.com>
Date: Tue, 9 Dec 2025 15:09:52 +0000
Subject: [PATCH 09/10] make value entry fields less janky, make PoS systems
 work

---
 .../_Impstation/PersonalEconomy/UI/POS/POSMenu.xaml.cs |  1 +
 .../PersonalEconomy/UI/POS/PoSSetupBox.xaml.cs         |  8 +++++++-
 .../PersonalEconomy/UI/TransactionMenu.xaml.cs         | 10 +++++++---
 3 files changed, 15 insertions(+), 4 deletions(-)

diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/POS/POSMenu.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/POS/POSMenu.xaml.cs
index 46cbb1e3f841e..f3112060bf6a3 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/POS/POSMenu.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/POS/POSMenu.xaml.cs
@@ -28,6 +28,7 @@ public POSMenu()
 
         CreateInvalidSetupBox();
 
+        UIKeypad.OnEnterButtonPressed += s => OnNumberEntered?.Invoke(s);
         UIKeypad.OnClearButtonPressed += () => OnClearButtonPressed?.Invoke();
     }
 
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSSetupBox.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSSetupBox.xaml.cs
index 0ae6bf0194382..4e86f14702c1a 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSSetupBox.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/POS/PoSSetupBox.xaml.cs
@@ -44,13 +44,19 @@ public PoSSetupBox(bool valid)
             TransferNoEntryBox.Text = numberStr;
         };
 
-        //todo need to make it so that this can be fully cleared
         TransferAmountEntryBox.OnTextChanged += args =>
         {
             var amountStr = string.Concat(args.Text.Where(char.IsDigit));
             if (amountStr.Length > 9)
                 amountStr = amountStr[..9];
 
+            if (string.IsNullOrEmpty(amountStr))
+            {
+                TransferAmount = 0;
+                TransferAmountEntryBox.Clear();
+                return;
+            }
+
             if (int.TryParse(amountStr, out var amount))
             {
                 TransferAmount = amount;
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml.cs
index 1d19bf945df6a..ca198c0303d99 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml.cs
@@ -33,15 +33,19 @@ public TransactionMenu()
             TransferNumberBox.Text = numberStr;
         };
 
-        //todo this acts strangely sometimes, need to figure out why
-        //specifically it can't be fully cleared
-        //generally need to clean up all of this text entry stuff
         TransferAmountBox.OnTextChanged += args =>
         {
             var amountStr = string.Concat(args.Text.Where(char.IsDigit));
             if (amountStr.Length > 9)
                 amountStr = amountStr[..9];
 
+            if (string.IsNullOrEmpty(amountStr))
+            {
+                TransferAmount = 0;
+                TransferAmountBox.Clear();
+                return;
+            }
+
             if (int.TryParse(amountStr, out var amount))
             {
                 TransferAmount = amount;

From a037abb61caf242ad2b6ec560312350bd31c99fa Mon Sep 17 00:00:00 2001
From: ruddygreat <ruddygreat1@gmail.com>
Date: Tue, 9 Dec 2025 17:02:20 +0000
Subject: [PATCH 10/10] minor reorganisation & starting on account management
 console

---
 .../BUI/ATMBoundUserInterface.cs              |  8 ++--
 ...ountManagementConsoleBoundUserInterface.cs | 25 ++++++++++
 .../PersonalEconomy/UI/{ => ATM}/ATMMenu.xaml |  3 +-
 .../UI/{ => ATM}/ATMMenu.xaml.cs              |  5 +-
 .../UI/{ => ATM}/AccountInfoBox.xaml          |  0
 .../UI/{ => ATM}/AccountInfoBox.xaml.cs       |  6 +--
 .../PersonalEconomy/UI/{ => ATM}/Keypad.xaml  |  7 +--
 .../UI/{ => ATM}/Keypad.xaml.cs               |  4 +-
 .../UI/{ => ATM}/NoAccountInfoBox.xaml        |  0
 .../UI/{ => ATM}/NoAccountInfoBox.xaml.cs     |  3 +-
 .../UI/{ => ATM}/TransactionInfoBox.xaml      |  5 +-
 .../UI/{ => ATM}/TransactionInfoBox.xaml.cs   |  2 +-
 .../UI/{ => ATM}/TransactionMenu.xaml         |  0
 .../UI/{ => ATM}/TransactionMenu.xaml.cs      |  6 +--
 .../AccountInfoSelectorButtonThing.xaml       |  5 ++
 .../AccountInfoSelectorButtonThing.xaml.cs    | 24 ++++++++++
 .../AccountManagementMenu.xaml                | 12 +++++
 .../AccountManagementMenu.xaml.cs             | 31 ++++++++++++
 .../DetailManagementPanel.xaml                |  6 +++
 .../DetailManagementPanel.xaml.cs             | 16 +++++++
 .../SalaryManagementPanel.xaml                | 20 ++++++++
 .../SalaryManagementPanel.xaml.cs             | 23 +++++++++
 .../PersonalEconomy/UI/POS/POSMenu.xaml       |  3 +-
 .../PersonalEconomy/BankingUIKeys.cs          |  5 ++
 .../AccountManagementConsoleComponent.cs      | 10 ++++
 .../_Impstation/personal-economy/banking.ftl  |  5 ++
 .../Entities/Objects/Misc/banking.yml         | 47 +++++++++++++++++--
 27 files changed, 252 insertions(+), 29 deletions(-)
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/BUI/AccountManagementConsoleBoundUserInterface.cs
 rename Content.Client/_Impstation/PersonalEconomy/UI/{ => ATM}/ATMMenu.xaml (82%)
 rename Content.Client/_Impstation/PersonalEconomy/UI/{ => ATM}/ATMMenu.xaml.cs (95%)
 rename Content.Client/_Impstation/PersonalEconomy/UI/{ => ATM}/AccountInfoBox.xaml (100%)
 rename Content.Client/_Impstation/PersonalEconomy/UI/{ => ATM}/AccountInfoBox.xaml.cs (94%)
 rename Content.Client/_Impstation/PersonalEconomy/UI/{ => ATM}/Keypad.xaml (92%)
 rename Content.Client/_Impstation/PersonalEconomy/UI/{ => ATM}/Keypad.xaml.cs (94%)
 rename Content.Client/_Impstation/PersonalEconomy/UI/{ => ATM}/NoAccountInfoBox.xaml (100%)
 rename Content.Client/_Impstation/PersonalEconomy/UI/{ => ATM}/NoAccountInfoBox.xaml.cs (76%)
 rename Content.Client/_Impstation/PersonalEconomy/UI/{ => ATM}/TransactionInfoBox.xaml (77%)
 rename Content.Client/_Impstation/PersonalEconomy/UI/{ => ATM}/TransactionInfoBox.xaml.cs (95%)
 rename Content.Client/_Impstation/PersonalEconomy/UI/{ => ATM}/TransactionMenu.xaml (100%)
 rename Content.Client/_Impstation/PersonalEconomy/UI/{ => ATM}/TransactionMenu.xaml.cs (88%)
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/AccountInfoSelectorButtonThing.xaml
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/AccountInfoSelectorButtonThing.xaml.cs
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/AccountManagementMenu.xaml
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/AccountManagementMenu.xaml.cs
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/DetailManagementPanel.xaml
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/DetailManagementPanel.xaml.cs
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/SalaryManagementPanel.xaml
 create mode 100644 Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/SalaryManagementPanel.xaml.cs
 create mode 100644 Content.Shared/_Impstation/PersonalEconomy/Components/AccountManagementConsoleComponent.cs

diff --git a/Content.Client/_Impstation/PersonalEconomy/BUI/ATMBoundUserInterface.cs b/Content.Client/_Impstation/PersonalEconomy/BUI/ATMBoundUserInterface.cs
index 1c55230c2c494..574d4fb0e686e 100644
--- a/Content.Client/_Impstation/PersonalEconomy/BUI/ATMBoundUserInterface.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/BUI/ATMBoundUserInterface.cs
@@ -11,8 +11,8 @@ public sealed class ATMBoundUserInterface : BoundUserInterface
 
     private readonly ClientBankingSystem _banking;
 
-    private ATMMenu? _atmMenu;
-    private TransactionMenu? _transactionMenu;
+    private UI.ATM.ATMMenu? _atmMenu;
+    private UI.ATM.TransactionMenu? _transactionMenu;
     private Entity<BankAccountComponent>? _account;
 
     public ATMBoundUserInterface(EntityUid owner, Enum uiKey) : base(owner, uiKey)
@@ -24,7 +24,7 @@ protected override void Open()
     {
         base.Open();
 
-        _atmMenu = this.CreateWindow<ATMMenu>();
+        _atmMenu = this.CreateWindow<UI.ATM.ATMMenu>();
         _atmMenu.OnClose += ClearMenu;
 
         _atmMenu.OnNumberEntered += s =>
@@ -41,7 +41,7 @@ protected override void Open()
 
         _atmMenu.OnClearButtonPressed += ClearMenu;
 
-        _transactionMenu = new TransactionMenu();
+        _transactionMenu = new UI.ATM.TransactionMenu();
 
         _atmMenu.OnTransactionButtonPressed += () =>
         {
diff --git a/Content.Client/_Impstation/PersonalEconomy/BUI/AccountManagementConsoleBoundUserInterface.cs b/Content.Client/_Impstation/PersonalEconomy/BUI/AccountManagementConsoleBoundUserInterface.cs
new file mode 100644
index 0000000000000..4d95efbad863f
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/BUI/AccountManagementConsoleBoundUserInterface.cs
@@ -0,0 +1,25 @@
+﻿using Content.Client._Impstation.PersonalEconomy.UI.AccountManagement;
+using Robust.Client.UserInterface;
+
+namespace Content.Client._Impstation.PersonalEconomy.BUI;
+
+public sealed class AccountManagementConsoleBoundUserInterface : BoundUserInterface
+{
+
+    private readonly ClientBankingSystem _banking;
+    private AccountManagementMenu? _window;
+
+    public AccountManagementConsoleBoundUserInterface(EntityUid owner, Enum uiKey) : base(owner, uiKey)
+    {
+        _banking = EntMan.System<ClientBankingSystem>();
+    }
+
+    protected override void Open()
+    {
+        base.Open();
+
+        _window = this.CreateWindow<AccountManagementMenu>();
+        _window.OpenCentered();
+        _window.FillOutAccountButtons(EntMan);
+    }
+}
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/ATMMenu.xaml
similarity index 82%
rename from Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml
rename to Content.Client/_Impstation/PersonalEconomy/UI/ATM/ATMMenu.xaml
index 941c75f2bf9f6..6bce8312a6183 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/ATMMenu.xaml
@@ -1,13 +1,14 @@
 ﻿<controls:FancyWindow xmlns="https://spacestation14.io"
             xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
             xmlns:ui="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI"
+            xmlns:atm="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI.ATM"
             Title="{Loc 'atm-machine-window-title'}"
             MinSize="512 320"
             SetSize="512 320">
     <BoxContainer Orientation="Horizontal">
         <BoxContainer Orientation="Vertical">
             <Control VerticalExpand="True"/>
-            <ui:Keypad Name="UIKeypad" NumberLength="6"/>
+            <atm:Keypad Name="UIKeypad" NumberLength="6"/>
             <Control VerticalExpand="True"/>
         </BoxContainer>
         <BoxContainer Name="AccountInfoContainer" Orientation="Vertical" HorizontalExpand="True" VerticalExpand="True"/>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/ATMMenu.xaml.cs
similarity index 95%
rename from Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs
rename to Content.Client/_Impstation/PersonalEconomy/UI/ATM/ATMMenu.xaml.cs
index c5477a6071757..efa8d3c29ddd1 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/ATMMenu.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/ATMMenu.xaml.cs
@@ -5,10 +5,9 @@
 using Robust.Client.AutoGenerated;
 using Robust.Client.Player;
 using Robust.Client.UserInterface.XAML;
-using Robust.Shared.Network;
 using Robust.Shared.Timing;
 
-namespace Content.Client._Impstation.PersonalEconomy.UI;
+namespace Content.Client._Impstation.PersonalEconomy.UI.ATM;
 
 [GenerateTypedNameReferences]
 public sealed partial class ATMMenu : FancyWindow
@@ -74,7 +73,7 @@ public void CreateInfoBoxForAccount(Entity<BankAccountComponent> account)
     {
         ClearInfoBox();
 
-        var infobox = new AccountInfoBox(account);
+        var infobox = new ATM.AccountInfoBox(account);
         AccountInfoContainer.AddChild(infobox);
         _accountOpen = true;
 
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/AccountInfoBox.xaml
similarity index 100%
rename from Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml
rename to Content.Client/_Impstation/PersonalEconomy/UI/ATM/AccountInfoBox.xaml
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/AccountInfoBox.xaml.cs
similarity index 94%
rename from Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs
rename to Content.Client/_Impstation/PersonalEconomy/UI/ATM/AccountInfoBox.xaml.cs
index 822c11e5b518e..dc58cd8c770cf 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/AccountInfoBox.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/AccountInfoBox.xaml.cs
@@ -1,12 +1,10 @@
-﻿using System.Linq;
-using Content.Shared._Impstation.PersonalEconomy.Components;
+﻿using Content.Shared._Impstation.PersonalEconomy.Components;
 using Robust.Client.AutoGenerated;
-using Robust.Client.UserInterface;
 using Robust.Client.UserInterface.Controls;
 using Robust.Client.UserInterface.XAML;
 using Robust.Shared.Timing;
 
-namespace Content.Client._Impstation.PersonalEconomy.UI;
+namespace Content.Client._Impstation.PersonalEconomy.UI.ATM;
 
 [GenerateTypedNameReferences]
 public sealed partial class AccountInfoBox : BoxContainer
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/Keypad.xaml
similarity index 92%
rename from Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml
rename to Content.Client/_Impstation/PersonalEconomy/UI/ATM/Keypad.xaml
index 5cddc692c64ec..a833f09ce954e 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/Keypad.xaml
@@ -1,6 +1,7 @@
-﻿<controls:Keypad
+﻿<atm:Keypad
     xmlns="https://spacestation14.io"
-    xmlns:controls="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI">
+    xmlns:controls="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI"
+    xmlns:atm="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI.ATM">
     <BoxContainer Orientation="Vertical" HorizontalExpand="False" HorizontalAlignment="Left">
         <LineEdit Name="NumberDisplay" HorizontalExpand="True" PlaceHolder="000000"/>
         <GridContainer Name="ButtonGrid" Columns="3" VSeparationOverride="0" HSeparationOverride="0">
@@ -18,4 +19,4 @@
             <Button Name="ClearButton" Text="CLR" StyleClasses="OpenBoth" Margin="0"/> <!--todo replace clr with a del button?-->
         </GridContainer>
     </BoxContainer>
-</controls:Keypad>
+</atm:Keypad>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/Keypad.xaml.cs
similarity index 94%
rename from Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml.cs
rename to Content.Client/_Impstation/PersonalEconomy/UI/ATM/Keypad.xaml.cs
index 6f1833f5256da..78005692a9d72 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/Keypad.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/Keypad.xaml.cs
@@ -3,7 +3,7 @@
 using Robust.Client.UserInterface;
 using Robust.Client.UserInterface.XAML;
 
-namespace Content.Client._Impstation.PersonalEconomy.UI;
+namespace Content.Client._Impstation.PersonalEconomy.UI.ATM;
 
 [GenerateTypedNameReferences]
 public sealed partial class Keypad : Control
@@ -23,7 +23,7 @@ public Keypad()
 
         NumberDisplay.OnTextChanged += args =>
         {
-            var numberStr = string.Concat(args.Text.Where(char.IsDigit));
+            var numberStr = string.Concat(Enumerable.Where<char>(args.Text, char.IsDigit));
             if (numberStr.Length > NumberLength)
                 numberStr = numberStr[..NumberLength];
 
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/NoAccountInfoBox.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/NoAccountInfoBox.xaml
similarity index 100%
rename from Content.Client/_Impstation/PersonalEconomy/UI/NoAccountInfoBox.xaml
rename to Content.Client/_Impstation/PersonalEconomy/UI/ATM/NoAccountInfoBox.xaml
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/NoAccountInfoBox.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/NoAccountInfoBox.xaml.cs
similarity index 76%
rename from Content.Client/_Impstation/PersonalEconomy/UI/NoAccountInfoBox.xaml.cs
rename to Content.Client/_Impstation/PersonalEconomy/UI/ATM/NoAccountInfoBox.xaml.cs
index 10fb38d9033eb..8a605d776f912 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/NoAccountInfoBox.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/NoAccountInfoBox.xaml.cs
@@ -1,9 +1,8 @@
 ﻿using Robust.Client.AutoGenerated;
-using Robust.Client.UserInterface;
 using Robust.Client.UserInterface.Controls;
 using Robust.Client.UserInterface.XAML;
 
-namespace Content.Client._Impstation.PersonalEconomy.UI;
+namespace Content.Client._Impstation.PersonalEconomy.UI.ATM;
 
 [GenerateTypedNameReferences]
 public sealed partial class NoAccountInfoBox : BoxContainer
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/TransactionInfoBox.xaml
similarity index 77%
rename from Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml
rename to Content.Client/_Impstation/PersonalEconomy/UI/ATM/TransactionInfoBox.xaml
index 0d47c103319f9..8a0d5034fbd11 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/TransactionInfoBox.xaml
@@ -1,6 +1,7 @@
-﻿<controls:TransactionInfoBox
+﻿<atm:TransactionInfoBox
     xmlns="https://spacestation14.io"
     xmlns:controls="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI"
+    xmlns:atm="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI.ATM"
     VerticalExpand="True"
     MouseFilter="Stop">
     <BoxContainer Orientation="Vertical" VerticalExpand="True">
@@ -10,4 +11,4 @@
         <Label Name="AmountLabel"/>
         <Control VerticalExpand="True"/>
     </BoxContainer>
-</controls:TransactionInfoBox>
+</atm:TransactionInfoBox>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/TransactionInfoBox.xaml.cs
similarity index 95%
rename from Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml.cs
rename to Content.Client/_Impstation/PersonalEconomy/UI/ATM/TransactionInfoBox.xaml.cs
index b051c66d7b62c..706ca3ed63831 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionInfoBox.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/TransactionInfoBox.xaml.cs
@@ -3,7 +3,7 @@
 using Robust.Client.UserInterface;
 using Robust.Client.UserInterface.XAML;
 
-namespace Content.Client._Impstation.PersonalEconomy.UI;
+namespace Content.Client._Impstation.PersonalEconomy.UI.ATM;
 
 [GenerateTypedNameReferences]
 public sealed partial class TransactionInfoBox : Control
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/TransactionMenu.xaml
similarity index 100%
rename from Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml
rename to Content.Client/_Impstation/PersonalEconomy/UI/ATM/TransactionMenu.xaml
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/TransactionMenu.xaml.cs
similarity index 88%
rename from Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml.cs
rename to Content.Client/_Impstation/PersonalEconomy/UI/ATM/TransactionMenu.xaml.cs
index ca198c0303d99..4b7e1170c9b60 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/TransactionMenu.xaml.cs
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/ATM/TransactionMenu.xaml.cs
@@ -3,7 +3,7 @@
 using Robust.Client.AutoGenerated;
 using Robust.Client.UserInterface.XAML;
 
-namespace Content.Client._Impstation.PersonalEconomy.UI;
+namespace Content.Client._Impstation.PersonalEconomy.UI.ATM;
 
 [GenerateTypedNameReferences]
 public sealed partial class TransactionMenu : FancyWindow
@@ -26,7 +26,7 @@ public TransactionMenu()
 
         TransferNumberBox.OnTextChanged += args =>
         {
-            var numberStr = string.Concat(args.Text.Where(char.IsDigit));
+            var numberStr = string.Concat(Enumerable.Where<char>(args.Text, char.IsDigit));
             if (numberStr.Length > 4)
                 numberStr = numberStr[..4];
 
@@ -35,7 +35,7 @@ public TransactionMenu()
 
         TransferAmountBox.OnTextChanged += args =>
         {
-            var amountStr = string.Concat(args.Text.Where(char.IsDigit));
+            var amountStr = string.Concat(Enumerable.Where<char>(args.Text, char.IsDigit));
             if (amountStr.Length > 9)
                 amountStr = amountStr[..9];
 
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/AccountInfoSelectorButtonThing.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/AccountInfoSelectorButtonThing.xaml
new file mode 100644
index 0000000000000..de99b64b8b767
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/AccountInfoSelectorButtonThing.xaml
@@ -0,0 +1,5 @@
+﻿<Control
+    xmlns="https://spacestation14.io"
+    xmlns:controls="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI.AccountManagement">
+    <Label Name="TransferNumberLabel"/>
+</Control>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/AccountInfoSelectorButtonThing.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/AccountInfoSelectorButtonThing.xaml.cs
new file mode 100644
index 0000000000000..e9b6dcd0de097
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/AccountInfoSelectorButtonThing.xaml.cs
@@ -0,0 +1,24 @@
+﻿using Content.Shared._Impstation.PersonalEconomy.Components;
+using Robust.Client.AutoGenerated;
+using Robust.Client.UserInterface;
+using Robust.Client.UserInterface.Controls;
+using Robust.Client.UserInterface.XAML;
+
+namespace Content.Client._Impstation.PersonalEconomy.UI.AccountManagement;
+
+[GenerateTypedNameReferences]
+public sealed partial class AccountInfoSelectorButtonThing : Control
+{
+    public AccountInfoSelectorButtonThing()
+    {
+        RobustXamlLoader.Load(this);
+    }
+
+    public AccountInfoSelectorButtonThing(Entity<BankAccountComponent> account)
+    {
+        RobustXamlLoader.Load(this); //todo make this work gooder. need to make it a custom control type that handles clicks etc etc itself
+
+        TransferNumberLabel.Text = $"{((int) account.Comp.TransferNumber):0000}";
+    }
+}
+
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/AccountManagementMenu.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/AccountManagementMenu.xaml
new file mode 100644
index 0000000000000..def7d5eea3aac
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/AccountManagementMenu.xaml
@@ -0,0 +1,12 @@
+﻿<controls:FancyWindow
+    xmlns="https://spacestation14.io"
+    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
+    xmlns:accountManagement="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI.AccountManagement"
+    MinSize="512 512"
+    SetSize="512 512"
+    Title="{Loc account-management-window-title}">
+    <TabContainer>
+        <accountManagement:SalaryManagementPanel Name="SalaryPanel"/>
+        <accountManagement:DetailManagementPanel Name="DetailPanel"/>
+    </TabContainer>
+</controls:FancyWindow>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/AccountManagementMenu.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/AccountManagementMenu.xaml.cs
new file mode 100644
index 0000000000000..ef9ae58663077
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/AccountManagementMenu.xaml.cs
@@ -0,0 +1,31 @@
+﻿using Content.Client.UserInterface.Controls;
+using Content.Shared._Impstation.PersonalEconomy.Components;
+using Robust.Client.AutoGenerated;
+using Robust.Client.UserInterface;
+using Robust.Client.UserInterface.Controls;
+using Robust.Client.UserInterface.XAML;
+
+namespace Content.Client._Impstation.PersonalEconomy.UI.AccountManagement;
+
+[GenerateTypedNameReferences]
+public sealed partial class AccountManagementMenu : FancyWindow
+{
+    public AccountManagementMenu()
+    {
+        RobustXamlLoader.Load(this);
+
+        TabContainer.SetTabTitle(SalaryPanel, Loc.GetString("salary-panel-tab-name"));
+        TabContainer.SetTabTitle(DetailPanel, Loc.GetString("detail-panel-tab-name"));
+    }
+
+    public void FillOutAccountButtons(IEntityManager entMan)
+    {
+        var accountQuery = entMan.EntityQueryEnumerator<BankAccountComponent>();
+        while (accountQuery.MoveNext(out var uid, out var comp))
+        {
+            var selector = new AccountInfoSelectorButtonThing((uid, comp));
+            SalaryPanel.AddSelectorThingToList(selector);
+        }
+    }
+}
+
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/DetailManagementPanel.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/DetailManagementPanel.xaml
new file mode 100644
index 0000000000000..e3a3a24ee5a0e
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/DetailManagementPanel.xaml
@@ -0,0 +1,6 @@
+﻿<BoxContainer
+    xmlns="https://spacestation14.io"
+    xmlns:controls="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI.AccountManagement"
+    Orientation="Vertical">
+    <Label Text="detail gaming"/>
+</BoxContainer>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/DetailManagementPanel.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/DetailManagementPanel.xaml.cs
new file mode 100644
index 0000000000000..192f86cd6b5c7
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/DetailManagementPanel.xaml.cs
@@ -0,0 +1,16 @@
+﻿using Robust.Client.AutoGenerated;
+using Robust.Client.UserInterface;
+using Robust.Client.UserInterface.Controls;
+using Robust.Client.UserInterface.XAML;
+
+namespace Content.Client._Impstation.PersonalEconomy.UI.AccountManagement;
+
+[GenerateTypedNameReferences]
+public sealed partial class DetailManagementPanel : BoxContainer
+{
+    public DetailManagementPanel()
+    {
+        RobustXamlLoader.Load(this);
+    }
+}
+
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/SalaryManagementPanel.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/SalaryManagementPanel.xaml
new file mode 100644
index 0000000000000..7e6925cdcc225
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/SalaryManagementPanel.xaml
@@ -0,0 +1,20 @@
+﻿<BoxContainer
+    xmlns="https://spacestation14.io"
+    xmlns:controls="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI.AccountManagement"
+    Orientation="Vertical">
+    <Label Name="TotalAllocationLabel" Text="Station Salary Allocation: "/>
+    <Label Name="CurrentSalaryLabel" Text="Current Salary Total: "/>
+    <Label Name="WageSlushFundAmountLabel" Text="Bonus Wage Amount: "/>
+    <Label Name="WageCutFromCargoAmountLabel" Text="Wage Cut From Cargo: "/>
+    <Label Name="TimeUntilNextPaymentLabel" Text="Time Until Next Payment: "/>
+    <BoxContainer VerticalExpand="True" HorizontalExpand="True" Orientation="Horizontal">
+        <ScrollContainer VerticalExpand="True" VScrollEnabled="True" HorizontalExpand="True" HScrollEnabled="False">
+            <BoxContainer Name="AccountInfoSelectorContainer">
+                <!-- filled out in-code -->
+            </BoxContainer>
+        </ScrollContainer>
+        <BoxContainer Name="AccountInfoDisplayContainer" VerticalExpand="True" HorizontalExpand="True">
+            <!-- filled out in-code after something from the scroller is selected-->
+        </BoxContainer>
+    </BoxContainer>
+</BoxContainer>
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/SalaryManagementPanel.xaml.cs b/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/SalaryManagementPanel.xaml.cs
new file mode 100644
index 0000000000000..f3a3f8b3f4d4d
--- /dev/null
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/AccountManagement/SalaryManagementPanel.xaml.cs
@@ -0,0 +1,23 @@
+﻿using Robust.Client.AutoGenerated;
+using Robust.Client.UserInterface;
+using Robust.Client.UserInterface.Controls;
+using Robust.Client.UserInterface.XAML;
+
+namespace Content.Client._Impstation.PersonalEconomy.UI.AccountManagement;
+
+[GenerateTypedNameReferences]
+public sealed partial class SalaryManagementPanel : BoxContainer
+{
+    public SalaryManagementPanel()
+    {
+        RobustXamlLoader.Load(this);
+    }
+
+    public void AddSelectorThingToList(AccountInfoSelectorButtonThing thing)
+    {
+        AccountInfoSelectorContainer.AddChild(thing);
+        AccountInfoSelectorContainer.InvalidateArrange();
+        AccountInfoSelectorContainer.InvalidateMeasure();
+    }
+}
+
diff --git a/Content.Client/_Impstation/PersonalEconomy/UI/POS/POSMenu.xaml b/Content.Client/_Impstation/PersonalEconomy/UI/POS/POSMenu.xaml
index 36149e8321dde..b22f711500269 100644
--- a/Content.Client/_Impstation/PersonalEconomy/UI/POS/POSMenu.xaml
+++ b/Content.Client/_Impstation/PersonalEconomy/UI/POS/POSMenu.xaml
@@ -2,13 +2,14 @@
     xmlns="https://spacestation14.io"
     xmlns:controls1="clr-namespace:Content.Client.UserInterface.Controls"
     xmlns:ui="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI"
+    xmlns:atm="clr-namespace:Content.Client._Impstation.PersonalEconomy.UI.ATM"
     Title="{Loc pos-window-title}"
     MinSize="512 320"
     SetSize="512 320">
     <BoxContainer Orientation="Horizontal">
         <BoxContainer Orientation="Vertical">
             <Control VerticalExpand="True"/>
-            <ui:Keypad Name="UIKeypad" NumberLength="6" Access="Public"/>
+            <atm:Keypad Name="UIKeypad" NumberLength="6" Access="Public"/>
             <Control VerticalExpand="True"/>
         </BoxContainer>
         <BoxContainer Name="MainContainer" Orientation="Vertical" VerticalExpand="True" HorizontalExpand="True"/>
diff --git a/Content.Shared/_Impstation/PersonalEconomy/BankingUIKeys.cs b/Content.Shared/_Impstation/PersonalEconomy/BankingUIKeys.cs
index 412b6a65bb98a..3bffc7b8866a4 100644
--- a/Content.Shared/_Impstation/PersonalEconomy/BankingUIKeys.cs
+++ b/Content.Shared/_Impstation/PersonalEconomy/BankingUIKeys.cs
@@ -14,3 +14,8 @@ public enum POSUIKey : byte
     Key
 }
 
+[Serializable, NetSerializable]
+public enum AccountManagementUIKey : byte
+{
+    Key
+}
diff --git a/Content.Shared/_Impstation/PersonalEconomy/Components/AccountManagementConsoleComponent.cs b/Content.Shared/_Impstation/PersonalEconomy/Components/AccountManagementConsoleComponent.cs
new file mode 100644
index 0000000000000..bc3af72dbf733
--- /dev/null
+++ b/Content.Shared/_Impstation/PersonalEconomy/Components/AccountManagementConsoleComponent.cs
@@ -0,0 +1,10 @@
+﻿namespace Content.Shared._Impstation.PersonalEconomy.Components;
+
+/// <summary>
+/// This is used to mark an entity as an account management console
+/// </summary>
+[RegisterComponent]
+public sealed partial class AccountManagementConsoleComponent : Component
+{
+
+}
diff --git a/Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl b/Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl
index 2092dadac923e..0dabb3c99199c 100644
--- a/Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl
+++ b/Resources/Locale/en-US/_Impstation/personal-economy/banking.ftl
@@ -51,3 +51,8 @@ pos-payment-spesito-amount = ${$amount} Spesitos
 pos-payment-reason = for "{$reason}"
 pos-payment-confirm-button-label = Confirm Transaction
 pos-payment-cancel-button-label = Cancel
+
+#account management console
+account-management-window-title = Account Management Console
+salary-panel-tab-name = Manage salaries & view transactions
+detail-panel-tab-name = Manage Account Details
diff --git a/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml b/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml
index 9ed60c94306ef..5f2ecc2eb9161 100644
--- a/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml
+++ b/Resources/Prototypes/_Impstation/Entities/Objects/Misc/banking.yml
@@ -5,14 +5,16 @@
   description: The little square of plastic that controls your life. No, the other one.
   components:
   - type: Sprite
-    sprite: _Impstation/Objects/Misc/id_cards.rsi
+    sprite: Objects/Misc/id_cards.rsi
     layers:
     - state: default
-    - state: idpassenger
+    - state: Passenger
+      sprite: Interface/Misc/job_icons.rsi
+      offset: -0.09375, 0.0625 #3 pixels left, 2 pixels up - copied from id cards
   - type: Clothing
     slots:
     - idcard
-    sprite: _Impstation/Objects/Misc/id_cards.rsi
+    sprite: Objects/Misc/id_cards.rsi
   - type: Item
     size: Small
     heldPrefix: default
@@ -23,6 +25,7 @@
   id: BankAccount
   name: Bank Account
   description: if you're seeing this, something has gone horribly wrong. admins yell at me and I'll make these properly interactable at some point.
+  categories: [ HideSpawnMenu ]
   components:
     - type: BankAccount
 
@@ -64,6 +67,44 @@
     color: "#e6e227"
   - type: ATM
 
+- type: entity
+  parent: BaseComputerAiAccess #ai accessible because it's really funny if someone convinces the AI to let them have all the money
+  id: AccountManagementConsole
+  name: Account Management Console
+  description: The other other computer that controls your life.
+  components:
+  - type: Sprite
+    layers:
+    - map: ["computerLayerBody"]
+      state: computer
+      sprite: _Impstation/Structures/Machines/computers.rsi
+    - map: ["computerLayerKeyboard"]
+      state: generic_keyboard
+      sprite: _Impstation/Structures/Machines/computers.rsi
+    - map: ["computerLayerScreen"]
+      state: allocate
+    - map: ["computerLayerKeys"]
+      state: generic_keys
+    - map: [ "enum.WiresVisualLayers.MaintenancePanel" ]
+      state: generic_panel_open
+      sprite: _Impstation/Structures/Machines/computers.rsi
+  - type: ActivatableUI
+    key: enum.AccountManagementUIKey.Key
+    singleUser: true
+  - type: UserInterface
+    interfaces:
+      enum.AccountManagementUIKey.Key:
+        type: AccountManagementConsoleBoundUserInterface
+      enum.WiresUiKey.Key:
+        type: WiresBoundUserInterface
+  - type: Computer
+    board: RadarConsoleCircuitboard
+  - type: PointLight
+    radius: 2.4
+    energy: 1.6
+    color: "#e6e227"
+  - type: AccountManagementConsole
+
 - type: entity
   parent: BaseItem
   id: POSSystem
